# AstroQ — Hướng dẫn làm việc (đọc trước khi bắt đầu)

> File này để Claude nắm cách làm việc & quy trình deploy — **không hỏi lại** những điều đã ghi ở đây.

## 1. Dự án
- Web **tĩnh, giáo dục** về Hệ Mặt Trời cho trẻ em. **HTML/CSS/JS thuần**, không framework, không build step.
- Thư mục mã nguồn (đồng thời là git repo): **`AstroQhtml/`** (chính là thư mục chứa file này).
- Phong cách UI: **glassmorphism + sci-fi/cockpit**, tông không gian (deep space, neon cyan `#38bdf8`, sun `#ffcf6b`, tím `#8f7bff`).
- **Song ngữ VI/EN**, đồng bộ qua `localStorage["astroq-lang"]` (mọi trang lắng nghe event `storage`). VI mặc định.
- Luôn tôn trọng `@media (prefers-reduced-motion: reduce)`.
- Ngôn ngữ trao đổi với người dùng: **Tiếng Việt**.

## 2. Cấu trúc file

> **Quy tắc bắt buộc (2026-07-26):**
> 1. **CSS luôn nằm trong `css/*.css`** — không viết `<style>` trong HTML, không dùng `style="..."` inline (trừ giá trị **động** do JS sinh: gradient theo dữ liệu, `--c1/--c2`, transform theo zoom… và trạng thái `display` do JS bật/tắt).
> 2. **Thứ dùng chung thì tách ra dùng lại**, không copy-paste giữa các trang: `css/common.css`, `js/ui-common.js`, `js/icons.js`, `economy.js`.
>
> Thứ tự nạp ở mỗi trang: `css/common.css` → `css/<trang>.css` → `js/icons.js` (nếu dùng icon) → `js/ui-common.js` → `economy.js` → script riêng của trang.

- **`css/game-shell.css` — khung dùng chung của MỌI mini-game** (nạp giữa `common.css` và `css/<game>.css`): header, `.stage`/`.play`/`.field`, thanh HUD + `.chip`, nút `.iconbtn` (pause/âm thanh), toàn bộ overlay `.ov`/`.ov-card`/`.res`/`.acts`/`.paid`, toast. Trang game chỉ còn phần **riêng** trong `css/<game>.css`: tỉ lệ sân (`aspect-ratio` + `max-width`) và thứ đặc thù (ngôi sao chấm điểm ở dodge; thanh giáp + ô quiz ở defender; chip tên chòm sao + modal thông tin ở constellation). **Làm game mới thì nạp `game-shell.css` chứ đừng copy khung.** Hai chi tiết đã sửa ở khung (2026-07-28): `.top .tag` dùng `nowrap` + ellipsis (tên game dài xuống dòng làm dấu `]` rơi xuống hàng dưới), và **trên màn ≤560px toast neo xuống ĐÁY** — điện thoại thì header xuống 2 dòng nên thanh HUD tụt qua mốc `top:116px` và toast đè kín các chip.
- `css/common.css` — token màu/chữ cơ bản, reset, và **component dùng chung**: `.back-btn`, `.lang-switch`, `.currency`/`.gem`, `.toast` (+ `.toast-msg`/`.toast-ic`), `.modal`, `.starfield`/`.nebula`, `.tt-inline`, `.lic`, keyframes (`drift/tw/blink/gemBump`), `@media (prefers-reduced-motion)`. Mỗi trang có `css/<trang>.css` cho phần riêng (nạp sau nên override được).
- `js/ui-common.js` — `AstroQ` + `$`: `getUser/setUser/clearUser`, `getLang/setLang/markLangButtons/initLang` (gắn nút `.lang-switch` + đồng bộ tab khác), **`applyTexts(t)`**, `makeToast(el|id, ms)` (toast có token `{tt}` → ảnh Thiên thạch tím với `alt` đổi theo ngôn ngữ, `type` `ok`/`bad` → icon SVG), `ttImg()`, `esc`.
- **Quy ước i18n (dùng `AstroQ.applyTexts(t)` trong `applyLang` của mọi trang):** `data-i18n` → textContent · `data-i18n-html` → innerHTML · `data-i18n-ph` → placeholder · `data-i18n-title` → title · `data-i18n-aria` → aria-label · `data-i18n-alt` → alt. **Thêm chuỗi mới thì phải thêm khoá vào CẢ `vi` và `en`.** Với markup do JS sinh ra sau khi `applyTexts` chạy, nội suy `t("key")` thẳng vào chuỗi thay vì gắn `data-i18n-*` (vì phần tử tạo sau sẽ không được dịch lại).
- `js/icons.js` — 22 icon lucide-style dùng chung, gọi `lic("telescope")`.
- **Backend AstroqSV** — `js/api.js` là **CHỖ DUY NHẤT** chứa địa chỉ máy chủ: `PROD_API` (`https://ueqp4gjr0l.execute-api.ap-southeast-1.amazonaws.com`) và `LOCAL_API` (`http://localhost:5080`). `apiPost/apiGet` có timeout 20s và **không bao giờ ném lỗi ra ngoài** — luôn trả `{ok, status, data, netError?}` để phía giao diện chỉ phải xử lý một hình dạng dữ liệu. **Ba chế độ chạy song song**, đổi bằng `?api=prod|local|<url>|reset` (nhớ ở `localStorage["astroq-api"]`); khi không phải bản thật thì góc dưới-trái hiện huy hiệu `.env-badge`. Mã nguồn backend nằm **ngoài repo** ở `../AstroqSV/`; xem thử ở máy: `python -m http.server 8000` + (tuỳ chọn) `dotnet run` trong `AstroqSV/src/AstroqSV.Api`. Chi tiết `docs/backend-astroqsv.md` mục 0b–0c.
- **Đăng nhập (Firebase Auth)** — `js/firebase-config.js` (CHỖ DUY NHẤT cần điền config) · `js/firebase-auth.js` (logic, ES module, tải SDK **động** nên chưa cấu hình thì không tốn byte nào) · `js/firebase-auth-ui.js` (gắn vào popup ở `landing-app.html`). **Hai nơi, hai vai trò: ĐĂNG KÝ đi qua backend AstroqSV, ĐĂNG NHẬP đi thẳng Firebase.** Firebase chỉ chứa tài khoản **đã kích hoạt** — tài khoản chỉ được tạo khi người dùng bấm link trong email (xem `docs/backend-astroqsv.md` mục 0b). **Chưa điền config → tự lùi về chế độ demo cũ**, trang không vỡ. Hồ sơ vẫn ghi vào `localStorage["astroq-user"]` như trước, chỉ thêm `uid` → dashboard/select/quiz không phải sửa. Hướng dẫn đầy đủ: `docs/firebase-auth.md`. ⚠️ **Không nạp Firebase ở `index.html` và `wiki/`** — SDK nặng hơn cả trang chủ (233 KB), hai khu đó đang tối ưu SEO/AEO.
- `wiki/` — **bộ 10 bài AEO Wiki × 2 ngôn ngữ**: `wiki/*.html` (VI) và `wiki/en/*.html` (EN), mỗi bản có mục lục riêng. CSS ở `css/wiki.css`. **Trang tĩnh thuần, KHÔNG nạp JS.** Mỗi bài theo *Direct Answer Pattern*: H1 là câu hỏi → khối `.answer` **40–60 từ** (phần AI Search trích dẫn) → thân bài H2/H3 + bảng → **Góc Khám Phá astroQ** (Comet & Byte) → **Nguồn tham chiếu** NASA/ESA/MIT (URL đã kiểm 200) → bài liên quan → CTA. `<head>` có JSON-LD **Article + FAQPage + BreadcrumbList** và **3 thẻ `hreflang`** (vi / en / x-default) trỏ chéo giữa 2 bản. Slug EN khác slug VI (vd `vat-ly-luong-tu-la-gi` ↔ `en/what-is-quantum-physics`) — ánh xạ nằm trong `gen_wiki_data_en.py`. Đường dẫn asset: `../` từ `wiki/`, `../../` từ `wiki/en/`. **Thêm/sửa bài:** sửa `scratchpad/gen_wiki_data.py` + `gen_wiki_data_en.py` rồi chạy `gen_wiki.py` (tự assert đoạn trả lời 40–60 từ cho cả 2 ngôn ngữ), sau đó cập nhật `sitemap.xml` và `llms.txt`.
- `index.html` — **trang chủ = landing "Sắp ra mắt"** (+ `css/index.css`, `js/index.js`): hero + đếm ngược tới **`2026-08-09T00:00+07:00`** (đổi 31/07/2026; **ngày ra mắt nằm ở 6 chỗ phải sửa CÙNG LÚC**: `LAUNCH_AT` trong `js/index.js`, `<title>` ở cả `index.html` và 2 từ điển VI/EN, `"datePublished"` + câu trả lời FAQ trong **JSON-LD**, đoạn `a5` hiển thị, và câu nhắc `wl_hint` của form — JSON-LD lệch với phần hiển thị là Google coi như dữ liệu sai), linh vật Comet (`img/m1.png`) & Byte (`img/b1.png`), form email nhận **500 Purple Meteors** (lưu `localStorage["astroq-waitlist"]`, `submitWaitlist()` mô phỏng API — đổi sang `fetch` thật chỉ ở 1 chỗ), khối AEO "astroQ.org là gì?" + FAQ. `<head>` có JSON-LD **`EducationalApplication`** + **`FAQPage`** (nội dung FAQ khớp 1-1 với phần hiển thị — sửa 1 bên phải sửa bên kia). Đã go-live: `index,follow`, canonical + `og:url` = `https://astroq.org/`.
- `landing-app.html` — landing cũ của app (chính là `index.html` trước 2026-07-26) + `css/landing-app.css`: màn kịch nhân vật bay, **1 nút "Trải nghiệm ngay"** mở popup Đăng ký/Đăng nhập. Đang `noindex,follow` để không tranh từ khoá với trang chủ; **bỏ meta robots này khi app mở cửa**. Là đích đến của nút Đăng xuất ở dashboard và logo ở select.

- `select.html` — **Cấp Thẻ ID & Chọn Nhân Vật** (ID card glassmorphism, roster 3D, HUD info).
- `dashboard.html` — **TRUNG TÂM ĐIỀU HƯỚNG (Navigation Hub)**, trước 29/07/2026 gọi là "Khoang Lái": header trạng thái tàu, **6 card HUD**, Stats HUD + console radar/log. **Phi hành gia MỚI vào lần đầu thì Comet dẫn tham quan tàu rồi tới màn loading bay tới Trái Đất** — xem 2 file dưới. **Mọi số trên trang là SỐ THẬT** lấy từ `GET /me/achievements` (một lần gọi trả cả cấp độ, bộ đếm và huy hiệu kèm ngày mở); chưa đăng nhập / mất mạng thì về 0 và nhật ký rỗng, **không đoán không bịa**. Radar kỹ năng **suy ra từ bộ đếm** (công thức ghi thẳng trong `radarData()`), nhật ký = **huy hiệu mở gần đây nhất** theo `earnedAt`. Bấm chip avatar ở header → `profile.html`; nút "🏆 Mở Kho Thành Tích" dưới cột stat tile → `achievements.html`.
  - **ĐỔI TÊN KHU VỰC (29/07/2026) — dùng đúng tên mới, đừng viết lại tên cũ.** Có phép kiểm ở `scratchpad/check_pages.py` mục [7] canh cả hai chiều:

    | EN | VI | Trang |
    |---|---|---|
    | Navigation Hub | Trung Tâm Điều Hướng | `dashboard.html` *(trước: Khoang Lái / Cockpit)* |
    | Mission Control | Trung Tâm Nhiệm Vụ | `missions.html` **(mới)** |
    | Knowledge Station | Trạm Tri Thức | `learn.html` |
    | Training Simulator | Khu Huấn Luyện | `games.html` *(trước EN: Training Bay)* |
    | Research Lab | Phòng Nghiên Cứu | **chưa có trang** |
    | Star Archive | Thư Viện Thiên Văn | **chưa có trang** |

    ⚠️ **"Khoang lái" theo nghĩa BUỒNG LÁI CỦA CON TÀU thì GIỮ NGUYÊN, cố ý:** bàn trưng mẫu vật ở `specimen-vault.html` ("Đặt lên Bàn Điều Khiển Khoang Lái"), lời SEO ở `index.html` + `js/index.js` ("giao diện khoang lái phi thuyền"), chú thích ảnh nền, mô tả nhịp phim ở `js/mission-intro.js`. "Mẫu vật trưng ở Trung Tâm Điều Hướng" là câu vô nghĩa. Phép kiểm vì thế chỉ cấm đúng những **chuỗi dùng nó làm TÊN TRANG** (`Về Khoang Lái`, `Back to Cockpit`, `home_btn:"Khoang Lái"`…), không cấm chữ "khoang lái" nói chung.
    ⚠️ `wiki/` CỐ Ý chưa sửa: đó là bộ trang SEO **sinh ra** từ `scratchpad/gen_wiki_data*.py`, sửa tay là lần sinh sau mất. Sửa thì phải đổi generator → sinh lại → cập nhật `sitemap.xml`.
    ⚠️ **Tên chốt lại 31/07/2026: quay về "Trạm Tri Thức"**, dùng nguyên cụm ở mọi nơi. Ngày 29/07 tên bị rút thành "Tri Thức" trơn, và chính vì thiếu danh từ chỉ loại nên trong câu văn phải chống đỡ bằng cách viết "khu Tri Thức" — tức là **một khu có hai cách gọi**. Có chữ "Trạm" thì câu tự đứng được: "Hãy ghé Trạm Tri Thức làm Quiz". Khớp luôn bản EN "Knowledge Station" (đã là *Station* từ đầu). Đổi 25 chỗ ở 11 file; phép kiểm mục [7] của `check_pages.py` canh cả hai chiều.
    ⚠️ **`index.html` + `js/index.js` CỐ Ý giữ "Khám Phá Ngân Hà Tri Thức"** — đó là một câu văn quảng bá, không phải tên khu; mà trang chủ đã go-live, được lập chỉ mục, và nội dung FAQ phải khớp **1-1** với khối JSON-LD. Sửa chữ ở đó là việc SEO, không phải việc đổi tên khu.
  - **6 CARD HUD, SỐ HIỆU `MOD-nn` GHI CỐ ĐỊNH.** Thứ tự hiển thị ≠ số hiệu (như `games.html`): **MOD-04** 🎯 Trung Tâm Nhiệm Vụ → `missions.html` · **MOD-01** 🧠 Tri Thức → `learn.html` · **MOD-02** 🎮 Khu Huấn Luyện → `games.html` · **MOD-03** 🌌 Bản Đồ Thiên Hà → `explorer.html` · **MOD-05** 🔬 Phòng Nghiên Cứu *(sắp ra mắt)* · **MOD-06** 📚 Thư Viện Thiên Văn *(sắp ra mắt)*. Ba card cũ **giữ nguyên MOD-01/02/03** — cùng bài học với `code:"ARCADE-nn"`: số hiệu đã đi vào tài liệu và cách người dùng gọi tên, đánh số lại là mọi tài liệu cũ sai hết. Card mới lấy số kế tiếp.
    - **Hai khu CHƯA CÓ TRANG thì phải nói thật:** class `.hud.soon` + nút `disabled` + đèn/nhãn `.standby` màu hổ phách (`SYS.STANDBY`) + **đèn ngừng nhấp nháy** + bỏ `hudPulse` và bỏ hiệu ứng nhấc-lên khi hover. `SYS.ONLINE` màu xanh cạnh một cái nút bấm không được là hai thông điệp ngược nhau. ⚠️ **KHÔNG dùng `filter:grayscale()`** để làm mờ: trên nền gradient sáng nó cho ra một khối xám SÁNG HƠN card bình thường (đo trên ảnh chụp ở nút Mặt Trăng của màn tổng kết nhiệm vụ), tức là hút mắt vào đúng cái không dùng được.
    - **Màn Comet dẫn tham quan VẪN 7 BƯỚC** — ba card mới không có `data-tour`, xem lý do ghi trong `js/onboard-tour.js`.
- `missions.html` — **SẢNH NHIỆM VỤ / Trung Tâm Nhiệm Vụ (Mission Control)** (+ `css/missions.css`, nạp `css/page-shell.css`): mở từ card MOD-04 ở dashboard. Lưới thẻ nhiệm vụ dựng **giống `games.html`** (ready trước, soon sau, `code:"MISSION-nn"` cố định, `.play-btn.locked` cho nút "Sắp ra mắt") + bảng `.kv` tổng quan. `MISSION-01` Hành Tinh Xanh → `mission-earth.html`; `MISSION-02` Mặt Trăng *(sắp ra mắt)*.
  - **`key` của `MISSIONS` PHẢI khớp id nhiệm vụ ở `AstroqSV/Services/Missions.cs`** — đó là khoá dùng trong `missions.<key>` của DynamoDB. Đặt sai thì `GET /me/missions` không có khoá đó và trang **im lặng** hiện như thể người chơi chưa làm gì. Phép kiểm mục [7c] đối chiếu hai bên.
  - **Số bước lấy từ SERVER** (`missions.<key>.steps.length`), không gán cứng: số bước là luật của server. `steps:8` trong `MISSIONS` chỉ là số dự phòng lúc chưa gọi được API.
  - **Chưa đăng nhập / mất mạng → mọi con số hiện dấu `—`, KHÔNG hiện 0.** "0/8 bước" là một lời khẳng định SAI về tiến độ của người chơi. Cùng nguyên tắc với `achievements.html` và `specimen-vault.html`; có dải nhắc `.banner` nói rõ lý do. Nhãn nút cũng theo tiến độ THẬT: chưa đọc được thì nói "Bắt đầu nhiệm vụ" (câu đúng trong mọi trường hợp), đọc được rồi mới đổi sang "Tiếp tục" / "Chơi lại".
  - **KHÔNG trừ Thiên thạch tím**: nhiệm vụ là phần học, không phải mini-game — bảng phí ở `Services/Wallet.cs` cũng không có mục nào cho nhiệm vụ.
  - `AstroQProgress.missions()` (thêm 29/07/2026) là chỗ duy nhất gọi `GET /me/missions`. Trang này **được phép** nạp `js/firebase-auth.js` (cần token), đã thêm vào danh sách cho phép ở `check_pages.py` mục [4].
- `profile.html` — **Hồ sơ Phi Hành Gia** (+ `css/profile.css`): thẻ ID với ảnh 3D nhân vật trong **vòng cấp độ SVG** (chu vi 2πr, `stroke-dashoffset` theo %), thanh XP + "còn n XP nữa lên cấp k", **đổi tên** (≤24 ký tự) và **đổi trang phục** (roster 10 nhân vật từ `js/characters.js`, bấm là đổi ngay rồi mới gọi API — chờ mạng xong mới nhấp nháy thì cảm giác như bấm không ăn), 8 ô thống kê, **hành trình khám phá** 8 hành tinh, **kỷ lục** 3 game (ưu tiên số của server, thiếu thì lấy kỷ lục cũ trong máy để người chơi lâu rồi mới đăng nhập vẫn thấy).
- `achievements.html` — **Kho Thành Tích** (+ `css/achievements.css`): vòng donut % hoàn thành, chip 4 nhóm, **lọc 2 chiều** (trạng thái × nhóm), lưới huy hiệu — đã mở thì vàng ấm + ngày mở, chưa mở thì xám + thanh tiến độ "39 / 50"; **đã mở lên trước, trong đó cái gần đạt nhất lên đầu** để trẻ thấy ngay cái sắp mở được. Bộ sưu tập: hành tinh đã ghé (từ API) + chòm sao đã ghép (từ `astroq-constellation-best` trong máy — phần này chưa có API).
- `specimen-vault.html` — **Kho Mẫu Vật Vũ Trụ (Specimen Vault)** (+ `css/specimen-vault.css`): phòng bảo tàng của phi thuyền, 21 mẫu vật ngủ đông trong **khoang kính tím** chia 4 nhóm (🌊 Thuỷ quyển · 🌿 Sinh quyển · 🪨 Địa quyển · 🌌 Cổ vật vũ trụ), thanh tiến độ "đã thu thập n/21" + "mẫu vật hiếm n/6" (**client đọc `total`/`rareTotal` từ API, không gán cứng** — thêm mẫu ở server là trang tự đúng), **màn soi chi tiết kiểu kính hiển vi** (ống kính quét + 2 vòng xoay ngược chiều, nút Zoom 10X↔40X, lời linh vật Comet/Byte, fun fact + link nguồn NASA/NOAA), và **bàn điều khiển khoang lái** — chọn tối đa 3 mẫu để trưng. Mở từ nút "📦 Mở Kho Mẫu Vật" ở cột stat dashboard và từ `achievements.html`. **KHÔNG có đường ghi "đã thu thập mẫu vật"** — trạng thái mở khoá **suy ra từ bộ đếm tiến độ** y như huy hiệu, nên gọi API kiểu nào cũng không thêm được mẫu. Chưa đăng nhập / mất mạng → vẫn vẽ đủ khoang ở trạng thái khoá nhưng **các con số hiện dấu `—` chứ không hiện 0** (hiện "0/0" cho mẫu hiếm là bịa rằng kho không có mẫu hiếm nào).
- **`js/specimens.js` + `AstroqSV/Services/Specimens.cs` — PHÂN CÔNG GIỐNG HUY HIỆU:** server giữ **điều kiện mở khoá** (`metric`, `goal`), nhóm, độ hiếm, nơi lấy mẫu; `js/specimens.js` chỉ giữ **tên/phân loại/emoji/lời linh vật/fun fact song ngữ** + hàm `hint(metric, goal, lang)` ghép câu nhắc mở khoá. **Thêm mẫu vật:** thêm dòng vào `Specimens.All` rồi thêm khoá cùng id vào `js/specimens.js` — thiếu tên thì trang hiện chính id, không vỡ. `scratchpad/check_pages.py` mục [11] đối chiếu hai bên. ⚠️ **Điều kiện mở khoá phải gắn với việc app THẬT SỰ có** (ghé hành tinh ở explorer, đọc bài, làm Quiz, chơi game, ghép chòm sao, và từ 29/07/2026 là **một bước của Nhiệm Vụ 01** qua `mission:earth:<step>` — `ancient-lava-rock` mở ở bước `timeline`) — **đừng viết "Mở khoá tại Mission 02"**: nhiệm vụ ĐÓ chưa tồn tại nên mẫu sẽ khoá vĩnh viễn. Phép kiểm mục [11] canh chuyện này bằng danh sách `MISSIONS_LIVE` (nhiệm vụ đã có trang chơi) và đòi mỗi điều kiện `mission:` phải có câu nhắc riêng trong `HINT`. ⚠️ **Nội dung `fact` chưa qua rà soát chuyên môn** (viết định tính, URL đã kiểm 200 ngày 29/07/2026) — cần giáo viên rà lại trước khi phát hành.
- `js/constellations.js` — **4 chòm sao, tên song ngữ, CHỖ DUY NHẤT khai báo** (trước 29/07/2026 nằm trong `achievements.html`). `key` phải khớp `cons.key` trong mảng `SKY` của `game-constellation.html`, vì đó là khoá dùng ở cả `PROGRESS.consts` trên server, `astroq-constellation-best` trong máy, và điều kiện `const:<key>` của `Specimens.cs`. Dùng chung bởi `achievements.html` và `specimen-vault.html`.
- **`js/badges.js` + `AstroqSV/Services/Achievements.cs` — PHÂN CÔNG PHẢI GIỮ ĐÚNG:** server giữ **mốc + điều kiện + quyết huy hiệu đã mở** (`goal`, `metric`, `group`) và trả kèm `current`/`goal` cho client; `js/badges.js` chỉ giữ **tên/mô tả/emoji song ngữ**. Mốc để hai nơi thì sớm muộn lệch, mà client thì ai cũng sửa được bằng DevTools. **Thêm huy hiệu:** thêm dòng vào `Achievements.All` rồi thêm khoá cùng id vào `js/badges.js` — thiếu tên thì trang hiện chính id, không vỡ. `scratchpad/check_pages.py` mục [5] đối chiếu hai bên.
- **`js/progress.js` — CHỖ DUY NHẤT báo "đã làm gì" lên server** (`AstroQProgress.quiz/game/lesson/planet`). Ba điều bắt buộc: (1) client **không tính XP, không tự mở huy hiệu** — chỉ gửi việc đã làm, server quyết; (2) **không bao giờ ném lỗi, không chặn giao diện** — game đang chạy thì nộp kết quả không được làm treo bảng kết thúc lượt; (3) mất mạng / chưa đăng nhập thì **xếp hàng chờ** trong `astroq-progress-queue`, lần sau mở trang có phiên đăng nhập tự gửi nốt. **7 điểm sinh dữ liệu:** `quiz.html` (`showSummary`) · `learn.html` + `library.html` (lúc nhận thưởng đọc bài) · 3 game (trong `onFinishGame`) · `explorer.html` (`selectBody` → ghé hành tinh).
- ⚠️ **CỐ Ý KHÔNG nạp `js/firebase-auth.js` ở game / quiz / learn / library / explorer** — SDK Firebase 233 KB, mà những trang đó cần mượt (explorer đã kéo three.js). Việc được xếp hàng chờ rồi dashboard/profile/achievements gửi nốt. `scratchpad/check_pages.py` mục [4] canh không ai nạp lẫn.
- `js/characters.js` — **roster 10 nhân vật, CHỖ DUY NHẤT khai báo** (trước 29/07/2026 nằm trong `js/auth-flow.js`). Dùng chung bởi `select.html` và `profile.html`; phải nạp **trước** `js/auth-flow.js`.
- `js/planets.js` — 8 hành tinh, tên song ngữ + màu. `id` **phải khớp** `PLANET_DATA[].id` trong `explorer.html`, vì đó là chỗ ghi "đã ghé hành tinh nào".
- `css/page-shell.css` — **khung dùng chung của MỌI trang nội dung** (không phải game): nền, `.wrap`, header `.hub-top`, `.hero`, `.panel`/`.panel-tag`/`.panel-sub`, `.seg` (nút lọc), `.filters` (hàng nhãn + dãy `.seg`), `.banner` (dải nhắc khi đang xem số cũ trong máy), `.kv` (lưới thống kê), `.pbtn`, `.sr-only`, `.toast`. Nạp giữa `common.css` và `css/<trang>.css`. **6 trang dùng** (thêm `specimen-vault.html` ngày 29/07; `.filters` chuyển từ `css/achievements.css` sang đây cùng ngày vì trang thứ hai cần đúng hàng đó): `games.html`, `learn.html`, `library.html`, `profile.html`, `achievements.html` — mỗi `css/<trang>.css` chỉ còn phần RIÊNG, kèm lý do cho từng chỗ cố ý lệch khỏi khung. **Làm trang nội dung mới thì nạp file này, đừng copy khung.** Hai chi tiết đã sửa ở khung (29/07): `.hub-tag` phải có **`min-width:max-content`** — có `flex:1` + `overflow:hidden` thì kích cỡ tối thiểu của flex item về 0, trên màn 390px nhãn bị bóp còn `[ …` không đọc được gì; và `.kv` dùng `minmax(172px,1fr)` chứ không phải 148px (4 cột trên desktop thì nhãn dài bị cắt đuôi).
  - ⚠️ **Đổi tên khi chuyển `library.css`:** thẻ bài viết nổi bật ở đó từng mang class `.hero`, trùng với `.hero` (tiêu đề canh giữa) của khung chung → đã đổi thành **`.feat`**. Trùng tên thì khung chung áp `text-align:center` + padding vào một cái thẻ grid, lệch hẳn bố cục. Header của `learn.html`/`library.html` cũng đổi từ `.top` sang **`.hub-top`** cho khớp khung.
- **`js/onboard-tour.js` + `css/onboard-tour.css` — Comet dẫn tham quan Trung Tâm Điều Hướng (chạy MỘT LẦN mỗi tài khoản).** **VẪN 7 bước dù dashboard đã lên 6 card** (lý do ghi trong chính file đó): lời chào có tên người chơi → 🗺️ Bản đồ Thiên Hà → 📚 Tri Thức → 🎮 Khu Huấn Luyện → 🏆 Kho Thành Tích → 👨‍🚀 Hồ sơ Phi Hành Gia → "khởi động động cơ". Mỗi khu vực được chỉ tới bằng **`data-tour="<key>"` đặt trên HTML** (`map`/`learn`/`train`/`awards`/`profile`), không cắm selector trong JS — đổi bố cục trang thì chỉ dời thuộc tính. **Ba lớp tách vai trò:** `.tour-block` trong suốt chặn bấm vào trang dưới · `.tour-hole` là ô sáng và **chính nó làm tối cả trang bằng `box-shadow` toả 9999px** (không cần thêm lớp phủ, khỏi chồng hai lớp tối) · `.tour-bubble` box thoại. Box thoại chọn chỗ theo thứ tự **dưới → trên → bên phải → bên trái**; **bắt buộc phải có nhánh đặt sang BÊN** vì thẻ HUD cao ~375px cộng box ~245px là vượt màn hình 900px, thiếu nhánh đó thì box rơi vào giữa và **đè lên chính khu vực đang giới thiệu**. Nút VI/EN của trang **bị chặn trong lúc tour mở** (cố ý — bấm ra ngoài là mất mạch), nhưng đổi ngôn ngữ ở **tab khác** thì tour dịch theo ngay qua `AstroQTour.setLang`. Xem lại màn giới thiệu: Console gõ **`AstroQTour.reset()`** rồi F5.
- **`js/space-scene.js` — BỘ VẼ CẢNH KHÔNG GIAN dùng chung** (sao, Trái Đất, phi thuyền Luna). Tách ra 29/07/2026 từ `js/warp-screen.js` vì màn mở đầu Nhiệm Vụ 01 cần **đúng Trái Đất và đúng con tàu đó** — vẽ lần thứ hai là chắc chắn có ngày hai cảnh trông khác nhau, mà người chơi đi từ cảnh này sang cảnh kia chỉ trong vài giây. `AstroQSpace.create(canvas, opt)` → `{ resize, clear, stars, earth, ship, shipRatio }`. **Ở đây CHỈ có phần VẼ; nhịp phim thuộc về từng màn** (warp = "tăng tốc rồi dừng", mission-intro = "vòng vào quỹ đạo") — nhồi cả hai vào một file thì sửa màn này lại sợ hỏng màn kia. Ảnh Luna tải MỘT LẦN cho cả trang nên chuyển màn không nhấp nháy. Trái Đất vẽ bằng code (kho `img/` chưa có ảnh Trái Đất); **không dùng `shadowBlur`** ở bất cứ đâu.
- **`js/mission-intro.js` + `css/mission-intro.css` — MÀN MỞ ĐẦU NHIỆM VỤ 01 "HÀNH TINH XANH"** (chạy MỘT LẦN mỗi tài khoản, ngay sau màn loading Luna). Nhịp phim ~30s, bỏ qua được bất cứ lúc nào: `0–1,4s` khoang lái mờ dần → không gian sâu, Luna bay vào từ mép trái · `1,4–6,4s` **vòng vào QUỸ ĐẠO** Trái Đất, mũi tàu tiếp tuyến đường bay, rồi bay vòng chậm · `2,2s→` box thoại Comet **gõ từng chữ** · xong thì **pop-up HUD** `☄️ MISSION 01: HÀNH TINH XANH ☄️` + `[ NHẤN ĐỂ KÍCH HOẠT SỨ MỆNH ]`. Âm thanh động cơ rầm rì + bip radar dựng bằng WebAudio (không tải file), tôn trọng `astroq-sfx` dùng chung với mini-game. Box thoại neo ở **ĐÁY** màn hình vì Trái Đất chiếm góc trên-phải. Xem lại để thử: Console gõ **`AstroQMissionIntro.reset()`** rồi F5.
  - Nút `[ NHẤN ĐỂ KÍCH HOẠT SỨ MỆNH ]` **dẫn sang `mission-earth.html`** (qua `onActivate` trong `dashboard.html`). `markSeen()` **trả Promise và `activate()` chờ nó** trước khi gọi `onActivate`: điều hướng huỷ request đang bay, không chờ thì cờ `intro01Seen` không tới DynamoDB và lần sau đăng nhập ở máy khác là xem lại cả màn phim (cache trong máy che mất lỗi này).
  - **Hai lỗi hiển thị chỉ nhìn ẢNH CHỤP mới thấy, đã sửa:** tiêu đề pop-up dùng `display:flex` + `flex-wrap` nên trên màn 390px **ngôi sao ☄️ thứ hai rơi xuống một dòng riêng** (đổi sang chảy như chữ thường + `text-wrap:balance`); và nút "Tiếp tục" của box thoại **vẫn hiện sau khi pop-up mở** nên trẻ không biết bấm cái nào.
- **`mission-earth.html` + `css/mission-earth.css` + `js/earth3d.js` — NHIỆM VỤ 01 "HÀNH TINH XANH": 8 bước, MỘT trang, KHÔNG tải lại.** Mở từ nút `[ NHẤN ĐỂ KÍCH HOẠT SỨ MỆNH ]` của `js/mission-intro.js`. Chuyển giữa các bước bằng **`world.panTo()` (camera pan/zoom của Three.js)**, đúng yêu cầu không load lại trang. `js/earth3d.js` lo CẢNH 3D, `mission-earth.html` lo LUỒNG — mỗi bước là một object `{ id, enter(), pick(), tick(), outro(), afterReport() }` nên thêm/sửa một bước không đụng bước khác.
  - **8 bước** (29/07/2026 xen 3 bước giáo dục vào bản 5 bước cũ; `STEP_IDS` phải khớp ĐÚNG THỨ TỰ với `Missions.All` ở server, có phép kiểm mục [3c]): ① `scan` *Khởi động hệ thống quét* — Lưới Chẩn Đoán bọc ngoài, bàn tay hướng dẫn kéo → phóng to → chạm, 3 điểm tín hiệu nhấp nháy, đủ 3/3 thì lưới tan · ② `timeline` *Lịch sử 4,5 tỷ năm* — thanh 4 mốc (🌋 dung nham → 🌊 đại dương đầu tiên → 🦕 khủng long → 🌍 ngày nay), **đi đúng thứ tự**, mốc dung nham trao mẫu vật 🪨 *Nham Thạch Cổ Đại* · ③ `sun` *Tìm nguồn năng lượng* — cả hành tinh **chìm vào bóng tối trước**, trẻ xoay camera ra tìm Mặt Trời rồi chạm, Mặt Trời bùng cháy và hiện **ranh giới ngày/đêm** · ④ `energy` *Kích hoạt năng lượng sạch* — khói đen phủ khí quyển, kéo-thả ☀️🌬️🌊 vào 3 ống khói, **khói mỏng đi theo TỪNG nguồn** rồi tan hết · ⑤ `rotation` *Khôi phục tín hiệu vệ tinh* — kéo xoay CHÍNH hành tinh cho trạm phát sóng hướng về vệ tinh (dưới 20°) · ⑥ `life` *Thu thập mẫu sự sống* — 4 vùng 🌊🌳🐧🏔️, drone bay xuống quét bằng laser tím rồi bật Thẻ Thu Thập · ⑦ `eco` *Eco-Hero* — 7 thẻ hành động vào 2 rổ 🟢 NÊN LÀM (4 thẻ) / 🔴 KHÔNG NÊN LÀM (3 thẻ), xong thì server mở huy hiệu 🌱 *Chiến Binh Xanh* · ⑧ `core` *Giải mã hành tinh* — kéo-thả 3 viên ngọc vào 3 ô (☀️ nhiệt độ · 💧 nước · 🌬️ khí thở); xong thì Màng Khí Quyển bọc Trái Đất → màn tổng kết.
  - **Ba bảng kéo-thả dùng CHUNG `dragDrop(opt)` và `.me-board`**, không chép ba lần. Thuộc tính bắt buộc: thẻ kéo mang **`data-want`**, vùng thả mang **`data-zone`** — đặt tên khác (đã từng viết `data-bucket` cho rổ Eco-Hero) thì **mọi cú thả đều tính là SAI mà bảng vẫn trông đúng**, lỗi im lặng, chỉ smoke test bắt được. Luật thả duy nhất khác nhau là `canDrop`: ô ngọc/ống khói nhận **một** thẻ, rổ Eco-Hero nhận **nhiều**. **Thả sai KHÔNG trừ điểm** ở cả ba, chỉ khích lệ ("Gần đúng rồi, thử lại xem nào!"), và **thả ra chỗ trống thì IM LẶNG** — nhấc thẻ lên rồi đổi ý là chuyện bình thường, mắng lúc đó là mắng oan. Câu khích lệ đi qua `boardSay()` để box thoại **nhấc lên khỏi bảng**, không thì nó phủ kín đúng mấy cái rổ trẻ đang phải kéo thẻ vào.
  - **Đổi tông hành tinh theo mốc thời gian làm bằng CSS, không sửa shader.** Trái Đất là canvas WebGL nên CSS không sửa được màu lục địa/đại dương; `setEra()` chỉ gắn class `#stage.era-magma|era-ocean|era-dino` (`filter`) + lớp phủ `.me-era` (gradient bầu trời). Khói của bước `energy` cũng là lớp phủ `.me-smog` với biến động `--smog` (1 → 0). Hai lớp này nằm **NGOÀI `.me-ui`**: `.me-ui` có `z-index:10` và tự tạo stacking context, đặt vào trong thì `z-index:5/6` của chúng vẫn vượt lên che mất bảng mục tiêu. `prefers-reduced-motion` **giữ** phần đổi tông (đó là nội dung bài học "Trái Đất từng khác hẳn"), chỉ bỏ phần chuyển màu dần.
  - **PHẦN THƯỞNG VÀ HUY HIỆU DO SERVER QUYẾT HẲN.** Client chỉ gửi `{mission, step}` qua `AstroQProgress.missionStep()`; bảng luật ở `AstroqSV/Services/Missions.cs`. Trong `mission-earth.html` **không có một con số thưởng nào**. Huy hiệu 🌱 Chiến Binh Xanh chỉ hiện khi server báo trong `newBadges` — hook `afterReport()` chạy SAU `reportStep` đúng vì thế; chưa đăng nhập / mất mạng thì **không khoe huy hiệu** và màn tổng kết hiện `+0` chứ không bịa (có phép kiểm riêng). Tên/emoji huy hiệu lấy từ `js/badges.js` (trang này nạp nó, 7,2 KB) — không gõ lại.
  - **Nội dung đọc mở khoá theo bước nằm ở `learningdata/astronomy/earth_codex.json`**, khớp theo `Step.Codex` id của server (9 mẫu: 6 mẫu cũ + `earth-formation`, `clean-energy`, `eco-habits`). Chữ trên bảng và chữ trong bài đọc phải khớp nhau — trẻ đọc lại bài codex sau khi xong bước. Con số **66** (không phải 65) triệu năm là ranh giới cuối kỷ Phấn Trắng theo NPS Geologic Time Scale. ⚠️ Nội dung **chưa qua rà soát chuyên môn** (`reviewed_by_teacher: false` ở từng entry).
  - ⚠️ **Bản đồ là bản đồ THẬT** (`COASTS` trong `js/earth3d.js` — đường bờ thô của 14 khối lục địa/đảo, vẽ bằng code, không thêm file ảnh). Bản đầu sinh lục địa bằng nhiễu fBm: thẻ mẫu vật ghi "🌳 Rừng Amazon" ở đúng lat/lon thật mà chỗ đó lại là giữa đại dương — **dạy sai địa lý**. Sửa bản đồ thì phải nhớ 4 thẻ đó neo vào toạ độ thật.
  - ⚠️ **`window.__mission` là bề mặt điều khiển cho test** (step/done/reward/badges/busy/satAngle/eras/smog/sorted + pick/tick/fill/era/place/sort/say/world). Nó **gán TRƯỚC `await steps.scan.enter()`** vì bước 1 chờ trẻ bấm "Tiếp tục", gán sau thì không bao giờ tồn tại. Các hàm **cố ý không trả Promise** — `page.evaluate` của Playwright tự chờ promise được trả về, mà `pick()`/`era()` bên trong lại chờ cú bấm hoặc chờ thẻ mẫu vật → script test treo vĩnh viễn thay vì báo hỏng.
  - **Bộ kiểm thử:** `scratchpad/check_pages.py` mục **[3b]** (không tự quyết thưởng) + **[3c]** (8 bước khớp server · codex ↔ earth_codex.json · i18n vi/en khớp · đủ khoá i18n ghép động cho ERAS/ENERGY/ECO · mọi `$('id')` tồn tại · mọi class có CSS), và `scratchpad/smoke_mission_earth.py` **chơi thật cả 8 bước trên Chromium** (VI + EN + điện thoại 390×844 + reduced-motion + mất mạng). Chạy: `python -m http.server 8123` rồi `python scratchpad/smoke_mission_earth.py` — mất ~10 phút vì nó chơi hết nhiệm vụ 5 lần. ⚠️ Trên Windows phải đặt `PYTHONIOENCODING=utf-8`, không thì `print` chữ Việt là `UnicodeEncodeError` ngay dòng tiêu đề đầu tiên.
  - ⚠️ `pointer-events` là chuyện sống còn: `.me-ui` để `none`, chỉ `.hit` mới nhận chuột — lớp phủ nào chắn hết thì không kéo-xoay được quả cầu.
- **`js/sfx.js` — BỘ ÂM THANH WEBAUDIO dùng chung.** `AstroQSfx.beep/arp/ready/pickup/nope/ignite/fanfare/rumble/tone`. Một `AudioContext` cho cả trang, **một** lựa chọn tắt tiếng `localStorage["astroq-sfx"]` dùng chung với 3 mini-game và `js/mission-intro.js`. Không tải file âm thanh nào. Tách ra vì `mission-intro` đã dựng WebAudio rồi `mission-earth` lại cần — chép sang trang thứ hai là sớm muộn lệch nhau ở chỗ tôn trọng tắt tiếng. Trang nhiệm vụ có nút 🔊/🔇 riêng trên header.
- **`js/warp-screen.js` + `css/warp-screen.css` — màn loading "Luna tăng tốc vào không gian rồi dừng ở Trái Đất"** (canvas 2D, ~4,6s, có nút "Bỏ qua ›"). **Vệt sao toả ra TỪ CHÍNH TÂM TRÁI ĐẤT** (điểm tụ) chứ không trôi ngang — Luna đang lao về cái đích đó, nên phối cảnh đúng là sao chạy ra xa khỏi nó; nhờ vậy chỉ cần hạ một biến `speed` về 0 là ra cảm giác giảm tốc, không phải điều khiển từng ngôi sao. Thêm **lớp sao nền đứng yên** vì sao chạy luôn sinh lại sát điểm tụ, lúc dừng thì cả đám tụ quanh Trái Đất và nửa bên kia trời trống trơn (đo được 26 px sáng). **Trái Đất vẽ bằng code, không dùng ảnh** (kho `img/` chưa có ảnh Trái Đất; muốn thay art thật thì đặt đường dẫn vào `CONFIG.earthSprite` — mặc định `null` nên **không có lời gọi mạng, không có 404 trong console**): lục địa là **đường cong bậc hai đi qua trung điểm các đỉnh** trên ellipse rx≠ry, mây và mũ băng là **gradient toả mờ dần**. Bản đầu vẽ đa giác 12 cạnh thẳng + mây khối đặc → **ảnh chụp ra một quả bóng**, không phải hành tinh. Vành khí quyển sáng dùng gradient ở **mép trong** hình cầu, không phải nét stroke (một cung nét dày trông như cái vòng rời lơ lửng cạnh hành tinh). **Không dùng `shadowBlur`** — hào quang bằng gradient + lớp chồng `"lighter"`, như ARCADE-01/04. Nhân vật là `img/luna-side.png` (mũi chỉ sang phải), ảnh lỗi thì lùi về bản vẽ vector.
- `games.html` — **Sảnh Trò Chơi (Arcade Bay / Training Simulator)**: header (Về Trung Tâm Điều Hướng + số dư tt + đổi ngôn ngữ), lưới thẻ game HUD (dữ liệu `GAMES`), mỗi thẻ có badge phí "n/lượt" + độ khó. Mở từ nút "Chơi Game" ở dashboard. **3 game đã mở**: Né Thiên Thạch (`game-dodge.html`), Space Defender (`game-defender.html`), Ghép Chòm Sao (`game-constellation.html`); 3 thẻ còn lại "Sắp ra mắt". `launch()` chỉ điều hướng (phí trừ trong game). **Số hiệu `code:"ARCADE-nn"` ghi CỐ ĐỊNH trong dữ liệu** (không còn sinh theo vị trí sau khi sort) — trước đây mỗi lần một game "soon" thành "ready" là số hiệu cả dãy nhảy hết, trong khi số hiệu đã đi vào tài liệu và cách gọi của người dùng. Thứ tự hiển thị vẫn là ready trước, soon sau; thêm game mới thì cấp số kế tiếp, không sửa số cũ.
- `game-defender.html` — **mini-game canvas 2D "Space Defender" (ARCADE-02), phòng thủ 360°**: **Trạm Vũ Trụ cố định ở TÂM sân**, nòng pháo xoay 360° theo con trỏ (`Math.atan2`), giữ chuột trái/`Space` để bắn liên tục (◀▶ xoay nòng cho người dùng bàn phím). **Sân VUÔNG 600×600** (`CONFIG.VW/VH`) — sân chữ nhật thì hướng trái/phải xa tâm hơn hướng trên/dưới, cùng một cú bắn mà bên khó bên dễ. Vật thể **sinh trên vòng tròn r=440 quanh tâm** (lớn hơn nửa đường chéo sân nên không "hiện ra" giữa sân) rồi dồn thẳng vào tâm; vật thể còn ngoài khung được báo bằng **dấu nhắc tam giác ở mép sân** theo màu từng loại — phòng thủ 360° thì phải biết đòn đến từ hướng nào. 4 loại ở `TYPES`: **xám** 2 phát (có vết nứt khi còn 1 máu) · **rác đỏ** 1 phát nhưng bay nhanh nhất · **tím** +10 điểm & +1 tt vào ví tạm · **vàng** bay chậm, bắn trúng thì **mở câu đố** (`CONFIG.quizCooldown` 12s để câu đố không bật quá dày vì mỗi lần bật là dừng game). Đâm vào Trạm: **mất 10–20% giáp** tuỳ loại + chớp đỏ + screen-shake. Câu đố đúng → **+20% giáp & +5 tt**, sai → chơi tiếp (luôn chỉ ra đáp án đúng để còn học được). Giáp 0 → nổ 0,8s rồi hiện bảng kết quả (Điểm · Trụ được (giây) · Kỷ lục · tt đã thu). **Thưởng chốt một lần** `onFinishGame(meteors)` → `Economy.addAsteroids` — **bỏ phần thưởng `floor(điểm/25)` của bản cũ** để con số trên bảng kết quả đúng bằng con số vào ví. Kỷ lục ở `astroq-defender-best-v2`. **Nhân vật điều khiển = phi thuyền Luna (`img/luna2.png`)** qua `CONFIG.stationSprite`: ảnh nhìn từ trên xuống mũi chỉ LÊN, nên vẽ phải quay `aim - CONFIG.spriteUp` để mũi trùng hướng ngắm — **cả tàu quay 360°**, và **mũi tàu chính là nòng** nên khi có sprite thì KHÔNG vẽ nòng vector nữa (hai thứ sẽ chồng lên nhau); đạn sinh ở `muzzleDist()` = `shipH*0.48`. Ảnh lỗi → lùi `img/b1.png` → cuối cùng lùi về bản vẽ vector (nòng pháo + thân tròn) vẫn còn nguyên trong `else`. Vòng khiên quanh tàu là **cung sáng thể hiện % giáp** (xanh→cam→đỏ), bán kính `shieldR:31` **bám sát thân tàu** — vẽ rộng hơn thì vật thể lọt vào trong khiên mà chưa mất giáp, trông như lỗi (va chạm xảy ra khi mép vật thể chạm `r=stationR=26`). Hitbox là hình **tròn** nên không đổi khi tàu quay. Reticle "+" + `cursor:none`. Module `CONFIG`/`TYPES`/`QUIZ` (8 câu, vi+en)/`SFX`/`draw*`.
- `game-dodge.html` — **mini-game canvas 2D "Né Thiên Thạch" (ARCADE-01), thể loại side-scroller / flappy**: Byte đứng ở **1/4 bên trái**, nhấn `Space`/click/chạm để bay vút lên, thả ra thì rơi (giữ phím = rơi chậm). Cột chùm thiên thạch trôi phải→trái, chừa **khe hẹp** để lách; đâm cột **hoặc chạm mép trên/dưới** = hết lượt (nổ hạt + screen-shake, chờ 0,7s mới hiện bảng kết quả). Thiên thạch tím nằm trong khe: **+10 điểm & +1 tt vào ví tạm** (âm "ting"). **Điểm = quãng đường (m) + 10/viên.** Sân có **hệ toạ độ ảo cố định 800×500** (`CONFIG.VW/VH`) rồi scale ra màn hình → độ khó y hệt trên mọi máy. Nền parallax 3 lớp (gradient + dải ngân hà cuộn + 2 lớp sao). **Bảng HUD là một thanh RIÊNG phía trên sân, không đè lên vùng bay** (`.hud` ngoài `.play`): Điểm · Quãng đường · tt · Kỷ lục + **nút Pause & nút Âm thanh** (`P`/`M`, tuỳ chọn tắt tiếng lưu ở `astroq-sfx`; nút Pause tự ẩn ở màn brief/kết quả vì bấm vô nghĩa). Overlay chỉ phủ `.play` nên nút Âm thanh lúc nào cũng bấm được. Trừ **5 tt mỗi lượt** lúc bắt đầu/Chơi lại; thiếu tt → overlay dẫn sang Quiz. Kỷ lục lưu ở **`astroq-dodge-best-v2`** (khoá mới vì cách tính điểm khác hẳn bản né-rơi cũ; khoá `astroq-dodge-best` cũ thành mồ côi). Thưởng chốt **một lần** ở cuối lượt qua `onFinishGame(mined)` → `Economy.addAsteroids` (trong lượt tt chỉ ở ví tạm). Module `CONFIG`/`SFX`/`draw*`. **Nhân vật điều khiển = phi thuyền Luna nằm ngang (`img/luna-side.png`, 8,7 KB)** qua `CONFIG.spritePath`: ảnh cắt-thu-nén từ bản gốc **`img/luna ngang.png` (1,7 MB, 1934×1080)** — mũi đã chỉ **SANG PHẢI** tức đúng hướng bay, nên chỉ cần `rotate(byte.rot)` như cũ. Cỡ vẽ theo `CONFIG.spriteH` (30) + tỉ lệ ảnh 1,88 → **56,5×30**, và **hitbox đổi theo hình: `byteW/byteH` = 50×30** (bản robot cũ là 46×34) để hình vẽ bằng đúng vùng chết. Ảnh lỗi → tự lùi về bản vẽ robot vector trong nhánh `else`. **Vệt sáng tím ở đuôi (`drawTrail`)**: mỗi khung ghi 1 điểm ở miệng ống đẩy vào `trail`, điểm cũ **trôi trái đúng bằng `dx` của màn hình** nên vệt đứng yên trong không gian như luồng khí thật; vẽ 2 lớp polygon (hào quang tím + lõi tím trắng) với pháp tuyến vuông góc đường bay và gradient dọc theo vệt, `globalCompositeOperation="lighter"`, **không dùng `shadowBlur`** (blur mỗi khung rất đắt). Chết thì động cơ tắt: vệt cũ tan dần, không sinh điểm mới. Lửa đẩy lúc bấm cũng đổi sang tông tím cho khớp. Tôn trọng `prefers-reduced-motion` (bỏ rung, giảm hạt, bỏ lớp hào quang rộng). Nút: Về Sảnh (`games.html`) / Trung Tâm Điều Hướng (`dashboard.html`).
- `game-constellation.html` — **mini-game giải đố 2D "Ghép Chòm Sao" (ARCADE-04)**: nền trời đêm (gradient `#0b0f19`→`#1a103c` + 2 vệt tinh vân + **240 sao nhấp nháy**), các **ngôi sao mốc** phát hào quang kèm **nhãn số thứ tự**; nối 1→2→3→… bằng **bấm 2 lần hoặc kéo-thả** (chuột và cảm ứng, qua Pointer Events). Nối **đúng** → đường khoá cố định màu **neon cyan** + tiếng "ping" cao dần theo số đường đã nối; nối **sai** → đường **nhấp nháy đỏ 0,45s rồi tự huỷ** (không khoá), +1 vào bộ đếm "Nối sai" và toast chỉ đúng cặp sao cần nối. Ghép hết → cả chòm **bừng sáng** + hiện **nét phác thảo hình chòm sao** (dashed, vẽ dần), 1,35s sau **tự bật modal thông tin thiên văn** (`ov-fact`), bấm tiếp mới ra **bảng kết quả** (Thời gian · Kỷ lục · Nối sai · Thưởng). **Phí 3 tt/lượt** (DIFF: Dễ), **thưởng 8–15 tt tính theo công thức KHÔNG ngẫu nhiên**: `8 + nhanh(0–4) + ít sai(0–3)`, mốc "nhanh" là `(số sao −1) × 5s`; chip tt trên HUD hiện luôn **phần thưởng đang chờ** nên trẻ thấy nó tụt dần khi chậm/sai. Thưởng chốt **một lần ngay lúc ghép xong** qua `onFinishGame(reward)` (không đợi bấm nút — đóng tab ở modal thì công sức vẫn được trả). **Dữ liệu chòm sao = mảng `SKY`** (`stars:[{id,x,y}]` + `sketch` + `fact` song ngữ, toạ độ theo hệ ảo 800×500): hiện có **Đại Hùng, Thiên Hậu, Lạp Hộ, Bọ Cạp** với toạ độ theo hình sao thật và 1–2 câu thiên văn có thật; thêm chòm mới chỉ cần thêm 1 phần tử, **không sửa logic**. "Chòm sao khác" luôn ra chòm **khác lượt vừa rồi**. Kỷ lục thời gian theo từng chòm ở `astroq-constellation-best` (JSON). Gợi ý cho trẻ: vòng nhấp nháy ở sao kế tiếp + dòng nhắc "TIẾP THEO: NỐI a → b" + **đường mờ chỉ lối sau 9 giây bí**. Tôn trọng `prefers-reduced-motion` (nửa số sao nền, bỏ nhấp nháy, ít hạt).
- `library.html` — **Thư Viện Khám Phá (Space Knowledge Reader)**: mở từ thẻ "Thư Viện" ở learn.html. Topbar (Về khu Tri Thức + tìm kiếm + lọc nguồn Tất cả/NASA/ESA/AI&Tech + số dư tt). Bố cục: sidebar trái (danh mục Thiên văn/AI/Lượng tử/Robot + thống kê "Đã đọc/Đã thu thập") | feed giữa (hero card + lưới card hover glow). Reader modal: **ảnh HD thật từ NASA** (images-assets.nasa.gov, có fallback gradient+emoji), tóm tắt cho trẻ em, khung thoại mascot Comet/Byte giải thích thuật ngữ, credit "Nguồn dữ liệu: NASA/JPL-Caltech" + link, CTA "Làm Quiz bài này"→`quiz.html`. Đọc ~5s nhận ☄️ (lưu `astroq-read`, chung với learn). Dữ liệu = mảng `ARTICLES` (nội dung minh hoạ cho trẻ, ảnh + link nguồn thật).
- `codex.html` — **Sổ Tay Thuật Ngữ (Terminology Codex)** (+ `css/codex.css`, nạp `css/page-shell.css`): mở từ thẻ **MOD-C** ở `learn.html`. Lưới 15 thuật ngữ thiên văn, bộ lọc trạng thái, màn soi chi tiết (định nghĩa · ví dụ đời thường · sơ đồ 3 nhãn · nguồn NASA). **Nội dung ở `js/codex-terms.js`** — CHỖ DUY NHẤT khai báo, song ngữ đầy đủ.
  - **BA trạng thái thẻ, không phải hai:** `done` đã giải mã · `lock` chưa giải mã nhưng CÓ đường mở (nói cách mở) · **`soon` chưa có câu hỏi nào trong bank** → nói thật "sắp có" và **KHÔNG dẫn sang Quiz**. Hiện 5 thuật ngữ ở trạng thái `soon` (lỗ đen · hấp dẫn · tinh vân · siêu tân tinh · bức xạ nền). Đúng bài học `js/specimens.js`: đừng hứa một nhiệm vụ chưa tồn tại.
  - **Trạng thái do SERVER quyết**, client không có bản sao: `PROGRESS.terms` (string set các khoá `term` đã trả lời đúng) → `GET /me/achievements` → `progress.terms`. Chưa đăng nhập / mất mạng → **hiện MỌI thẻ ở trạng thái chưa giải mã kèm dải nhắc nói rõ lý do**, không bịa "đã giải mã".
  - **`q` là DÂY NỐI THẬT** với `js/quiz-questions.js`: mỗi phần tử phải là khoá `term` CÓ THẬT trong bank, sai một chữ là thuật ngữ khoá vĩnh viễn mà không ai thấy lỗi. Phép kiểm `check_pages.py` mục **[12]** đối chiếu hai bên, cộng `scratchpad/smoke_codex.py` đo trên Chromium.
  - **Thêm thuật ngữ:** thêm một phần tử vào `T` ở `js/codex-terms.js` (đủ `vi`+`en`, `src` nguồn NASA, `ic` khoá icon) rồi thêm bản vẽ `cx-<tên>` vào `js/icons.js`. Muốn thẻ giải mã được thì phải có câu hỏi tương ứng trong bank.
  - ⚠️ **4 thuật ngữ AI/Lượng tử CỐ Ý chưa có** — bản React để `reviewed:false`, không nguồn; thêm vào là phát hành nội dung chưa đối chiếu. Vì thế trang chưa có bộ lọc Phân loại.
- `learn.html` — **Tri Thức (Knowledge Station / Learning Hub)**: mở từ nút "Vào học & làm Quiz" ở dashboard. Header (Về Trung Tâm Điều Hướng + số dư tt + đổi ngôn ngữ), 2 thẻ chế độ (📖 Thư Viện Khám Phá → cuộn tới danh sách bài / ⚡ Thử Thách Quiz → `quiz.html`), lưới bài viết nổi bật (NASA/ESA/MIT, dữ liệu `ARTICLES`) mở trình đọc trong trang; đọc ~6s xong nhận ☄️ (một lần/bài, lưu `astroq-read`).
- `quiz.html` — **màn Chẩn đoán toàn màn hình** (Space Cockpit HUD): header (Rút lui + bình năng lượng + tiền tệ), câu hỏi + minh hoạ Byte né thiên thạch, 4 đáp án bảng điều khiển, mascot Byte + bong bóng thoại, popup giải thích, màn Hoàn thành nhiệm vụ (sao/độ chính xác/huy hiệu). Mở từ nút "Vào học & làm Quiz" ở dashboard. **Câu hỏi KHÔNG còn nằm trong trang** — xem `js/quiz-questions.js` ngay dưới.
- **`js/quiz-questions.js` — NGÂN HÀNG CÂU HỎI, CHỖ DUY NHẤT khai báo** (tách khỏi `quiz.html` ngày 30/07/2026). **35 câu** (từ 30/07/2026; trước là 25): 30 câu thiên văn theo NASA cho **15** thuật ngữ nền (Star · Planet · Dwarf Planet · Moon · Asteroid · Comet · Meteoroid · Meteor · Meteorite · Exoplanet — mỗi thuật ngữ 2 câu) + 5 câu lập trình/robot cũ. Một câu gồm `term` (khoá thuật ngữ) · `topic` · `q` · `opts[4]` · `a` · `ok`/`no` · `hint` · `src` (nguồn NASA, hiện ở cuối popup giải thích).
  - **MỖI LƯỢT RÚT `ROUND_SIZE` CÂU (mặc định 5), KHÔNG hỏi hết bank.** `pickRound(n)` **ưu tiên không trùng `term`** — bank có 2 câu mỗi thuật ngữ, không lọc thì một lượt 5 câu có thể hỏi Sao chổi hai lần trong khi bỏ qua 9 thuật ngữ khác. Nhờ vậy "Làm lại" ra đề khác chứ không lặp y nguyên, và **thêm câu vào bank không làm nhiệm vụ dài thêm**. Đổi `ROUND_SIZE` (trong `quiz.html`) thì nhớ trần thưởng của server: quiz **220 ☄️ ≈ 11 câu đúng** (`AstroqSV/Services/Wallet.cs`).
  - ⚠️ **`shuffled()` trộn một BẢN SAO.** Trộn tại chỗ thì thứ tự khai báo của `ALL` bị đổi vĩnh viễn và mọi phép kiểm dựa vào thứ tự sẽ hỏng một cách khó hiểu — có phép kiểm riêng canh chuyện này.
  - ⚠️ **Số liệu khoa học phải có `src` trỏ đúng trang NASA đã đối chiếu** (9 URL, kiểm trả 200 ngày 30/07/2026). Sửa con số mà không sửa nguồn là bịa. Năm câu lập trình cố ý **không có `src`** (câu khái niệm của bài học, không phải số liệu) — popup tự ẩn dòng nguồn.
  - ⚠️ **Đáp án đúng phải rải đều A/B/C/D**: trẻ học "cứ chọn B" thì bài kiểm tra mất tác dụng. Hiện A=8 · B=6 · C=6 · D=5; riêng 20 câu thiên văn đúng 5/5/5/5. Thêm câu thì đếm lại (`check_quiz_bank.py` mục [3] canh).
  - **Kiểm thử: `scratchpad/check_quiz_bank.py` 54/54 đạt, 0 lỗi console** — chạy trên Chromium thật vì dữ liệu là JS, cách duy nhất chứng minh nó đúng cấu trúc là để trình duyệt nạp rồi hỏi lại.
- `explorer.html` — bản đồ **3D Hệ Mặt Trời + Solar Neighborhood** (three.js qua CDN, ES module).
- `economy.js` — số dư **"Thiên thạch tím"**. ⚠️ **Từ 29/07/2026 localStorage chỉ còn là CACHE** — nguồn sự thật là ví trong DynamoDB (`SK=WALLET`). **PHÍ do server quyết hẳn**: `Economy.spend("dodge")` chỉ gửi TÊN GAME, server tra bảng `Wallet.Fees` rồi trừ (cho client gửi số tiền thì ai cũng chơi miễn phí bằng cách gửi 0). **THƯỞNG thì server đặt TRẦN** (`Wallet.Award`): số tt thu trong lượt phụ thuộc người chơi bay qua đâu, game chạy ở máy người dùng nên không tự tính lại được — nhận con số client báo rồi kẹp xuống trần theo từng loại việc. `addAsteroids()` vẫn cộng LẠC QUAN vào cache cho giao diện phản hồi ngay, rồi `setFromServer()` **ghi đè** bằng số thật → không bao giờ cộng hai lần. Hàm đọc/ghi cache vẫn **đồng bộ** để 3 game không phải đổi logic chặn phí.
- `js/auth-flow.js` — roster nhân vật + chọn + lưu hồ sơ + chuyển hướng.
- `img/` (2D), `3d/` (ảnh 3D nhân vật), `ava/` (avatar tròn).

## 3. Luồng người dùng
- Trang chủ `index.html` là landing **"Sắp ra mắt"** (chỉ có form waitlist, không dẫn vào app). Luồng app bên dưới bắt đầu từ `landing-app.html` — hiện chỉ vào được bằng URL trực tiếp.
- **Khách mới**: `landing-app` → "Trải nghiệm ngay" → popup **Đăng ký** → `select.html` (điền tên + chọn nhân vật → "BẮT ĐẦU HÀNH TRÌNH" → đóng dấu APPROVED) → `dashboard.html` → **Comet dẫn tham quan tàu (7 bước)** → **màn loading Luna bay tới Trái Đất** → **màn mở đầu Nhiệm Vụ 01 "Hành Tinh Xanh"** (Luna vòng vào quỹ đạo + Comet giao nhiệm vụ + pop-up kích hoạt) → bấm **`[ NHẤN ĐỂ KÍCH HOẠT SỨ MỆNH ]`** → **`mission-earth.html`** (8 bước, xong thì màn tổng kết → Kho Thành Tích / Trung Tâm Điều Hướng) → dùng dashboard bình thường. Bỏ qua giữa tour cũng đi qua màn loading (đó là lúc tàu rời bến, không phải thưởng cho việc xem hết lời giới thiệu). Chỉ chạy **một lần mỗi tài khoản** — cờ `tourSeen` ở DynamoDB, xem mục 4.
- **Người cũ**: trong popup bấm link **"Đăng nhập"** → `dashboard.html` (dùng lại avatar/nhân vật đã lưu).
- Đã có hồ sơ trong máy → bấm "Trải nghiệm ngay" vào thẳng `dashboard.html`.
- `dashboard` → card "Bản Đồ Thiên Hà" → `explorer.html`. Trong explorer nút **"Quay lại"** → `dashboard.html`.
- `dashboard` → card "Tri Thức" (nút "Vào học & làm Quiz") → `learn.html` (Learning Hub). Trong learn: thẻ "Thử Thách Quiz" → `quiz.html`; thẻ "Thư Viện" → `library.html` (Thư Viện Khám Phá, đọc bài + reader modal → CTA "Làm Quiz bài này" → `quiz.html`; nút "Về khu Tri Thức" → `learn.html`); nút **"Về Trung Tâm Điều Hướng"** → `dashboard.html`. Trong quiz nút **"Rút lui"/"Về Trung Tâm Điều Hướng"** → `dashboard.html`.
- `dashboard` → card "Khu Huấn Luyện" (nút "Chơi Game") → `games.html` (Sảnh Trò Chơi) → thẻ game → `game-dodge.html` / `game-defender.html` / `game-constellation.html`. Nút **"Về Sảnh"/"Về Trung Tâm Điều Hướng"** quay lại. Phí Thiên thạch tím trừ **mỗi lượt trong game** (không trừ ở dashboard/hub).
- `dashboard` → **chip avatar ở header** → `profile.html` (Hồ sơ Phi Hành Gia) · **nút "🏆 Mở Kho Thành Tích"** → `achievements.html`. Hai trang đó liên kết chéo với nhau và về `games.html` / `learn.html`; nút **"Về Trung Tâm Điều Hướng"** → `dashboard.html`.

## 4. localStorage — ĐANG CHUYỂN DẦN SANG DB
> ⚠️ Theo quy tắc 1–2 ở mục 6, những khoá dưới đây **phải chuyển sang API + DynamoDB**. Hiện mới chỉ có *danh tính* đi qua backend (đăng ký) và Firebase (đăng nhập); số dư và tiến độ vẫn nằm trong máy từng người — mỗi máy một kết quả, và mở DevTools là sửa được. Khi chuyển: **server quyết thưởng, client không gửi số dư lên**.

- `astroq-user` = `{ name, pilotName, character, selectedCharacter, avatar, email, purpleAsteroids }`
- `astroq-asteroids` = **CACHE** số dư Thiên thạch tím. Nguồn sự thật là `PK=USER#<uid>, SK=WALLET` trong DynamoDB; mọi lời gọi `/me/*` trả kèm `wallet.meteors` và `js/progress.js` đẩy thẳng vào `Economy.setFromServer()`. Pilot mới bắt đầu **0**. ⚠️ `Economy.DEFAULT_BALANCE` **phải là 0** cho khớp ví server (`DynamoContext` tạo ví `meteors = 0`) — trước 30/07/2026 nó là 50 và đó là một **số dư ảo**: máy sạch thấy 50 tt, chơi được một lượt, rồi lời gọi `/me/*` đầu tiên kéo về 0 trong khi server từ chối trừ phí (409). Phép kiểm `check_pages.py` mục [3d] canh hai bên.
- **NGUỒN THU THIÊN THẠCH TÍM (chốt 30/07/2026) — chỉ có hai:** ① **làm Quiz ĐẠT** (≥ **60%**, tức 3/5 câu với `ROUND_SIZE = 5`) · ② **thu trong mini-game**. **Đọc bài KHÔNG thưởng tt** (vẫn ghi `lessonsRead` + huy hiệu đọc sách). Ghé hành tinh trên thực tế cũng 0 tt vì `AstroQProgress.planet()` không gửi `meteors` — chưa chốt, xem ghi chú ở `Wallet.MaxPerPlanet`. Luật nằm ở **server** (`Wallet.MaxPerLesson = 0`, `Wallet.QuizPassRatio`, `Wallet.AwardQuiz`); `PASS_PCT` trong `quiz.html` chỉ là bản sao để vẽ giao diện và có phép kiểm đối chiếu hai bên.
- `astroq-constellation-best` = **CACHE** kỷ lục chòm sao (`{ <id chòm>: giây }`, id là `cons.key` trong mảng `SKY`: `ursa-major`/`cassiopeia`/`orion`/`scorpius`). Nguồn sự thật là `PROGRESS.consts`. ⚠️ **Khoá là id, KHÔNG phải tên tiếng Việt** — bản đầu của `achievements.html` dùng tên tiếng Việt nên bộ sưu tập luôn 0/4.
- `astroq-sfx` = `"off"` khi người dùng tắt tiếng. **MỘT khoá cho cả app** — 3 mini-game, `js/mission-intro.js` và `mission-earth.html` (qua `js/sfx.js`) đều đọc đúng khoá này.
- **Nhiệm vụ KHÔNG có bản sao trong máy.** Bước nào đã xong, thưởng bao nhiêu đều nằm ở `missions` trên bản ghi `SK=PROGRESS` trong DynamoDB. Cố ý: đoán ở client thì lúc mất mạng sẽ hiện phần thưởng chưa có thật.
- `astroq-lang` = `"vi" | "en"`
- `astroq-progress` = **CACHE** bộ đếm tiến độ (`quizCorrect`, `gamesPlayed`, `bests`, `planets`…) để `profile.html` vẽ được khi mất mạng. Nguồn sự thật là bản ghi `PK=USER#<uid>, SK=PROGRESS` trong DynamoDB. ⚠️ Cache **không chứa XP/cấp độ/huy hiệu** — những thứ đó do server tính, không có server thì trang hiện dải nhắc chứ không đoán.
- `astroq-progress-queue` = việc chưa gửi được lên server (mất mạng / chưa đăng nhập), tối đa 40 việc, tự gửi lại khi mở trang có phiên đăng nhập. Xem `js/progress.js`.
- `astroq-tour-seen` = `"1"` khi đã xem Comet dẫn tham quan.
- **BA cờ onboarding, ĐỘC LẬP nhau, nguồn sự thật ở DynamoDB** (`SK=PROFILE`, đọc/ghi qua `GET|PUT /me/onboarding`): `tourSeen` · `intro01Seen` · **`earth1Greeted`** (Comet chúc mừng xong chuỗi Trái Đất — thêm 30/07/2026). `PUT` **chỉ ghi cờ nào được gửi lên**; gộp chung thì xem màn này sẽ xoá dấu màn kia. ⚠️ Điều kiện "body rỗng = `tourSeen` true" (giữ cho client cũ) phải **loại trừ cả ba cờ** — thiếu thì gọi `{earth1Greeted:true}` sẽ bật luôn `tourSeen`.
- `astroq-mission01-intro-seen` = `"1"` khi đã xem màn mở đầu Nhiệm Vụ 01. ⚠️ **Chỉ là CACHE** — nguồn sự thật là `intro01Seen` trên bản ghi PROFILE, cùng route `GET|PUT /me/onboarding` với `tourSeen`. **Hai cờ ĐỘC LẬP:** `PUT` chỉ ghi cờ nào được gửi lên; gộp chung thì xem cutscene nhiệm vụ sẽ xoá dấu "đã xem tour" và Comet dẫn lại từ đầu. ⚠️ **Chỉ là CACHE** — nguồn sự thật là thuộc tính `tourSeen` trên bản ghi `PK=USER#<uid>, SK=PROFILE` trong DynamoDB, đọc/ghi qua **`GET|PUT /me/onboarding`** (`AstroQAuth.getOnboarding/setOnboarding`). Cache nói "đã xem" → bỏ qua ngay không chờ mạng; cache rỗng → hỏi server (hạn chờ 1,8s) để **người đã xem ở máy khác không phải xem lại**; server không trả lời được (chưa đăng nhập / mất mạng) → **vẫn dẫn tham quan**, vì thà chào hai lần còn hơn không chào lần nào.
- Mapping nhân vật (3d ↔ ava) ở mảng `CHARACTERS` trong `js/auth-flow.js`. **role/trait/stats đang là placeholder — chờ cập nhật.**

## 5. GIT & DEPLOY  ⚠️ (đã chốt — KHÔNG hỏi lại)
- **Remote**: `https://github.com/meowoi/AstroQhtml.git`
- **Repo gốc = thư mục `AstroQhtml/`**. Nhánh chính: **`main`**.
- **Deploy**: GitHub Pages từ `main`, custom domain **astroq.org** (file `CNAME`). Sau push, Pages tự build ~1–2 phút.
- **Khi người dùng nói "push / đẩy lên git"** → chạy ngay (trong `AstroQhtml/`), không hỏi URL:
  ```bash
  cd AstroQhtml
  git add -A
  git commit -m "<mô tả ngắn>" -m "<chi tiết>" -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
  git push origin main
  ```
- Push thẳng `main` là ĐÚNG (Pages deploy từ main). Cảnh báo LF→CRLF vô hại. `gh` CLI không có trên máy.

## 6. Quy ước làm việc

> **Quy tắc bắt buộc (2026-07-27) — cách làm việc:**
> 1. **Tính năng nào cũng làm CẢ hai phía**: client **và** backend. Không dừng lại ở client.
> 2. **Không lưu localStorage nữa** — viết API và lưu vào DB. `localStorage` chỉ còn là cache / dự phòng khi mất mạng, không phải nguồn sự thật.
> 3. **Tên bảng DB luôn bắt đầu bằng tiền tố `Astroq`** để phân biệt với bảng của dự án khác trong cùng tài khoản AWS. *(Bảng đang chạy `astroq-main` đã có tiền tố; DynamoDB không đổi tên tại chỗ được — muốn đúng chữ hoa phải tạo bảng mới rồi chuyển dữ liệu.)*
> 4. **Test API độc lập cho đạt hết rồi mới tích hợp vào giao diện** — kể cả các nhánh hỏng (sai token, hết hạn, trùng email, dữ liệu vào không hợp lệ). Nối API chưa kiểm vào client thì lúc hỏng không biết lỗi ở server, mạng hay JS.
> 5. **Tự triển khai, tự kiểm thử, tự đọc CloudWatch, tự dọn dữ liệu test.** Người dùng **không đọc code** nên không nhờ chạy lệnh hay xác nhận kết quả kỹ thuật. Báo lại bằng số liệu thật (bao nhiêu đạt / bao nhiêu hỏng).

1. **Mỗi lần code: ghi requirement** vào mục "Nhật ký yêu cầu" bên dưới (ngày · yêu cầu tóm tắt · file ảnh hưởng).
2. Giữ vanilla JS, không thêm dependency/build. Hỏi tên file ảnh nếu chưa rõ ánh xạ nhân vật.
3. Không sửa được pixel bên trong ảnh PNG (đổi màu vùng cụ thể) → báo người dùng chỉnh ảnh, hoặc dùng CSS filter cho cả ảnh.
4. Thêm text cho trang song ngữ → thêm khóa vào **cả** từ điển `vi` và `en`.

## 7. Nhật ký yêu cầu (Requirement Log)
> Ghi mới nhất lên trên.

### 2026-07-31
- **NÚT ĐỔI NGÔN NGỮ CHO `explorer.html` + `<html lang>` ĐỘNG + 3 PHÉP KIỂM CANH QUY ƯỚC.** Kiểm thử: `check_pages` **341/341** (thêm mục [14] và 3 phép kiểm ở [7]) · `scratchpad/smoke_lang_switch.py` MỚI **181/181** (17 trang × 9 phép đo trên Chromium) · `audit_viewports` 684/684 · `smoke_missions` 74/74 · `smoke_profile_pages` 125/125 · `smoke_onboard` 100/100 · `smoke_vault` 83/83 · `smoke_codex` 37/37 · `smoke_earth_done` 33/33 · `smoke_mission_intro` 57/57 · `regress_auth_pages` 19/19.
  - ⚠️ **`explorer.html` KHÔNG CÓ NÚT ĐỔI NGÔN NGỮ SUỐT NHIỀU THÁNG, VÀ KHÔNG GÌ BÁO LỖI.** Nó có đủ 3 rule CSS `.lang-btn`, có đủ lệnh `initLang(applyLanguage, '.lang-btn')` — **chỉ MARKUP là không có phần tử nào**. Bài học: *CSS có rule và JS có lệnh đều KHÔNG chứng minh được rằng người dùng bấm được.* Gốc bệnh là **trang tự đặt tên riêng** thay vì dùng `.lang-switch` chung, nên nó trượt khỏi mọi thứ canh quy ước.
  - **Chỗ đặt: hàng trên của `.deck-head`, logo trái — VI/EN phải** (khớp 15 trang còn lại). Hàng đó nằm **ngoài `.deck-scroll`** nên không cuộn mất. **Cố ý KHÔNG dựng khối `position:fixed` mới**: góc trên-phải đã bị `#info` (bảng thông tin rộng 400px) chiếm, hai thứ sẽ đè nhau khi mở bảng.
  - ⚠️ **`mission-earth.html` LÀ CA THỨ HAI, do chính phép kiểm mới bắt được.** Nó dùng tên riêng `.me-lang` và **lặp lại y nguyên 4 thuộc tính khung** của `.lang-switch`. Cái giá đo được rất cụ thể: lượt sửa vùng chạm 44px trước đó **đã phải viết THÊM một rule riêng cho nó**, vì rule dùng chung không với tới cái tên khác. Nay đã gộp về `.lang-switch` và **xoá được rule 44px trùng lặp đó**.
  - **`<html lang>` GIỜ ĐỔI THEO NGÔN NGỮ, sửa ở MỘT chỗ dùng chung.** Trước đó **không trang nào** làm việc này — `explorer.html` ghi cứng `lang="en"` rồi hiển thị toàn bộ tiếng Việt. Sai `lang` không phải chuyện hình thức: trình đọc màn hình chọn giọng và quy tắc phát âm theo nó. Đặt trong `initLang` (`js/ui-common.js`) chứ không sửa 16 trang — gọi một lần theo ngôn ngữ đang lưu (vì phần lớn trang gọi `applyLang(LANG)` trực tiếp), rồi bọc `applyLang` để mọi lần đổi sau cũng cập nhật. Hàm `setDocLang` cũng được export.
  - ⚠️ **LỖI THẬT CÓ TỪ TRƯỚC, phát hiện nhờ đo `elementFromPoint`: màn chờ nạp của explorer NUỐT MỌI CÚ BẤM trong ~0,8s.** `#loader` có `transition:opacity .8s, visibility .8s`, nên ở mốc 1600ms nó đã mờ tới `opacity:0,02` mà `visibility` vẫn `visible` → `elementFromPoint` giữa nút EN trả về `div#loader.hidden`; tới 3000ms mới trả về đúng cái nút. Trang trông đã sẵn sàng nhưng bấm không ăn, mà người dùng chỉ tưởng mình bấm trượt. Sửa bằng `pointer-events:none` trên `#loader.hidden`.
  - ⚠️ **SÓT CỦA LƯỢT ĐỔI TÊN, VÀ SÓT VÌ `grep -i` KHÔNG CASE-FOLD ĐƯỢC DẤU TIẾNG VIỆT.** `learn.html` còn chữ TĨNH `TRI THỨC & DỮ LIỆU VŨ TRỤ` trong markup (JS ghi đè nên đo trên trình duyệt không thấy). Hai lần quét đầu của tôi đều báo "không còn chỗ nào": lần một vì tôi tìm `Tri Thức` viết hoa-thường lẫn, lần hai vì **`grep -i` không fold được `Ứ` ↔ `ứ`**. Phải quét bằng `casefold()` của Python. **Trước đó `check_pages.py` KHÔNG CÓ phép kiểm nào cho tên khu Tri Thức** — đó mới là lý do gốc; nay đã thêm.
  - **BA PHÉP KIỂM MỚI, đều sinh ra từ lỗi thật:**
    1. *[14] trang nào gọi `initLang` thì phải có markup `data-lang` đủ `vi`+`en`* — bắt đúng ca explorer.
    2. *[14] không trang nào truyền selector RIÊNG cho `initLang`* — bắt ca mission-earth; đó là đường quay lại đúng cái bẫy trên.
    3. *[14] không trang nào nạp script từ tên miền ngoài.* **Không đòi 0 ngay**: nó GHIM danh sách hiện tại (`explorer.html` + `mission-earth.html`, cả hai kéo three.js từ unpkg) để thêm trang thứ ba là biết ngay, và tự thắt lại khi bỏ dần three.js.
    4. *[7] không còn "Tri Thức" trơn* — so bằng `casefold()`, có loại trừ `Trạm ` và `Ngân Hà `.
  - ⚠️ **MỘT PHÉP KIỂM TÔI ĐO RỒI QUYẾT ĐỊNH KHÔNG THÊM:** "chữ tĩnh trong markup phải khớp từ điển `vi`". Đo được **14/256 chỗ lệch, nhưng 13 là CỐ Ý** — markup giữ nhãn HUD kiểu sci-fi (`METEOR`, `XP`, `O₂`, `PILOT ID`, `FLIGHT LOG`) rồi từ điển VI mới dịch ra. Thêm vào là một phép kiểm báo oan 13 lần, đúng cái bẫy *"phép kiểm hay báo oan thì người ta sẽ bỏ qua nó"* đã ghi ở đây. **Đo trước rồi hãy thêm phép kiểm.**
  - **`scratchpad/smoke_lang_switch.py` — vì sao cần dù đã có mục [14]:** mục [14] soi văn bản nên không chứng minh được 4 điều người dùng thật sự trải nghiệm — nút **nhìn thấy và bấm được** (không bị lớp khác phủ), bấm thì **chữ đổi thật**, lựa chọn **được lưu**, và `<html lang>` khớp. Chính phép đo "không bị phủ" mới tìm ra lỗi `#loader`. Bộ này còn đo **đồng bộ giữa hai tab**: đổi ở `learn.html` thì `explorer.html` dịch theo.
  - ⚠️ **HAI LỖI TRONG CÁCH ĐO CỦA CHÍNH TÔI, đáng ghi vì cả hai đều làm phép đo thành vô nghĩa:** (a) `grep -E "HONG|KET QUA"` để tìm dòng hỏng **khớp cả chữ "KHONG"** trong nhãn `[OK]`, nên lượt thử phá hoại đầu tiên in ra toàn dòng `[OK]` và trông như "phép kiểm không bắt được gì" — phải dùng `^\s+\[HONG\]`. (b) Viết script chứa regex qua **heredoc của shell thì dấu `\` bị ăn mất** (`[^"\\]` → `[^"\]` → *unterminated character set*); phải tạo file bằng công cụ ghi file, đừng qua shell.
  - **Chứng minh 4 phép kiểm mới có răng:** bỏ markup nút VI/EN của explorer → hỏng · trả `learn.html` về chữ HOA tên cũ → hỏng · cho mission-earth truyền lại selector riêng → hỏng · cho `quiz.html` nạp script từ `cdn.jsdelivr.net` → hỏng **2 phép kiểm**. Khôi phục → 341/341.
  - ⚠️ **CÒN TREO: bỏ three.js ở `mission-earth.html` (bậc 2b–5).** Việc còn lại nhỏ hơn nhật ký cũ ghi: **13 class CSS `e2-*`** (hiện `css/mission-earth.css` có 0), và bộ test **chỉ có 2 chỗ** dùng `readPixels` chứ không phải 141 phép kiểm phải viết lại — `world.screenOf` thì `js/earth2d.js` đã cài sẵn nên 3 chỗ dùng nó chạy nguyên. Lợi ích đo được: three.js **257 KB gzip** + `earth3d.js` 51 KB → `earth2d.js` **19,7 KB** + ảnh quả cầu 51 KB, giảm ~77% đường tải đầu, bỏ một tên miền ngoài khỏi luồng onboarding BẮT BUỘC. **Cách làm ít rủi ro nhất: cho hai engine sống song song một vòng bằng cờ URL `?scene=2d`** (đúng lối `js/api.js` đã dùng `?api=prod|local`) để chạy **cùng bộ 141 phép kiểm lên cả hai engine**; chỉ khi 2D đạt 141/141 mới lật mặc định rồi xoá. ⚠️ **`explorer.html` cũng dùng three.js**, nên bỏ ở mission-earth KHÔNG loại được three.js khỏi dự án. — `explorer.html`, `css/explorer.css`, `mission-earth.html`, `css/mission-earth.css`, `learn.html`, `js/ui-common.js`, `scratchpad/{check_pages.py,smoke_lang_switch.py}`
- **LOGO THƯƠNG HIỆU: thay ô vuông gradient hổ phách bằng wordmark thật `img/astroq-logo.png`, dùng ở 5 chỗ.** Bản gốc người dùng đặt vào là `img/AstroQ logo1.png` (2162×1080, 467 KB); asset dùng thật do `scratchpad/make_logo_asset.py` sinh: **392×114, 7,0 KB (−98%)**. Kiểm thử: `check_pages` **332/332** · `audit_viewports` **684/684** · `regress_auth_pages` 19/19 · `smoke_onboard` 100/100 · `smoke_missions` 74/74 · `smoke_profile_pages` 125/125.
  - **Ô VUÔNG CŨ BỊ CHÉP LÀM 5 BẢN Ở 5 FILE CSS** (`index`/`select`/`landing-app`/`dashboard`/`explorer`) — đổi màu thương hiệu là phải sửa 5 chỗ, đúng thứ quy tắc 2 mục 6 cấm. Nay chỉ còn **`.brand-logo` ở `css/common.css`**, chiều cao truyền qua biến `--logo-h` nên mỗi trang khai đúng một con số. Đã xoá cả 5 rule cũ và các rule chữ đi kèm (`.brand b`, `.auth-brand strong`, `.ship-id b`) — để lại là CSS chết.
  - ⚠️ **LOGO LÀ WORDMARK CÓ SẴN CHỮ "astroQ", nên nó thay CẢ CỤM `[ô vuông] + "AstroQ"`,** không phải chỉ thay ô vuông. Chỉ thay ô vuông thì trang đọc ra **"astroQ AstroQ"**. Vì thế `select`/`landing-app`/`dashboard` bỏ luôn chữ bên cạnh; riêng **trang chủ giữ ".org"** vì đó là tên miền chứ không phải tên thương hiệu, và `alt="astroQ"` + chữ ".org" ghép lại mới thành đúng tên trang cho trình đọc màn hình (đặt `alt="astroQ.org"` là nó đọc "astroQ.org .org").
  - ⚠️ **EXPLORER PHẢI ĐẶT LOGO Ở HÀNG RIÊNG, khác 4 trang còn lại.** Bảng bên trái chỉ rộng 296px sau padding; wordmark tỉ lệ **3,44:1** nên cao 38px là rộng 131px, cộng nút gập 44px thì tiêu đề chỉ còn 121px trong khi riêng chữ "Solar System" cỡ 20px đã cần ~125px → tiêu đề xuống 3 dòng. Đây là hệ quả trực tiếp của việc đổi từ hình VUÔNG sang hình NGANG: mọi chỗ đặt logo cạnh chữ đều phải tính lại bề rộng.
  - ⚠️ **LỆCH ĐƯỜNG CHÂN CHỮ 2px, CHỈ ĐO PIXEL MỚI THẤY.** Wordmark là ẢNH nên CSS không biết đường chân chữ của nó ở đâu; `align-items:center` canh giữa cái KHỐI, không canh chân chữ. Đo trên ảnh chụp DPR3: đáy dấu `.` của ".org" cao hơn đáy chữ `a` của wordmark **2px** → chữ ".org" trông như bị nhấc lên. Bù bằng `.brand-name{position:relative;top:2px}`, đo lại còn **0,0px**. Cũng vì thế `.brand` hạ `gap` từ 10px (dành cho ô vuông đứng cạnh chữ) xuống **2px** để ".org" dính sát wordmark thành một tên miền liền mạch.
  - ⚠️ **PNG 256 MÀU THẮNG CẢ AVIF LẪN WEBP — đo ở CỠ HIỂN THỊ THẬT, không đo ở cỡ file.** So với bản gốc 1736×505 hạ thẳng xuống 117×34: PNG 256 màu **6,4 KB / lệch TB 0,52-255**; WebP q80 14,2 KB / 0,52 (nặng gấp 2,2× mà không nét hơn); AVIF q72 8,7 KB / **0,63** (vừa nặng hơn vừa xấu hơn); PNG RGBA 29,7 KB / 0,26. Nên **một file PNG cho mọi nơi, không cần `<picture>`** — cùng kết luận đã có với `img/luna-side.png`.
  - ⚠️ **ĐỪNG DÙNG `convert("P", palette=ADAPTIVE)` ĐỂ NÉN ẢNH CÓ ALPHA** — nhánh đó làm phẳng alpha thành trong-suốt-nhị-phân, tức chặt hết viền mềm và vầng sáng quanh chữ Q. Phải dùng `quantize(method=FASTOCTREE)`; đo được nó giữ **136 mức alpha trung gian** trên 254. Lần đầu tôi đo bằng `ADAPTIVE` và con số 6,4 KB đó **không dùng được** để so sánh.
  - ⚠️ **68% khung ảnh gốc là khoảng trong suốt** (2162×1080 nhưng nội dung thật 1736×505). Không cắt viền thì logo hiển thị nhỏ hơn hẳn khung của nó và mọi phép canh lề đều lệch.
  - **Bản gốc 467 KB KHÔNG commit** (`.gitignore` chặn `img/AstroQ logo1.png`), như mọi ảnh gốc khác của dự án. Muốn sinh lại asset thì chạy `scratchpad/make_logo_asset.py`.
  - `wiki/` **cố ý chưa đổi** — 22 trang đó sinh từ `scratchpad/gen_wiki_data*.py`, sửa tay là lần sinh sau mất; `.brand-mark` trong `css/wiki.css` vì thế vẫn giữ. — `img/astroq-logo.png`, `index.html`, `select.html`, `landing-app.html`, `dashboard.html`, `explorer.html`, `css/{common,index,select,landing-app,dashboard,explorer}.css`, `.gitignore`, `scratchpad/make_logo_asset.py`
- **BỐN VIỆC: đổi tên "Trạm Tri Thức" · ngày ra mắt 09/08/2026 · pop-up khuyến nghị dùng máy tính · rà tương thích iPad/MacBook/Windows.** Kiểm thử: bộ mới `scratchpad/audit_viewports.py` **684/684 đạt** (18 trang × 7 cỡ màn) · `scratchpad/audit_taps.py` **0 nhóm cần sửa** · `check_pages` **332/332** · `check_quiz_bank` 63/63 · `check_earth2d` 133/133 · `smoke_missions` **74/74** · `smoke_profile_pages` 125/125 · `smoke_vault` 83/83 · `smoke_onboard` 100/100 · `smoke_mission_intro` 57/57 · `smoke_codex` 37/37 · `smoke_earth_done` 33/33 · `regress_auth_pages` 19/19 · `shoot_dodge` 27 PASS · `shoot_defender` 44 PASS. **Chỉ đổi client, không cần deploy backend.**
  - **VIỆC 1 — "Tri Thức" → "Trạm Tri Thức", 25 chỗ ở 11 file.** Lý do và hai chỗ cố ý không đổi ghi ở mục 2 (bảng tên khu). Gọn nhưng phải nhất quán: `dashboard.html` (thẻ MOD-01 + `quiz_title` + `quiz_modal_title` + `need_body`), `learn.html` (`<title>`/eyebrow/H1), `quiz.html`, `library.html` + `codex.html` (nút "Về Trạm Tri Thức"), `achievements.html` + `profile.html` (nút `go_learn`), `games.html`, `js/badges.js`, `js/specimens.js`, `js/onboard-tour.js`. Bản EN vốn đã là "Knowledge Station" nên **không sửa gì bên EN** — chính chỗ lệch đó là bằng chứng tên tiếng Việt bị rút sai.
  - **VIỆC 2 — ngày ra mắt 09/08/2026.** Sáu chỗ, liệt kê ở mục 2 (`index.html`). ⚠️ Con số này ở **hai hệ**: chuỗi hiển thị cho người đọc và mốc thời gian cho máy (`LAUNCH_AT` + `datePublished`). Sửa một hệ mà quên hệ kia thì đồng hồ đếm ngược về 0 trong khi trang vẫn ghi "sắp ra mắt", hoặc ngược lại.
  - **VIỆC 3 — dải khuyến nghị dùng laptop/PC ở `index.html`** (+ 4 khoá i18n × 2 ngôn ngữ, `.mob-note` trong `css/index.css`, `initMobNote()` trong `js/index.js`). Đo trên Chromium **25/25 đạt** qua 6 cấu hình máy + bản EN.
    - ⚠️ **CỐ Ý LÀ DẢI ĐÓNG ĐƯỢC Ở ĐÁY, KHÔNG PHẢI MODAL CHẶN TRANG.** `index.html` là trang duy nhất được lập chỉ mục và đang chạy thật. Một lớp phủ chặn nội dung khi vào từ điện thoại chính là **intrusive interstitial** — Google đánh tụt hạng mobile-first, tức là đánh vào đúng thứ trang này tồn tại để làm. Nên: `role="region"` chứ không `role="dialog"`, không khoá cuộn, có nút ✕, đóng rồi thì **F5 không nhắc lại** (`localStorage["astroq-mob-note"]`).
    - ⚠️ **ĐIỀU KIỆN HIỆN LÀ `(max-width: 860px) and (pointer: coarse)`, KHÔNG phải chỉ bề rộng.** Chỉ xét bề rộng thì kéo hẹp cửa sổ Chrome trên laptop cũng bị mắng "hãy dùng laptop". Đo được: iPhone 12 và iPad mini **hiện**; iPad Pro 12,9", laptop 1440×900, cửa sổ laptop bóp còn 700px, và Windows 2-trong-1 cảm ứng 1366×768 đều **không hiện**.
    - ⚠️ `data-i18n` sẽ in thẻ `<b>` thành chữ thô — câu này có `<b>laptop hoặc PC</b>` nên phải dùng **`data-i18n-html`**.
  - **VIỆC 4 — RÀ TƯƠNG THÍCH: 18 trang × 7 cỡ màn thật** (iPad mini dọc/ngang · iPad Pro 12,9" · MacBook Air 13" 1470×956 · MacBook Pro 14" 1512×982 · Windows 1366×768 · Windows Full HD). Bộ đo mới `scratchpad/audit_viewports.py` (tràn ngang · chữ bị cắt · vùng chạm · phần tử chìa ra ngoài · lỗi console · asset 404) và `scratchpad/audit_taps.py` (liệt kê vùng chạm gom theo selector).
    - **KẾT QUẢ SẠCH SẴN Ở 4 TRONG 5 TIÊU CHÍ:** **0 trang tràn ngang**, **0 phần tử chìa ra ngoài**, **0 lỗi console**, **0 asset 404** trên cả 7 cỡ màn — kể cả 3 mini-game (sân khoá `aspect-ratio`) và 2 trang three.js. Bố cục vốn đã chịu được từ 768px tới 1920px.
    - ⚠️ **LỖI THẬT THỨ NHẤT — CHỮ BỊ CẮT, VÀ CHỈ Ở BẢN TIẾNG VIỆT.** Trên iPad mini 768px, `--panel-w` của explorer hạ 340→300px làm cột chữ của `.reg-head` còn **112px**, trong khi "Vùng lân cận Mặt Trời" cần 136px và "Ngoại hành tinh lân cận" cần 148px → hiện ra **"Vùng lân cận Mặt Trờ…"**. Bản EN ngắn hơn nên không bao giờ lộ. Sửa: `@media (max-width:900px)` cho `.reg-title` xuống dòng. **Giữ ellipsis cho `.loc-ds`** (dòng mô tả một dòng; cho xuống dòng thì mỗi mục dài gấp đôi ở đúng màn nhỏ nhất, mà chữ đầy đủ vẫn hiện ở bảng thông tin) — bộ đo **in ra mục này mỗi lần chạy** thay vì im lặng bỏ qua.
    - ⚠️ **LỖI THẬT THỨ HAI — 14 NHÓM VÙNG CHẠM DƯỚI 44×44px TRÊN iPad**, gồm những nút đi lại chính: VI/EN **26×24**, "Về Trung Tâm Điều Hướng" cao **34**, "Rút lui" **34**, "Đăng xuất" **34**, nút lọc `.seg` **25–32**, `.pbtn` **40–43**, ô tìm kiếm **18**, và ở explorer thì cả một bảng nút nhỏ (nút `→` 26×26, nút gập 32×32, **thanh trượt tốc độ cao 5px**). Mốc 44px là WCAG 2.5.5 + hướng dẫn Apple; với app cho trẻ 8–15 dùng máy tính bảng thì bấm trượt nút Về nghĩa là mắc kẹt trong trang. Sửa ở **file dùng chung trước** (`common.css` phủ 13 trang, `page-shell.css`, `game-shell.css`), rồi mới tới `css/<trang>.css`.
    - ⚠️ **GẮN VÀO `pointer: coarse`, KHÔNG GẮN THEO BỀ RỘNG.** Bề rộng không cho biết người dùng bấm bằng ngón tay hay chuột. Nhờ vậy **giao diện desktop không đổi một pixel**, và toàn bộ bộ kiểm thử cũ (đo vị trí/kích cỡ thật trên Chromium) vẫn đạt.
    - ⚠️ **BA CÁI BẪY CSS, cả ba đều làm bản sửa đầu của tôi VÔ TÁC DỤNG:** (a) **`@media` KHÔNG làm tăng specificity** — tôi đặt override `.reg-title` ở mục RESPONSIVE đầu file trong khi khai báo gốc ở dòng 475, nên `nowrap` vẫn thắng và đo lại vẫn thấy cắt; phải đặt **sau** khai báo gốc. (b) **phần tử `inline` BỎ QUA `min-height`** — link "Wiki" ở chân trang là `<a>` mặc định inline, đặt `min-height:44px` không có gì xảy ra; phải đổi sang `inline-flex`. (c) **`min-width`/`min-height` GHI ĐÈ ĐƯỢC `width`/`height` cố định** (bề rộng dùng thật là `clamp(min, width, max)`), nên các nút vuông 32/34px không phải xoá giá trị cũ.
    - ⚠️ **NGOẠI LỆ WCAG CỦA TÔI LÚC ĐẦU MIỄN TRỪ ĐÚNG THỨ CẦN SỬA.** WCAG cho phép bỏ qua link nằm **giữa một câu**; bản đầu tôi nhận diện bằng "thẻ cha có nhiều chữ hơn con", và điều kiện đó coi **cả dãy nút VI|EN** là inline (vì `<button>` mặc định `inline-block` và thẻ cha chứa cả hai chữ). Sửa: đòi thẻ cha có **chữ TRỰC TIẾP** (`nodeType === 3`) bao quanh. Sau khi siết, "Wiki" ở chân trang **lộ ra là vùng chạm thật** (25×15px) — nó là flex item đứng riêng, hai dấu `·` là `<span>` riêng, không nằm trong câu nào.
    - ⚠️ **PHÉP KIỂM "CHÌA RA NGOÀI" BÁO HỎNG 7 LẦN TRÊN MỘT THIẾT KẾ ĐÚNG.** explorer đỗ bảng thông tin **hẳn** ra ngoài mép khi đóng. Sửa: chỉ tính phần tử **vừa trong vừa ngoài** (bị mép màn hình cắt đôi); phần tử nằm hẳn ngoài là bảng kéo đang đóng, và phép kiểm "không tràn ngang" đã chứng minh nó không sinh cuộn ngang. Miễn trừ riêng lớp `#labels` của explorer: nhãn tên thiên thể do `CSS2DRenderer` đặt theo **vị trí hành tinh trên quỹ đạo**, không phải bố cục CSS — muốn "sửa" thì phải đọc `getBoundingClientRect` cho từng nhãn mỗi khung hình, tức ép trình duyệt tính lại bố cục 60 lần/giây.
    - ⚠️ **MỘT MỤC BÁO CÁO CHỈ HIỆN KHI CÓ VẤN ĐỀ THÌ KHÔNG PHÂN BIỆT ĐƯỢC "SẠCH" VỚI "KHÔNG CHẠY".** Mục ellipsis của tôi để `if ell:` và nó im lặng, tôi tưởng không còn chữ nào bị thu gọn — thật ra **khối `print` chưa được chèn vào file**. Giờ nó in không điều kiện kèm số trang.
    - ⚠️ **`smoke_missions.py` ĐÒI TÊN CŨ "Tri Thức"** nên nó báo hỏng đúng lúc trang đã sửa đúng — cùng loại lỗi "bộ kiểm bảo vệ trạng thái cũ" đã ghi ở nút Mặt Trăng. Đã đổi sang khớp **chính xác** "Trạm Tri Thức" để đổi tên nửa vời cũng bị bắt.
    - **Chứng minh bộ đo có răng** (`--dev`/`--only` để chạy nhanh một tập nhỏ): bỏ `min-height` của `.back-btn` → hỏng ở cả learn và games · trả `.reg-title` về `nowrap` → hỏng đúng 3 tiêu đề · cho `.seg button` rộng 3000px → bắt được **cả** "tràn ngang" **và** "chìa ra ngoài".
    - ⚠️ **PHÁT HIỆN NGOÀI PHẠM VI, CHƯA SỬA: `explorer.html` KHÔNG CÓ NÚT VI/EN NÀO.** JS gọi `AstroQ.initLang(applyLanguage, '.lang-btn')` và `css/explorer.css` có đủ rule `.lang-btn`, nhưng **markup không có phần tử nào** (`grep data-lang` → 0 kết quả) — trang chỉ đi theo ngôn ngữ đã chọn ở trang khác, người dùng không đổi được tại chỗ. Trái quy ước song ngữ ở mục 1. Thêm nút là **thêm tính năng**, không phải sửa tương thích, nên để thành việc riêng.
  — `dashboard.html`, `learn.html`, `quiz.html`, `library.html`, `codex.html`, `achievements.html`, `profile.html`, `games.html`, `index.html`, `js/index.js`, `js/badges.js`, `js/specimens.js`, `js/onboard-tour.js`, `css/{common,page-shell,game-shell,index,select,dashboard,quiz,library,profile,explorer,mission-earth}.css`, `scratchpad/{audit_viewports.py,audit_taps.py,probe_explorer_deck.py,smoke_missions.py}`

### 2026-07-30
- **XOÁ 3.396 DÒNG REACT ĐÃ BỊ THAY THẾ + nghỉ hưu `check_codex.py`.** Xoá `react/astronaut-codex/` (2.920 dòng) và `react/astronomy-library/` (476 dòng); thư mục `react/` còn **4.445 dòng** (chỉ `earth-mission-2d` + `EarthMissionSteps.tsx` + css). Kiểm thử: `check_pages` **332/332** (nhận thêm 2 phép kiểm chuyển sang) · `check_quiz_bank` **62/62** · `smoke_codex` **37/37** · `smoke_earth_done` **33/33** · `check_earth2d` **124/124** · `smoke_missions` 74/74 · `regress_auth_pages` 19/19.
  - **LÝ DO XOÁ KHÔNG PHẢI CHỈ VÌ TRÙNG — mà vì giữ nó BẮT phải sửa hai lần.** Lượt thêm 10 câu hỏi ngay trước đó tôi đã phải sửa `termsData.ts` **lần thứ hai** chỉ để `check_codex.py` không đỏ, trong khi file đó **không trang nào nạp**. Đó là chi phí thật và sẽ lặp lại ở mọi thay đổi sau. Bản vanilla (`codex.html` + `js/codex-terms.js` + 15 icon `cx-` + dây nối `terms` chạy đầu-cuối) đã thay thế đủ và đang chạy trên astroq.org.
  - **KHÔNG MẤT PHÉP KIỂM NÀO — chuyển TRƯỚC khi xoá.** `check_codex.py` có 2 phép kiểm canh **code đang chạy**, đã đưa sang `check_pages.py` mục [12]: ① *5 câu lập trình của bank không bị thuật ngữ nào nhận bừa* (nhận bừa là giải mã sai bằng một câu không liên quan) · ② *bộ nguồn sổ tay trùng khớp bộ nguồn bank* — nhờ vậy 12 URL mà `check_quiz_bank.py` kiểm 200 **thật trên Chromium** cũng chính là bộ sổ tay đang dùng, không phải kiểm 200 lần thứ hai. 139 phép kiểm còn lại của nó soi **chính mã vừa xoá**, nên nghỉ hưu cùng.
  - **Phần MẤT có ý thức: 4 thuật ngữ AI/Lượng tử** (`term_neural_network` · `term_robotics` · `term_qubit` · `term_entanglement`). Chúng là bản nháp `reviewed:false`, **không có nguồn**, nên cố ý không port — thêm vào là phát hành nội dung chưa đối chiếu. Muốn dùng thì bổ sung nguồn rồi thêm vào `js/codex-terms.js` với `cat:"ai"`/`"quantum"`; nhánh phân loại ở `codex.html` đã sẵn.
  - **Vỏ 5 tab của `astronomy-library` cũng bỏ**: 4/5 tab là standby, còn tab Sổ tay đã tách thành trang riêng `codex.html`, nên cái vỏ đó không còn giá trị gì. Muốn dựng lại MOD-06 dạng nhiều tab thì làm bằng vanilla (logic tab ~40 dòng), đừng dựng lại React.
  - **Lưu trữ đầy đủ trong git của `react/`** (repo riêng, chỉ ở máy — chưa có remote): `git show 1b09d96:astronaut-codex/termsData.ts` (mốc gốc) · `git show 492cc09:astronaut-codex/termsData.ts` (bản cuối, đã đồng bộ). Commit `492cc09` được tạo **trước** khi xoá đúng để lịch sử giữ được trạng thái cuối cùng thật, không phải bản trước lúc đồng bộ.
  - ⚠️ **CÒN 4.445 DÒNG REACT NỮA, cùng loại vấn đề.** `earth-mission-2d/` + `EarthMissionSteps.tsx` viết lại `mission-earth.html` (1.592 dòng) + `js/earth3d.js` (1.057 dòng) đang chạy thật. Khi làm `mission-earth.html` v2 (bỏ three.js) thì **giữ 3 kết quả ĐO** rồi xoá nốt: 9 URL ảnh NASA đã kiểm 200 · phát hiện *Blue Marble là ảnh quả cầu tâm Bắc Mỹ, không phải bản đồ phẳng* (đặt lat/lon lên đó là dạy sai địa lý) · `~thumb` của NASA nặng **đúng bằng** `~small` (nên không dùng làm placeholder). `check_earth2d.py` nghỉ hưu cùng lúc đó.
- **THÊM 10 CÂU HỎI cho 5 thuật ngữ mới — mở khoá 5 thẻ Sổ Tay đang "sắp có".** Bank lên **35 câu / 15 thuật ngữ** (trước là 25/10). Kiểm thử: `check_quiz_bank` **62/62** (chạy Chromium thật, gồm **12 URL nguồn NASA kiểm 200**) · `smoke_codex` **37/37** (thêm 4 phép đo dây nối) · `check_pages` **330/330** · `check_codex` **141/141** · hồi quy `smoke_earth_done` 33/33, `smoke_profile_pages` 125/125, `smoke_missions` 74/74, `regress_auth_pages` 19/19, `check_earth2d` 124/124. **Không cần deploy** — lượt này chỉ đổi client.
  - **Số liệu dẫn từ 4 trang NASA đã đối chiếu**, không viết theo ký ức: chân trời sự kiện là **đường biên, không phải mặt đất**, và **không gì thoát ra được kể cả ánh sáng** (Black Holes) · lực hấp dẫn kéo về **tâm**, **càng nhiều khối lượng càng mạnh**, **càng xa càng yếu** (NASA Space Place) · sao sinh trong **mây khí và bụi**, chỗ đặc co lại vì **hấp dẫn** rồi nóng đủ để nhiệt hạch khởi động (Stars) · sao lớn cạn nhiên liệu thì **lõi sắt sụp rồi NẢY lại** tạo sóng xung kích, vật chất bắn ra **làm giàu mây phân tử** cho thế hệ sao sau (Stars) · bức xạ nền là **ánh sáng cổ nhất quan sát được**, phát ra **~380.000 năm** sau Big Bang (Cosmic History).
  - ⚠️ **`gravity` DẪN NASA SPACE PLACE, không phải `science.nasa.gov`** — cố ý: đó là trang NASA viết **cho trẻ em**, đúng độ tuổi 8–15, và `science.nasa.gov` không có trang định nghĩa lực hấp dẫn tương đương. Phép kiểm nguồn ở `check_quiz_bank` đã nới **đúng hai tên miền NASA**, không mở cho URL bất kỳ.
  - **Rải đáp án:** phần thiên văn **7/8/7/8** (30 câu), toàn bank **A=10 · B=9 · C=8 · D=8** (35 câu). ⚠️ Phép kiểm cũ đòi **tuyệt đối 5/5/5/5** — với 30 câu thì không chia hết cho 4, tức là đòi một điều bất khả thi. Đã đổi sang khoảng cho phép suy ra từ số câu; điều muốn biết là "không vị trí nào bị bỏ qua và không vị trí nào bị lạm dụng" (trẻ học "cứ chọn B" thì bài kiểm tra mất tác dụng), không phải một con số cố định.
  - **Nối dây ở `js/codex-terms.js`:** 5 thuật ngữ từ `q: []` thành đủ 2 khoá. Đo trên Chromium: trả lời đúng → **cả 5 thẻ giải mã được**, **không còn thẻ nào ở trạng thái `soon`**, và dùng **một** khoá thì chỉ mở **đúng** thẻ của nó (không "rò" sang thẻ khác).
  - ⚠️ **BA PHÉP KIỂM KHẲNG ĐỊNH ĐÚNG TRẠNG THÁI HỎNG — cùng loại lỗi đã giữ nút Mặt Trăng sống.** `smoke_codex` đòi **"10 thẻ chưa giải mã + 5 thẻ sắp có"** và **"sổ tay chờ 20 khoá bank"**; `check_codex`/`check_pages` điểm danh đúng 5 id ở `PENDING`. Nghĩa là bộ kiểm đang **bảo vệ tình trạng 5/15 thẻ khoá vĩnh viễn**: sửa cho đúng thì nó báo hỏng. Đã đổi tất cả sang **suy ra từ dữ liệu** (2 khoá/thuật ngữ, `soon == 0`), và **giữ** phép kiểm `PENDING` với tập rỗng — thêm thuật ngữ mà quên thêm câu hỏi thì nó lại nói ra ngay.
  - ⚠️ **Lỗi "gán cứng con số" — lần thứ tư, 5 chỗ:** `bank co 25 cau` · `20 cau thien van rai deu 5/5/5/5` · `doc duoc khoa term ... == 25` · `phu dung 20 khoa` · `so tay dang cho 20 khoa`. Tất cả đổi sang hỏi điều muốn biết (`len(bank) == len(NEEDED)*2 + 5`, `len(space_keys) == 2 × số thuật ngữ đã nối dây`, các mốc sàn `>=`).
  - ⚠️ **PHẢI SỬA `termsData.ts` CỦA BẢN REACT LẦN THỨ HAI** để `check_codex` không đỏ — đây chính là **cái giá của việc giữ nhánh chết**: cùng một dữ liệu nay nằm ở hai chỗ (`js/codex-terms.js` đang chạy thật và `react/astronaut-codex/termsData.ts` không trang nào nạp), và mỗi thay đổi phải làm hai lần. **Nên xoá `react/astronaut-codex/` + `react/astronomy-library/` (3.396 dòng)** — bản vanilla đã thay thế đủ, và toàn bộ đã lưu trong git (`react/` commit `1b09d96`). 4 thuật ngữ AI/Lượng tử ở bản React là **bản nháp không nguồn**, cố ý không port; muốn dùng thì bổ sung nguồn rồi thêm vào `js/codex-terms.js`.
  - **Chứng minh dây nối có răng:** trỏ khoá `q` của lỗ đen vào một `term` không tồn tại và xoá 2 câu về hấp dẫn khỏi bank → **cả `check_pages` và `smoke_codex` đều báo hỏng**, chỉ đúng 3 chỗ trỏ hỏng.
- **HOÀN TẤT LUỒNG HẬU-NHIỆM-VỤ: đường về tự động 5 giây + Comet chỉ đường trên dashboard.** Backend deploy `J7lZVtEK…` → **`mcGXtn9hEK0vCO2CFHFCLHzZRiYNtgrE1LF9Iuprofs=`**. Kiểm thử: **254/254 trên bản thật AWS** (`test_onboarding` 45 — thêm mục [6c] 10 phép kiểm · `test_wallet` 69 · `test_profile` 74 · `test_specimens` 66) · `check_pages` **330/330** (thêm mục [13], 28 phép kiểm) · `smoke_earth_done.py` MỚI **33/33** · hồi quy `smoke_onboard` 100/100, `smoke_mission_intro` 57/57, `smoke_missions` 74/74, `smoke_profile_pages` 125/125, `smoke_vault` 83/83, `regress_auth_pages` 19/19, `check_codex` 140/140, `check_earth2d` 124/124. Dữ liệu test dọn sạch (bảng còn 3 dòng), CloudWatch không có lỗi thật.
  - **CỜ "ĐÃ CHÀO" ĐẶT Ở SERVER**, làm cờ thứ ba cạnh `tourSeen`/`intro01Seen` — đổi máy không bị chào lại. ⚠️ **CỐ Ý KHÔNG lưu "chuỗi đã hoàn thành chưa"**: server ĐÃ BIẾT từ `missions.earth.done`. Lưu lần thứ hai là hai nguồn sự thật cho một việc, sớm muộn lệch nhau.
  - **ĐƯỜNG VỀ TỰ ĐỘNG 5 GIÂY** (`mission-earth.html`): ⚠️ bất biến "chỉ đếm SAU khi báo xong lên server" có sẵn nhờ **thứ tự gọi** — `finishStep()` làm `await reportStep(id)` rồi mới tới `showWin()`; **đừng đảo hai lời gọi đó**. Đếm ngay lúc mở modal thì mạng chậm là trẻ bị kéo đi trước khi con số thưởng kịp về, và thứ duy nhất nó kịp đọc là "+0". ⚠️ Mọi tương tác **TẮT đếm hẳn**, không tạm dừng — trẻ đọc chậm hơn người lớn nhiều, một màn thưởng tự biến mất sau 5 giây là màn thưởng bị lấy đi giữa lúc đang đọc. ⚠️ **KHÔNG bắt `focus`**: nút chính có `focus()` cho người dùng bàn phím, bắt focus là đếm tắt ngay lúc mở modal và tính năng thành vô nghĩa.
    - **Nút chính (vàng) là "Về Trung Tâm Điều Hướng"**, không phải nút sang Trung Tâm Nhiệm Vụ — đường về dashboard là đường ĐƯỢC THIẾT KẾ (về tới đó Comet mới chào và chỉ đường), và cũng khớp với đường về tự động. Nút sang Trung Tâm Nhiệm Vụ vẫn còn làm lối đi nhanh.
  - **COMET CHỈ ĐƯỜNG: DÙNG LẠI `js/onboard-tour.js`, KHÔNG viết overlay thứ hai.** Thêm `AstroQTour.guide()` + tham số `steps`/`onSeen`/`pulse` cho `start()`. Nhờ vậy thừa hưởng nguyên 4 bài học đã trả giá (ô sáng tự làm tối cả trang · box thoại có nhánh đặt sang bên · lớp chặn bấm · khung linh vật to) cùng `prefers-reduced-motion` và xử lý bàn phím. Mọi chỗ đọc bộ bước đi qua `stepsNow()`.
    - ⚠️ **`onSeen` LÀ BẮT BUỘC** (`guide()` không chạy nếu thiếu). Thiếu nó thì `markSeen()` rơi về nhánh mặc định và ghi `tourSeen = true` — một phi hành gia mới **mất luôn màn dẫn tham quan** vì một lời chúc mừng.
    - **Một bước thì không vẽ chấm bước** — một chấm đơn độc đọc ra thành "còn nhiều bước nữa mà bị lỗi".
    - **Vòng nhấp nháy dùng ĐÚNG giá trị đề bài** (`0 0 20px rgba(56,189,248,.8)`), nhưng **cộng vào** lớp toả 9999px chứ không thay thế — thay thế thì mất luôn phần làm tối và cả trang sáng trở lại. `prefers-reduced-motion` tắt animation nhưng **giữ bóng đổ tĩnh**, không thì lúc đó không còn gì chỉ đường.
    - **Điều kiện chào hỏi SERVER cả hai vế**, không đoán: chuỗi xong chưa (`/me/missions`) và đã chào chưa (`earth1Greeted`). ⚠️ **Không đọc được cờ → KHÔNG chào.** Khác hẳn màn dẫn tham quan ("thà chào hai lần hơn không chào lần nào") — ở đây chào sai là **chúc mừng một việc trẻ chưa làm**.
    - Chốt tên `data-tour="missions"` (SỐ NHIỀU) cho thẻ MOD-04; ghi chú cũ trong `onboard-tour.js` hướng dẫn số ít, hai tên cho một thẻ thì màn chỉ đường bắt trượt và trẻ chỉ thấy trang tối đi.
  - ⚠️ **LỖI BỐ CỤC THẬT, CHỈ ĐO MỚI THẤY — box thoại đè 74% lên chính thẻ đang giới thiệu (điện thoại 390×844).** Nguyên nhân: engine cuộn thẻ đích bằng `block:"center"`, mà thẻ HUD cao 343px nằm giữa màn 844px chỉ để lại ~250px trên và ~250px dưới, còn box thoại cao 254px → **không nhánh nào (dưới/trên/phải/trái) vừa**, engine rơi về nhánh "giữa" và đè lên thẻ. Sửa ở **engine dùng chung**: thẻ cao hơn 40% khung nhìn thì cuộn `block:"start"` (343 + 254 + lề = 633px < 844px → nhánh "dưới" vừa chỗ). Đo lại: **che 74% → 0%**. Bộ tour 7 bước cũng được lợi và không hồi quy (100/100).
  - ⚠️ **HAI PHÉP KIỂM CỦA TÔI LÚC ĐẦU QUÁ LỎNG, phát hiện bằng chính phép thử phá hoại** — gây 4 lỗi cố ý thì chỉ 2 bị bắt: (a) "không đọc được cờ thì không chào" chỉ tìm mảnh `o.earth1Greeted) return`, mà mảnh đó **vẫn còn** sau khi bỏ `!o || !o.ok` → phải đòi đủ cả vế; (b) "vòng nhấp nháy giữ lớp làm tối" đếm `9999px` trên **cả file**, nên khi keyframes bị đổi thành `box-shadow:0 0 22px …` thì cả file vẫn còn 9999px ở chỗ khác và phép kiểm vẫn "đạt" → phải kiểm **trong chính khối keyframes**. Sau khi siết: cả 4 lỗi đều bị bắt. **Phép thử phá hoại không chỉ chứng minh phép kiểm có răng — nó còn tìm ra phép kiểm rỗng.**
  - **Bề mặt test mới ở `window.__mission`:** `win()` mở thẳng màn tổng kết (khỏi phải chơi hết 8 bước) và `autoLeft`. Cùng quy tắc cũ: **KHÔNG trả Promise** — `page.evaluate` tự chờ promise được trả về nên script test sẽ treo thay vì báo hỏng.
  - ⚠️ **Còn treo:** ① `mission-earth.html` **v2 bỏ three.js** (4.445 dòng React còn lại — giữ 3 kết quả ĐO: 9 URL ảnh NASA kiểm 200 · Blue Marble là ảnh quả cầu tâm Bắc Mỹ · `~thumb` nặng đúng bằng `~small`) · ② **5 thẻ Sổ Tay còn "sắp có"**, thêm 10 câu vào `js/quiz-questions.js` là mở được ngay, không phải sửa logic · ③ hai mảng `ARTICLES` trùng chủ đề ở learn/library · ④ `MaxPerPlanet = 20` là trần chưa bao giờ có tác dụng · ⑤ nội dung 15 thuật ngữ chưa qua rà soát chuyên môn.
- **BỎ KHỐI MẶT TRĂNG Ở MÀN TỔNG KẾT NHIỆM VỤ 01, thay bằng việc tiếp theo CÓ THẬT.** Yêu cầu này được chốt từ trước nhưng khi đó chỉ áp vào `react/earth-mission-2d/` — một nhánh **không trang nào nạp** — nên trang THẬT `mission-earth.html` vẫn còn nguyên, và từ 30/07 nó đã lên bản thật. Kiểm thử: `check_pages` **301/301** (thêm 6 phép kiểm) · `smoke_mission_earth` **141/141** (chơi thật cả 8 bước × 5 lượt: VI · EN · điện thoại 390×844 · giảm chuyển động · mất mạng) · hồi quy `smoke_missions` 74/74, `smoke_profile_pages` 125/125, `smoke_onboard` 100/100, `smoke_mission_intro` 57/57.
  - ⚠️ **KHÔNG CHỈ XOÁ — PHẢI THAY.** Khối Mặt Trăng là thứ **DUY NHẤT** trả lời câu "xong rồi thì làm gì tiếp" trên màn tổng kết. Xoá trắng thì trẻ vừa hoàn thành nhiệm vụ lớn nhất của app và nhận lại hai cái nút xám — **thành đường cụt, tệ hơn cả lời hứa hão**. Nay là khối 🧭 **VIỆC TIẾP THEO** chỉ sang **Trung Tâm Nhiệm Vụ** + **Kho Mẫu Vật** (nhiệm vụ này mở mẫu vật THẬT, nên đó là phần thưởng đáng chỉ tới), và nút chính `win-missions` **bấm được** chứ không `disabled`.
  - **Cố ý KHÔNG hứa nhiệm vụ kế tiếp:** `missions.html` cho biết MISSION-02 Mặt Trăng vẫn "sắp ra mắt", nên không có nhiệm vụ nào để dẫn tới. Chỉ sang khu đã có nội dung thật.
  - **Đã xoá kèm:** 3 khoá i18n (`win_un_k` · `win_un` · `win_moon`) × 2 ngôn ngữ · CSS `.me-win .unlock` (đổi tên thành `.nextup` — giữ tên `.unlock` cho một khối không còn nói về mở khoá gì là để lại bẫy đọc hiểu cho người sửa sau) · và **`reward.unlocked` đã thành mã chết**: nó chỉ tồn tại để bật khối Mặt Trăng, bỏ khối đó rồi thì trường này chỉ còn được GHI mà không ai ĐỌC.
  - ⚠️ **PHÉP KIỂM CŨ CHÍNH LÀ THỨ ĐÃ GIỮ LỖI SỐNG.** `smoke_mission_earth.py` **khẳng định nút Mặt Trăng phải tồn tại và phải `disabled`** (2 phép kiểm), nên trang càng "đúng" theo bộ kiểm thì càng sai theo yêu cầu. Đã đảo chiều cả hai, và `check_pages.py` mục [3b] thêm 6 phép kiểm: không còn dấu vết `win-moon`/`🌙`/`MẶT TRĂNG`/`THE MOON` (quét trên code **đã bóc comment** — ghi chú ghi lại lịch sử "trước đây ở đây có nút Mặt Trăng" là thứ nên có), CÓ khối thay thế, nút thay thế **không** `disabled`, và 3 khoá i18n mới đủ vi+en.
    - **Bài học chung:** trước đó `check_pages` **không có phép kiểm nào** về Mặt Trăng ở trang thật — chỉ `check_earth2d` (soi bản React) có. Yêu cầu được áp vào nhánh chết mà bộ kiểm của nhánh sống không biết gì. **Phép kiểm phải soi thứ người dùng thật sự chạy.**
  - **Chứng minh có răng:** nhét lại một nút `win-moon disabled` → `check_pages` báo hỏng ngay (`['Mặt Trăng', 'win-moon', '🌙']`).
  - ⚠️ **Còn treo (bước 2 và 3 của việc này):** đường về tự động 5 giây · Comet chỉ đường trên dashboard + pulse ở thẻ MOD-04 · cờ "đã chào". Bước 3 nên **dùng lại** `js/onboard-tour.js` (thêm tham số `steps` tuỳ chọn) chứ không viết mới — `css/onboard-tour.css` đã có `.tour-hole` (ô sáng tự làm tối cả trang), `.tour-bubble` (có nhánh đặt sang bên), `.tour-block`, `.tour-ava`, nên thừa hưởng luôn cả 4 bài học đã trả giá. Cần chốt: cờ "đã chào" đặt ở server (cạnh `tourSeen`/`intro01Seen`, phải deploy lại) hay localStorage.
- **PUSH CLIENT lên GitHub Pages — 6 commit, `ef92819` → `1033169`.** Toàn bộ công việc từ 28/07 (trước đó chưa từng commit) chia theo chủ đề để lịch sử còn đọc được, không dồn một commit khổng lồ: ① hạ tầng dùng chung (`page-shell`/`game-shell`/`progress.js`/dữ liệu song ngữ/ví) · ② 3 mini-game · ③ 4 trang mới (hồ sơ · thành tích · mẫu vật · sảnh nhiệm vụ) · ④ onboarding + Nhiệm Vụ 01 · ⑤ luật thưởng mới + Sổ Tay Thuật Ngữ · ⑥ 28 script kiểm thử + nhật ký.
  - ⚠️ **TẠO `.gitignore` TRƯỚC KHI `git add -A` — nếu không thì đẩy 38 MB rác lên một repo công khai.** Repo này deploy qua Pages nên mọi thứ commit là thứ người khác phải tải khi clone. Bị chặn: `scratchpad/*.png` (**88 ảnh chụp = 35 MB**, chỉ dùng soi mắt một lượt) và **3 ảnh gốc chưa tối ưu** — `luna ngang.png` 1,7 MB (CLAUDE.md đã ghi rõ *đừng `git add`*; nó chỉ được NHẮC trong ghi chú ở `game-dodge.html`, bản dùng thật là `luna-side.png` 8,7 KB), `AstroQ logo.png` + `tramvutru.png` 1,1 MB mỗi cái (**grep ra 0 chỗ tham chiếu**). Đã kiểm `git log --all` để chắc chúng **chưa từng** vào lịch sử, và kiểm lại trên bản thật: cả ba trả 404, `luna-side.png` trả 200.
    - **GIỮ `scratchpad/*.py`** — đó là bộ kiểm thử của dự án. CLAUDE.md đã ghi một lần mất `gen_wiki_data.py` vì nó chỉ nằm trên máy.
  - ⚠️ **`git commit -F -` qua ĐƯỜNG ỐNG là hỏng.** `git commit -F - <<'MSG' | grep …` làm heredoc bị đường ống ăn mất, git báo *"Aborting commit due to empty commit message"* nhưng file **đã staged** — dễ tưởng là đã commit. Ghi thông điệp ra file rồi `-F <file>`.
  - **Đo trên BẢN THẬT sau khi Pages build xong: 13/13 đạt.** 10 file mới trả 200 (`js/progress.js`, `codex.html`, `missions.html`, `profile.html`, `achievements.html`, `specimen-vault.html`, `mission-earth.html`, `css/page-shell.css`, `js/quiz-questions.js`, `js/codex-terms.js`); 4 trang mở trên Chromium **0 lỗi console, 0 asset 404**; Sổ Tay vẽ đủ 15 thẻ, chưa đăng nhập thì **0 thẻ giải mã** + có dải nhắc; khu Tri Thức **không còn** lời hứa "Đọc xong nhận Thiên thạch tím".
  - ⚠️ **TỪ GIỜ LUẬT MỚI CÓ HIỆU LỰC VỚI TRẺ THẬT:** đọc bài không thưởng tt, quiz phải đúng ≥3/5 mới có tt, số dư khởi tạo 0 (không còn 50 ảo).
- **DEPLOY BACKEND lên AWS (luật thưởng mới + `terms`).** `AstroqSV` Lambda: `T7Tzt7x1…` → **`J7lZVtEKDvJxAuwBivD8jaaTGBqTHS3II5IunxWbW5Y=`**. Chạy lại **cả 4 bộ test API trên bản thật: 245/245 đạt** (`test_wallet` 69 · `test_profile` 74 · `test_specimens` 66 · `test_onboarding` 36). Dữ liệu test đã dọn sạch — bảng `astroq-main` còn đúng **3 dòng** của tài khoản thật.
  - ⚠️ **KHÔNG CÓ `sam` CLI TRÊN MÁY** (chỉ có `aws` CLI + `dotnet`). Vì lượt này **không đổi `template.yaml`** (chỉ đổi code C#, không thêm resource/route/biến môi trường), nên cập nhật code Lambda trực tiếp bằng `aws lambda update-function-code` — **an toàn hơn** cả `sam deploy` vì không tạo changeset CloudFormation. Đổi template thì bắt buộc phải có `sam`.
  - **Quy trình đã dùng, làm lại y hệt nếu deploy lần sau:**
    1. `aws lambda get-function --query Code.Location` rồi `curl` tải **gói đang chạy** về làm **mốc rollback** (39 file, 5,5 MB). Rollback = `aws lambda update-function-code --zip-file fileb://ROLLBACK-*.zip`.
    2. `dotnet publish -c Release -r linux-arm64 --self-contained false -o <ngoài OneDrive>`. ⚠️ **`-r linux-arm64` là BẮT BUỘC** — hàm chạy Graviton (`Architectures: [arm64]`); publish x64 là hàm chết ngay. ⚠️ **`--self-contained false`** vì `dotnet10` là managed runtime.
    3. ⚠️ **PUBLISH RA NGOÀI OneDrive.** Đây chính là lỗi đã ghi ngày 29/07: `sam build` chết vì OneDrive giữ `.aws-sam/build` (`WinError 5`), rồi `sam deploy` đóng gói đúng thư mục rỗng đó → Lambda trả `Uploaded file must be a non-empty zip` → CloudFormation `UPDATE_ROLLBACK_COMPLETE`.
    4. ⚠️ **KIỂM ARTIFACT TRƯỚC KHI DEPLOY** — đếm file (39), có `AstroqSV.Api.dll` + `.deps.json`, và **tìm chuỗi trong DLL** để chắc gói chứa code mới (`quizPassed`, `termsDone`, `QuizPassRatio` đều có). `aws lambda update-function-code` KHÔNG tự phát hiện gói sai.
    5. `aws lambda wait function-updated` rồi kiểm `/health` 200 + các route `/me/*` vẫn trả 401 + route cũ `/auth/register` vẫn trả `invalid-email`.
    6. **Đọc CloudWatch.** ⚠️ Git Bash biến `/aws/lambda/...` thành đường dẫn Windows — phải đặt `MSYS_NO_PATHCONV=1`. Có 11 sự kiện khớp `ERROR|Exception`, đọc ra thì **toàn bộ là nhánh token rác mà test cố tình gây** (`IDX14100: JWT is not well formed`) — không có lỗi thật nào.
  - **Không cần migration cho `terms`.** Nó là string set thêm mới; `ReadProgress` đã coi "thiếu khoá = danh sách rỗng", và bản ghi PROGRESS của tài khoản thật thậm chí chưa tồn tại.
  - ✅ **CLIENT ĐÃ PUSH 30/07/2026** (xem mục *Push client* ngay dưới). Ghi lại tình trạng trước đó vì nó giải thích vì sao deploy backend không gây rủi ro: **client chưa được push từ 27/07.** Commit gần nhất của `AstroQhtml` là `ef92819` (27/07); **toàn bộ công việc từ 28/07 đến nay chưa commit** — `js/progress.js`, `js/badges.js`, `js/quiz-questions.js`, `css/page-shell.css`, `missions.html`, `profile.html`, `achievements.html`, `specimen-vault.html`, `mission-earth.html`, `codex.html`, 3 game… Đo trên bản thật: `https://astroq.org/js/progress.js` trả **404**, `missions.html` **404**, `codex.html` **404**.
    - **Hệ quả tốt:** deploy backend lần này **không ảnh hưởng người dùng thật**, vì client đang chạy còn chưa có `js/progress.js` nên không hề gọi `/me/progress`.
    - **Hệ quả xấu:** mọi thứ làm 3 ngày qua **chưa tới tay ai**, và chỉ được OneDrive bảo vệ. Push là việc nên làm sớm.
    - ⚠️ Sau khi push thì **luật mới có hiệu lực với trẻ thật ngay**: đọc bài không còn thưởng, quiz phải đúng ≥3/5 mới có tt.
- **PORT SỔ TAY THUẬT NGỮ TỪ REACT SANG VANILLA + NỐI DÂY "làm Quiz xong tự giải mã".** Trang mới **`codex.html`** (+ `css/codex.css`, `js/codex-terms.js`, 15 icon `cx-` vào `js/icons.js`), server nhận thêm `terms`, `quiz.html` gửi khoá thuật ngữ trả lời đúng. Kiểm thử: `test_wallet` **69/69** (thêm mục [8b], 10 phép kiểm cho `terms`) · `test_profile` **74/74** · `test_specimens` **66/66** · `test_onboarding` **36/36** (đều trên backend chạy ở máy) · `check_pages` **295/295** (thêm mục [12], 43 phép kiểm) · `smoke_codex.py` MỚI **31/31** · hồi quy `smoke_profile_pages` **125/125**, `smoke_missions` **74/74**, `smoke_vault` **83/83**, `regress_auth_pages` **19/19**, `check_quiz_bank` **54/54**, `check_codex` **140/140**, `check_earth2d` **124/124**.
  - **QUYẾT ĐỊNH KIẾN TRÚC: PORT NGƯỢC, KHÔNG DỰNG TOOLCHAIN.** Con số quyết định không phải "8.953 dòng React" mà là **tỷ lệ trùng lặp: 62%** (`earth-mission-2d` + `EarthMissionSteps` 4.445 dòng viết lại `mission-earth.html` 1.592 + `js/earth3d.js` 1.057 **đang chạy thật**; `SpecimenVault.tsx` 1.112 dòng viết lại `specimen-vault.html` 535 **đang chạy thật**). Chỉ **38%** là chức năng mới, và phần đáng giá là Sổ tay. Ba lý do chọn vanilla: ① React+ReactDOM ~140 KB gzip đi ngược đúng cái dự án đã trả giá để có (font 621→101 KB, ảnh 72 MB→2,79 MB, **cố ý không nạp SDK Firebase 233 KB** ở trang cần mượt); ② mã React **không dùng hệ i18n của dự án — grep `applyTexts`/`data-i18n`/`AstroQ.` ra 0 kết quả**, dựng toolchain là nuôi hai hệ i18n + hai hệ state song song; ③ ở đây vanilla *dễ hơn* — phụ thuộc thật chỉ có `react` + `lucide-react`, mà `js/icons.js` đã có sẵn bộ icon cùng phong cách.
  - **LƯU TRỮ TRƯỚC KHI XOÁ.** `../react/` **nằm ngoài mọi git repo** (`AstroQhtml/.git` là repo duy nhất, `react/` là thư mục ngang hàng) — 8.953 dòng chỉ được OneDrive bảo vệ. Đã `git init` trong `react/` và commit mốc `1b09d96` "Mốc lưu trữ", rồi commit `413b553` xoá `SpecimenVault.tsx` + `specimen-vault.css` **riêng** để tra lại được (`git show 1b09d96:SpecimenVault.tsx`). ⚠️ Repo này **chỉ ở máy**, chưa có remote — muốn an toàn khỏi mất ổ đĩa thì phải thêm remote.
  - ⚠️ **DÂY NỐI TRƯỚC ĐÂY HỞ HOÀN TOÀN, VÀ ĐÓ MỚI LÀ LÝ DO SỔ TAY VÔ DỤNG.** `quiz.html` chỉ gửi `{correct, total, meteors}` nên **server không biết trẻ trả lời đúng thuật ngữ nào** → mọi thẻ khoá vĩnh viễn, kể cả khi bản React chạy được. Nay: `quiz.html` gom `it.term` của từng câu ĐÚNG → `AstroQProgress.quiz({terms})` → `POST /me/progress` → **`PROGRESS.terms`** (string set) → `GET /me/achievements` trả `progress.terms` → `codex.html` suy trạng thái.
    - **Lưu bằng `ADD` trên string set** (như `planets`): phép HỢP nên trả lời đúng lại thuật ngữ cũ không ghi trùng, và hai tab nộp cùng lúc không mất khoá nào. ⚠️ SS của DynamoDB **không nhận tập rỗng** — phải chặn `Count > 0` ở server VÀ `if (o.terms && o.terms.length)` ở client.
    - ⚠️ **KẸP SỐ KHOÁ THEO SỐ CÂU ĐÚNG** (`Take(Math.Min(correct, 25))`). Thiếu chốt này thì gửi `{correct:1, total:5, terms:[…20 khoá…]}` là **giải mã cả sổ tay bằng một câu đúng**. Có phép kiểm API riêng, đo được "1 câu đúng → chỉ nhận thêm 1 khoá".
    - **Gửi `terms` KỂ CẢ khi chưa ĐẠT** — cổng "đạt" chỉ chi phối Thiên thạch tím. Trẻ trả lời đúng một thuật ngữ thì đã hiểu thuật ngữ đó; bắt làm lại cả bài mới cho giải mã là phạt oan.
  - **BA trạng thái thẻ, không phải hai.** `done` · `lock` (có câu hỏi → nói cách mở) · **`soon`** (chưa có câu hỏi nào trong bank → nói thật "sắp có", **không** dẫn sang Quiz). 5 thuật ngữ thêm hôm nay (lỗ đen · hấp dẫn · tinh vân · siêu tân tinh · bức xạ nền) đang ở trạng thái này. Đúng bài học `js/specimens.js`: *đừng viết "Mở khoá tại Mission 02" cho nhiệm vụ chưa tồn tại*. Phép kiểm **điểm danh đúng 5 id đó** — thêm thẻ khoá vĩnh viễn thứ 6 mà không khai là báo hỏng.
  - **4 thuật ngữ AI/Lượng tử CỐ Ý chưa port**: bản React để `reviewed:false`, **không có nguồn**. Port vào là phát hành nội dung chưa đối chiếu. Vì thế trang chỉ còn bộ lọc **Trạng thái**, chưa có bộ lọc Phân loại (một chip thì lọc để làm gì).
  - **Song ngữ đầy đủ**: 15 thuật ngữ × 2 ngôn ngữ × 6 trường (`t·an·sum·def·gr·dg`). Bản React trước đó VI-only cho phần lớn trường. Phép kiểm đếm `vi: {` == `en: {` == số thuật ngữ và mỗi trường xuất hiện đúng `n × 2` lần.
  - ⚠️ **LỖI THẬT CHỈ ĐO BẰNG CHROMIUM MỚI THẤY: `applyLang` không chạy lần đầu.** `AstroQ.initLang()` chỉ gắn nút VI/EN và lắng nghe tab khác — **nó KHÔNG tự gọi callback lần đầu**, mà tôi lại thiếu dòng `applyLang(LANG)` trước nó (achievements.html có). Hậu quả: chọn EN thì **nửa trang một thứ tiếng** — badge và dải nhắc do JS sinh ra tiếng Anh, còn `h1`/phụ đề/nhãn lọc khai bằng `data-i18n` đứng nguyên tiếng Việt. Đọc code không thấy; `#m-state` = "Not decoded" mà `h1` = "Sổ Tay Thuật Ngữ" mới thấy.
  - **Sửa kèm một lời hứa đã sai từ lượt trước:** `learn.html` vẫn quảng cáo *"Đọc xong nhận Thiên thạch tím"* (VI + EN) sau khi đọc bài đã bỏ thưởng. Đổi thành "Đọc xong được ghi vào hồ sơ", và thẻ Quiz nói rõ "làm **ĐẠT** là có Thiên thạch tím". Thêm thẻ **MOD-C** dẫn sang Sổ tay; lưới `.modes` từ 2 cột cố định đổi thành 1→2→3 cột, không thì thẻ thứ ba nằm lẻ một hàng rộng gấp đôi.
  - ⚠️ **Lỗi "đếm cả chữ trong ghi chú của chính mình" — LẦN THỨ TÁM, và lần này sửa Ở BỘ KIỂM.** Phép kiểm "khoá i18n khai mà không dùng" quét `t("…")` trên **văn bản thô**, nên một ghi chú của tôi viết `t("…")` bị tính là khoá đang dùng và báo "chưa khai" oan. Đã đổi sang quét trên `strip_comments(js)` — sửa cho **mọi trang**, không né ở một chỗ.
  - **Chứng minh mục [12] CÓ RĂNG:** trỏ một khoá `q` vào term không tồn tại → hỏng · bỏ `terms: okTerms` ở quiz.html → hỏng · bỏ chốt kẹp theo `correct` ở server → hỏng · thêm `filter:grayscale()` vào thẻ khoá → hỏng.
  - ⚠️ **Còn treo:** ① **`earth-mission-2d` + `EarthMissionSteps.tsx` (4.445 dòng) chưa xử** — nên **giữ 3 kết quả ĐO** (9 URL ảnh NASA kiểm 200 · *Blue Marble là ảnh quả cầu tâm Bắc Mỹ, không phải bản đồ phẳng* · `~thumb` nặng đúng bằng `~small`) rồi dựng `mission-earth.html` v2 bỏ three.js, là một việc riêng và lớn hơn. ② `mission-earth.html` **vẫn còn nút/nhãn Mặt Trăng** (dòng 184·188·400–401·1412–1415) trái yêu cầu đã chốt. ③ **Chưa bổ sung câu hỏi cho 5 thuật ngữ mới** → 5 thẻ đó còn ở trạng thái "sắp có"; thêm 10 câu vào `js/quiz-questions.js` là mở được ngay, không phải sửa logic. ④ Tour vẫn không giới thiệu MOD-04; tên `data-tour` lệch `mission` vs `missions`. ⑤ Hai mảng `ARTICLES` trùng chủ đề ở learn/library. ⑥ `MaxPerPlanet = 20` là trần chưa bao giờ có tác dụng. ⑦ Nội dung 15 thuật ngữ **chưa qua rà soát chuyên môn** (số liệu có nguồn NASA, cách diễn đạt cần giáo viên đọc lại). ⑧ **Backend ĐÃ deploy 30/07/2026** — xem mục *Deploy backend* ngay dưới. ⑨ **CLIENT CHƯA PUSH** — xem cùng mục đó, đây là việc còn treo LỚN NHẤT. — `codex.html`, `css/codex.css`, `js/codex-terms.js`, `js/icons.js`, `js/progress.js`, `quiz.html`, `learn.html`, `css/learn.css`, `scratchpad/{check_pages.py,test_wallet.py,smoke_codex.py}`, `AstroqSV/src/AstroqSV.Api/{Data/DynamoContext.cs,Endpoints/MeEndpoints.cs}`, `../react/` (git init + xoá bản trùng)
- **RÀ SOÁT TOÀN BỘ LUỒNG HỌC / CHƠI / ONBOARDING, rồi SỬA NHÓM LỖI KINH TẾ.** Yêu cầu chốt: **đọc bài không thưởng Thiên thạch tím, làm Quiz phải ĐẠT mới có.** Kiểm thử: `test_wallet` **59/59** · `test_specimens` **66/66** · `test_profile` **74/74** · `test_onboarding` **36/36** (đều trên backend chạy ở máy, `dotnet run`), `check_pages` **239/239** (thêm mục [3d], 20 phép kiểm mới) · `smoke_profile_pages` **125/125** · `check_quiz_bank` **54/54** · `smoke_missions` **74/74** · `smoke_vault` **83/83** · `regress_auth_pages` **19/19** · `check_codex` **140/140** · `check_earth2d` **124/124**.
  - ⚠️ **LỖI NẶNG NHẤT ĐÃ SỬA — ĐỌC BÀI: CLIENT CỘNG 6–15 tt, SERVER CỘNG 0.** `AstroQProgress.lesson(id)` gửi payload **không có trường `meteors`**; server `Clamp(null) → 0` rồi `Award("lesson", 0) → 0`. Trong khi đó `learn.html`/`library.html` gọi `Economy.addAsteroids(reward)` lạc quan và toast báo "+15". Tệ hơn: `report()` gọi `syncWallet()` ngay khi có phản hồi nên **số dư tụt lại trước mắt trẻ trong khoảng một giây**, không phải đợi sang trang. Sửa theo luật mới: bỏ hẳn phần cộng ở client, bỏ mọi nhãn "+N ☄️" (nút hero, badge thẻ, nút trong trình đọc), và **chặn thật ở server** bằng `Wallet.MaxPerLesson = 0` — bỏ ở client thôi thì ai mở DevTools cũng gửi được `{type:"lesson", meteors:9999}`.
  - ⚠️ **HAI BÀI VƯỢT TRẦN, phát hiện khi đối chiếu client ↔ server:** `lib-nebula` **15 tt** và `lib-blackhole` **13 tt** trong khi trần cũ `MaxPerLesson = 12`. Ghi chú ngay trên hằng đó (*"bài trả cao nhất hiện là 8 → trần 12"*) đã lạc hậu từ khi có `library.html`. Nay không còn ý nghĩa vì trần là 0, nhưng đây là ví dụ mẫu của lỗi **hai nơi cùng giữ một con số**.
  - ⚠️ **`meteorsEarned` TỪNG PHÌNH BẰNG TIỀN ẢO.** `deltas["meteorsEarned"]` được cộng bằng **số client khai**, *trước* khi kẹp trần — nên đọc một bài khai 15 tt thì bộ đếm cả đời +15 trong khi ví +0, và **huy hiệu "nhà sưu tầm" mở bằng số tiền chưa bao giờ tồn tại**. Đã chuyển sang cộng bằng `award` (số thật) và dời phép tính lên trước `BumpProgressAsync`.
  - **CỔNG "ĐẠT" CỦA QUIZ nằm ở SERVER, không phải client.** `Wallet.QuizPassRatio = 0.60` + `Wallet.QuizPassed()` + `Wallet.AwardQuiz(correct, total, claimed)`; endpoint gọi `AwardQuiz` cho `type=="quiz"` chứ không gọi `Award` trơn. Đặt cổng ở client thì chỉ cần gửi `{correct:5, total:5, meteors:220}` là ăn trần. Response trả thêm **`quizPassed` + `quizPassMark`** để client khỏi tự tính lại ngưỡng — hai nơi giữ một con số thì sớm muộn lệch, và bên lệch sẽ là bên nói với trẻ.
    - Chọn **60% chứ không phải 50%** vì 50% đã là mốc 2/3 ngôi sao ở bảng tổng kết; để hai mốc trùng nhau thì "2 sao" và "đạt" thành hai tên cho một thứ. 60% × `ROUND_SIZE 5` = **đúng 3 câu**; đổi `ROUND_SIZE` thì phải kiểm lại `PASS_MIN`.
    - `quiz.html`: **bỏ `Economy.addAsteroids()` ở chỗ trả lời đúng từng câu**, dồn về **một lần duy nhất** ở `showSummary()` và chỉ khi đạt. Cộng từng câu thì trẻ đúng 1/5 vẫn thấy ví +20 rồi bị kéo về — đúng lỗi vừa sửa ở phần đọc bài. Thêm dòng `#sum-note`: đạt thì nói được bao nhiêu, **chưa đạt thì nói rõ cần đúng bao nhiêu câu** (không có dòng này trẻ chỉ thấy "+0" mà không hiểu vì sao). Tông **hổ phách chứ không đỏ** — đây là màn học, đỏ đọc ra thành "em sai rồi".
  - **Đo được trên Chromium thật:** log `smoke_profile_pages` cho thấy lượt 1/5 câu đúng gửi lên đúng `{correct:1, total:5, meteors:0}` — cổng "đạt" chạy đầu-cuối, không chỉ đúng trên giấy.
  - **Dọn kèm:** ô thống kê thứ hai của `library.html` trước đây cộng **tổng tt thưởng đọc bài** → giờ luôn bằng 0, mà một ô thống kê luôn 0 đọc ra thành "app hỏng"; đổi thành **tổng số bài** (cặp số thành "đã đọc / tổng") và đổi icon tt sang icon layers. Xoá field `reward` khỏi cả hai mảng `ARTICLES` (dữ liệu chết, để lại là mời người sau thêm thưởng trở lại) và xoá **`bumpGem()` đã thành mã chết** ở `learn.html` + `library.html` (3 game và `quiz.html` vẫn dùng nên CSS `.gem.bump` giữ nguyên).
  - ⚠️ **BỘ KIỂM CŨ KHÔNG BẮT ĐƯỢC GÌ TRONG SỐ NÀY.** `check_pages` vẫn 219/219 sau khi tôi sửa xong — vì nó **không có phép kiểm nào cho luật thưởng**. Đã thêm mục **[3d]** đối chiếu HAI BÊN: trần đọc bài = 0 · `learn`/`library` không còn `addAsteroids` nhưng **vẫn** gọi `AstroQProgress.lesson` · `PASS_PCT` client **khớp** `QuizPassRatio` server · endpoint đi qua `AwardQuiz` · quiz chỉ cộng ví **một** chỗ · `meteorsEarned` cộng bằng `award` · `DEFAULT_BALANCE = 0` · khoá i18n `pass_note`/`fail_note` có ở **cả** vi và en. **Chứng minh có răng:** lệch ngưỡng client 50 vs server 60 → hỏng; mở lại trần đọc bài về 12 → hỏng; thêm lại `addAsteroids` vào `library.html` → hỏng.
  - ⚠️ **Lỗi "gán cứng con số" LẦN THỨ BA, lần này ở `test_specimens.py`** — 4 phép kiểm đòi đúng *20 mẫu / 5 mẫu hiếm* trong khi danh mục đã lên **21/6**; hỏng sẵn từ trước, **không phải do lượt sửa này**. Đã đổi sang hỏi điều muốn biết (`summary.total` khớp số mẫu API trả về · `rareTotal` khớp số mẫu hiếm đếm được · các mốc sàn `>=`). Cùng bài học đã ghi cho `smoke_vault.py` và `check_codex.py`.
  - ⚠️ **CÒN TREO — các vấn đề rà ra nhưng CHƯA sửa trong lượt này** (xếp theo mức nghiêm trọng, cần chốt hướng trước khi làm):
    1. **~7.000 dòng React ở `../react/` KHÔNG có trang nào nạp** (grep toàn bộ `*.html`+`*.js` cho 5 tên module → 0 kết quả; repo không có `package.json`). Sổ tay thuật ngữ, Thư viện Thiên văn 5 tab, bản 2D nhiệm vụ, Kho mẫu vật React — trẻ **không mở được cái nào**. Hai lượt cải tiến gần đây đều nằm ở nhánh không chạy. Phải chốt: dựng toolchain, hay **port ngược** về HTML thuần (tôi nghiêng về port ngược — mục 1 định nghĩa repo là tĩnh, không build step).
    2. **`mission-earth.html` (trang ĐANG CHẠY) vẫn còn nút/nhãn Mặt Trăng** (dòng 184 · 188 · 400–401 · 1412–1415), trái yêu cầu đã chốt; và **không có** đường về tự động 5s, **không có** Comet chỉ đường, **không ghi** `earth_chain_1_completed` — tất cả nằm ở bản React không chạy.
    3. **Sổ tay thuật ngữ không bao giờ giải mã được:** `quiz.html` chỉ gửi `{correct, total, meteors}`, **không gửi `terms`** → server không biết trẻ đúng thuật ngữ nào. Cần thêm `terms: string[]` vào `POST /me/progress` (cả hai phía) rồi map qua `QUIZ_BANK_TERM_TO_CODEX_ID`.
    4. **Tour 7 bước KHÔNG giới thiệu MOD-04 Trung Tâm Nhiệm Vụ** (không có `data-tour` cho nó), trong khi đó là hành động chính sau khi xong nhiệm vụ. Và `js/onboard-tour.js` hướng dẫn thêm bằng `data-tour="mission"` (số ít) còn `CometGuidance` bắt `[data-tour="missions"]` (số nhiều) — **lệch tên, phải chốt một**.
    5. **`learn.html` và `library.html` có HAI mảng `ARTICLES` riêng, trùng chủ đề** (`gaia`↔`lib-gaia`, `eht`↔`lib-blackhole`, `exo-ai`↔`lib-exoplanet`) → sửa nội dung phải sửa hai nơi (trái quy tắc 2 mục 6). *(Đường trả thưởng hai lần cho cùng chủ đề thì đã hết vì đọc bài không còn thưởng.)*
    6. **Ghé hành tinh: `MaxPerPlanet = 20` là trần chưa bao giờ có tác dụng** (`planet()` không gửi `meteors`). Cần chốt: cho thưởng thật, hay đặt trần 0 cho rõ ràng như đọc bài.
    — `AstroqSV/src/AstroqSV.Api/Services/Wallet.cs`, `AstroqSV/src/AstroqSV.Api/Endpoints/MeEndpoints.cs`, `economy.js`, `quiz.html`, `css/quiz.css`, `learn.html`, `library.html`, `scratchpad/check_pages.py`, `scratchpad/test_wallet.py`, `scratchpad/test_specimens.py`
- **⚠️ ĐÃ XOÁ 30/07/2026 (xem mục *Xoá 3.396 dòng React*)** — THƯ VIỆN THIÊN VĂN (Star Archive, MOD-06) — vỏ ngoài 5 tab + 5 thuật ngữ thiên văn mới.** Thư mục mới `../react/astronomy-library/` (4 file): `libraryTabs.ts` · `AstronomyLibrary.tsx` · `astronomy-library.css` · `index.ts`. Đây là **trang của card MOD-06** mà mục 2 vẫn ghi là *chưa có trang*. Kiểm thử: `check_codex.py` **143/143 đạt** (thêm mục [9], 48 phép kiểm mới, gồm 3 URL nguồn mới **kiểm 200 trực tiếp**); hồi quy `check_earth2d` **124/124**, `check_pages` **219/219**, `check_quiz_bank` **53/53**, `smoke_missions` **74/74**.
  - **DÙNG LẠI `astronaut-codex/` CÓ SẴN CHO TAB SỔ TAY, KHÔNG VIẾT `TermCodex` THỨ HAI.** Đề bài yêu cầu 3 component `AstronomyLibrary`/`TermCodex`/`TermCard`; hai cái sau **đã tồn tại** từ hôm nay (`AstronautCodex.tsx`, `TermCard.tsx`) với đủ lưới thẻ, 2 bộ lọc, sổ tay chi tiết và store giải mã. Nên chỉ dựng `AstronomyLibrary`, còn tên `TermCodex` được xuất thành **bí danh** ở `astronaut-codex/index.ts` (`export { default as TermCodex }`). Dựng file thứ hai cùng chức năng là hai bản sẽ lệch nhau sau vài lần sửa. Phép kiểm canh **không có file `TermCodex.tsx` nào tồn tại**.
  - **5 thuật ngữ mới, ĐỌC NGUỒN NASA TRƯỚC KHI VIẾT:** Lỗ đen · Lực hấp dẫn · Tinh vân · Siêu tân tinh · Bức xạ nền vũ trụ. Số liệu dẫn nguyên văn: lỗ đen nhỏ nhất đã biết **3,8 lần** Mặt Trời, lớn nhất **TON 618 66 tỉ lần**, lỗ đen Ngân Hà **4 triệu lần**; bức xạ nền phát ra **380.000 năm** sau Big Bang, chênh lệch nhiệt độ **13,8 tỉ năm** tuổi. Bộ thuật ngữ lên **19**, icon lên **19** (5 bản vẽ SVG mới, vẫn viewBox 24×24 + `currentColor`, 0 file tải thêm).
  - ⚠️ **NỚI PHÉP KIỂM NGUỒN TỪ `science.nasa.gov` SANG "tên miền CỦA NASA", chỉ nới đúng 2 tên miền.** `term_gravity` dẫn **NASA Space Place** (`spaceplace.nasa.gov/what-is-gravity/en/`) — trang NASA viết **cho trẻ em**, đúng độ tuổi 8–15, và `science.nasa.gov` không có trang định nghĩa lực hấp dẫn tương đương. Vẫn là nguồn NASA chính thức, không phải mở cửa cho URL bất kỳ.
  - ⚠️ **PHÉP KIỂM "bộ nguồn Codex TRÙNG KHỚP bộ nguồn đã kiểm 200 ở `quiz-questions.js`" ĐÃ ĐỔI HÌNH MÀ KHÔNG MẤT BẢO ĐẢM.** 3 URL mới không có trong bank nên đòi trùng khớp tuyệt đối là sai, nhưng bỏ phép kiểm là mất luôn bảo đảm "Codex không dẫn URL chưa ai kiểm". Nay: (a) mọi nguồn của bank **phải còn** trong Codex, (b) URL **chỉ riêng Codex** thì script **tự curl kiểm 200** (thêm `--offline` để bỏ qua phần mạng).
  - ⚠️ **PHÁT HIỆN QUAN TRỌNG NHẤT — 5 THẺ MỚI SẼ KHOÁ VĨNH VIỄN NẾU KHÔNG LÀM TIẾP.** `js/quiz-questions.js` chỉ có câu hỏi cho **10** thuật ngữ thiên văn; không có câu nào về lỗ đen/hấp dẫn/tinh vân/siêu tân tinh/bức xạ nền. Nên `quizBankTerms: []` (trung thực) và `requiredQuizId` của chúng (`quiz_black_hole_01`…) **chưa tồn tại ở đâu cả** → cơ chế tự giải mã đúng nhưng **không có quiz nào bắn tín hiệu**. Đúng lỗi đã ghi ở `js/specimens.js` ("đừng viết *Mở khoá tại Mission 02* — nhiệm vụ ĐÓ chưa tồn tại nên mẫu khoá vĩnh viễn"). Phép kiểm **điểm danh đúng 5 id đó** (`PENDING_BANK`): thêm thẻ khoá vĩnh viễn thứ 6 mà không khai là báo hỏng, và nối dây được một thẻ mà quên xoá khỏi danh sách cũng báo hỏng.
  - **Sửa một lỗi thật do `extraQuizIds` làm lộ ra:** `markQuizCompleted` trả về `termsUnlockedBy(id)` **không so với trạng thái TRƯỚC khi ghi**, nên một thuật ngữ mở được bằng 2 quiz sẽ bị báo "vừa giải mã" **lần thứ hai** và toast đọc lại tên nó. Đã lọc theo `before` + gom theo id thuật ngữ (`markQuizzesCompleted` còn có thể trả về **trùng trong cùng một lượt** đồng bộ). Đồng thời **5 chỗ đọc lẻ `requiredQuizId`** trong store được đổi hết sang `isTermUnlockedBy()` — đọc lẻ thì thuật ngữ mở bằng quiz phụ hiện "đã giải mã" ở màn này mà "chưa giải mã" ở màn kia; phép kiểm canh **store không còn chữ `requiredQuizId` nào**.
  - **4 tab chưa có nội dung thì NÓI THẬT:** `status:'soon'` trong `libraryTabs.ts` → nút `disabled` + `aria-disabled` + nhãn hổ phách `SYS.STANDBY`, y như 2 card HUD "sắp ra mắt" ở dashboard. **Chặn ở CẢ chỗ vẽ nút VÀ chỗ đổi tab** — chặn một chỗ thì truyền `initialTab='lab'` là vào được một tab trống. Không dùng `filter:grayscale()` (bài học đã ghi 3 lần); làm mờ bằng màu hổ phách + vạch chéo rất nhạt.
  - **Skeleton blur-up dựng bằng gradient CSS, 0 byte tải thêm** (không dùng ảnh mờ nhỏ — `~thumb` của NASA nặng đúng bằng `~small`, đã đo). Hai chi tiết phép kiểm canh: lưới skeleton phải khớp **từng breakpoint** với lưới thật (`grid-cols-2 · md:3 · xl:4` — lệch một mốc là lúc dữ liệu về cả lưới nhảy, đúng thứ skeleton sinh ra để tránh), và `aria-busy` khai **một lần ở cấp lưới** chứ không phải 8 thẻ cùng khai. `loading` **mặc định false** vì dữ liệu thuật ngữ nằm sẵn trong bundle — bật mặc định là dựng một nhịp loading giả.
  - ⚠️ **BA CHỖ ĐỀ BÀI GHI MỘT ĐẰNG, CODE GIỮ MỘT NẺO — cố ý, nêu để chốt lại:** (1) `category: 'astronomy'` → code giữ **`'space'`** (đã đi vào `CATEGORY_META`, `CATEGORY_ORDER`, 15 thuật ngữ và `check_codex.py`; nhãn hiện ra cho trẻ vẫn là "Không Gian"). (2) `status: 'locked' | 'decoded'` → code giữ **`'locked' | 'unlocked'`**, nhưng **badge hiện ra đúng chữ đề bài**: "🔓 Đã giải mã" / "🔒 Chưa giải mã". (3) `relatedQuizIds: string[]` → tách thành **`requiredQuizId`** (một quiz, để câu gợi ý "Hoàn thành Nhiệm vụ X" biết đọc tên bài nào) + **`extraQuizIds?`** (các đường phụ), gộp lại qua `quizIdsUnlocking()`. Đổi tên nội bộ là churn không có lợi ích nào cho người dùng; nếu muốn đổi thì nói.
  - ⚠️ **Lỗi "gán cứng con số" trong CHÍNH BỘ KIỂM — 4 chỗ, và đây là lần thứ hai loại này.** Thêm 5 thuật ngữ + 5 icon là `check_codex.py` báo hỏng ở "dùng 14 icon", "đọc được 14 thuật ngữ", "10 thuật ngữ space", "9 trang nguồn" — **không có gì sai, chỉ là phép kiểm hỏi sai câu**. Đã đổi sang hỏi *điều muốn biết*: "bộ phân tích có đọc **đủ số thuật ngữ khai trong file**" (so với `len(declared_ids)`, chính là phép kiểm chống "đọc ra 0 rồi đạt rỗng") và các mốc sàn `>=`. Cùng bài học `smoke_vault.py` gán cứng "20 mẫu vật" ở 8 chỗ.
  - ⚠️ **Lỗi "class vs id" lặp lại:** phép kiểm class `al-` hai chiều báo thiếu CSS cho `al-panel-` và `al-tab-` — hai thứ đó là **ID phần tử** ghép chuỗi (`id={`al-tab-${id}`}`, dùng cho `aria-controls`/focus), không bao giờ là class. Đúng bài học `em-win-title`/`ac-codex-title`. Đồng thời phát hiện `al-root` là **class chết** (không có rule, không có tác dụng) → xoá khỏi JSX thay vì bịa một rule cho nó.
  - **Chứng minh 48 phép kiểm mới CÓ RĂNG:** cố tình gây 5 lỗi rồi đo — đổi một tab `soon` thành `ready` (2 phép kiểm báo hỏng) · viết `unlockTermByQuiz` thành bản cài đặt thứ hai thay vì vỏ mỏng (hỏng) · lệch breakpoint lưới skeleton (hỏng, in ra cả hai chuỗi class để thấy lệch ở đâu) · nhét `new AudioContext()` vào thư viện (hỏng — đề bài cấm audio).
  - ⚠️ **Còn treo:** **4 tab kia chưa có nội dung** (Bản đồ vũ trụ 2D · Phòng thí nghiệm · Nhật ký hiện tượng · Thước đo quy mô) — đề bài liệt kê 5 tab ở phần *kiến trúc điều hướng* nhưng chỉ đặc tả Sổ tay, và phần Deliverables cũng chỉ nêu 3 component; làm 4 khu nửa vời còn tệ hơn nói thật là chưa có · **PWA/service worker chưa có** (dữ liệu thuật ngữ đã nằm sẵn trong bundle nên đọc offline được, nhưng chưa có manifest + SW để cài đặt) · chưa biên dịch/chạy thử (không có toolchain) · chưa có trang HTML nào mount `AstronomyLibrary`, và card MOD-06 ở `dashboard.html` vẫn `disabled` — bỏ khoá nó thì phải sửa cả `check_pages.py` mục [7b] (đang canh **đúng 2** card khoá) · nội dung 5 thuật ngữ mới **chưa qua rà soát chuyên môn** (số liệu có nguồn NASA, cách diễn đạt cho trẻ cần giáo viên đọc lại) — cùng ghi chú như `learningdata/`. — `../react/astronomy-library/*`, `../react/astronaut-codex/{termsData.ts,TermIcons.tsx,TermCard.tsx,useTermStore.ts,astronaut-codex.css,index.ts}`, `scratchpad/check_codex.py`
- **LUỒNG HẬU-NHIỆM-VỤ: màn tổng kết → tự về Trung Tâm Điều Hướng → Comet chỉ đường.** Hoàn tất phần còn thiếu của `../react/earth-mission-2d/`: thêm `CometGuidance.tsx` + `missionChains.ts` vào **điểm nhập** `index.ts`, viết **8 nhóm rule CSS còn khuyết**, và mở rộng `scratchpad/check_earth2d.py` (mục [11] mới, 8 → **8 file** trong bộ soi). Kết quả: **133/133 đạt** (gồm 9 URL ảnh NASA trả 200); hồi quy `check_pages` **219/219**, `check_codex` **90/90**.
  - ⚠️ **LỖI NẶNG NHẤT LÀ LỖI BỘ KIỂM KHÔNG THẤY: overlay Comet dùng 8 class KHÔNG CÓ MỘT RULE CSS NÀO** (`em-spotlight`, `em-target-pulse`, `em-dim`, `em-bubble` + 5 nhánh, `em-comet-bounce`, `em-guide-root`) **và `CometGuidance.tsx` cũng không `import` file css**. Tức là: ô sáng không làm tối trang, vòng nhấp nháy chỉ đường **không tồn tại**, mũi chỉ không có, linh vật không nhảy — overlay hiện ra thành một khối trong suốt không trỏ vào đâu. Bộ kiểm vẫn báo **81/83 đạt** vì `FILES` của nó **chưa có hai file đó**, nên phép kiểm class-CSS hai chiều mục [10] không hề soi tới. **Bài học: phép kiểm hai chiều chỉ đúng trong phạm vi danh sách file của nó — thêm file mới vào thư mục thì PHẢI thêm vào bộ soi, không thì mọi thứ trong file đó là điểm mù.**
  - ⚠️ **`em-fade-out` cũng thiếu**, mà `EarthMission2D.tsx` dùng nó cho cả nền và thẻ lúc rời màn → đường về tự động 5 giây "hoạt động" nhưng **cắt đột ngột không có hiệu ứng nào**. Đây là lỗi duy nhất bộ kiểm cũ bắt được (mục [10]).
  - **`prefers-reduced-motion`:** tắt `em-target-pulse` + `em-comet-bounce` (vô hạn), nhưng **giá trị bóng đổ `0 0 20px rgba(56,189,248,0.8)` nằm ở rule GỐC chứ không chỉ trong keyframes** — để trong keyframes thì lúc tắt animation vòng sáng mất hẳn và **không còn gì chỉ đường cho trẻ**.
  - **Bỏ ghép chuỗi `em-bubble-${side}` → bảng `BUBBLE_SIDE_CLASS` viết đủ 5 chuỗi.** Ghép chuỗi thì phép kiểm class chỉ thấy tiền tố `em-bubble-` nên nó vừa báo thiếu CSS cho một class không tồn tại, vừa báo 5 rule thật là "bỏ không" — và grep tên class trong repo không ra gì.
  - ⚠️ **BA CHỖ ĐỀ BÀI GHI MỘT ĐẰNG, CODE LÀM MỘT NẺO — CỐ Ý, xem lý do ở từng chỗ:** (1) đề ghi **"Trạm Điều Khiển"**, code dùng **"Trung Tâm Điều Hướng"** vì đó là tên chính thức đổi 29/07 và mọi trang khác đều có nút "Về Trung Tâm Điều Hướng" — đặt tên thứ ba cho một chỗ thì trẻ không nối được hai thứ (tên khai một chỗ duy nhất ở `AREA_NAMES`). (2) đề ghi **`+100 ☄️` viết cứng**, code hiện **số server trả về** — 100 chỉ là tiền thưởng hoàn thành (`DoneMeteors`), tổng thật của 8 bước là **255 tt + 385 XP**, viết cứng 100 vừa phạm phép kiểm [3b] vừa **báo thiếu cho trẻ 155 tt nó đã kiếm được**. (3) đề ghi **"🏅 Rookie Astronaut & 🌱 Eco-Warrior"**, code lấy tên qua `badgeLabelOf` từ `js/badges.js` — ở đó chúng là "🎖️ Phi Hành Gia Tập Sự" và "🌱 Chiến Binh Xanh"; gõ tay tên tiếng Anh là dựng tên thứ hai cho một huy hiệu, mà `js/badges.js` được ghi rõ là **chỗ duy nhất** giữ tên.
  - **Phép kiểm mục [8] ĐẢO CHIỀU:** trước đây nó đòi một nút Mặt Trăng `disabled`, giờ đòi **không còn dấu vết Mặt Trăng nào** trong màn tổng kết (đề bài yêu cầu bỏ hẳn). Server vẫn giữ `Unlocks: "moon"` — dữ liệu của server, không phải giao diện. Ghi chú đầu `EarthMission2D.tsx` đã sửa theo, vì nó vẫn đang tả cái nút không còn tồn tại.
  - ⚠️ **Lỗi "đếm cả chữ trong ghi chú của chính mình" — LẦN THỨ NĂM trong dự án.** Phép kiểm mới "không được gọi dashboard là *Trạm Điều Khiển*" báo hỏng ngay lần chạy đầu, thủ phạm là **đoạn ghi chú giải thích vì sao không dùng tên đó**. Sửa bằng cách quét trên `allcode` (đã bóc comment) chứ không phải văn bản thô.
  - **Chứng minh phép kiểm mới CÓ RĂNG, không "đạt rỗng":** cố tình gây 4 lỗi rồi đo — xoá rule `.em-spotlight` → 2 phép kiểm báo hỏng; bỏ dòng `import` css → hỏng; xoá một nhánh `em-bubble-right` → 2 phép kiểm báo hỏng; nhét lại một nút `🌙 MẶT TRĂNG` → hỏng. ⚠️ **Lần thử đầu của tôi cũng là một phép đo sai:** đổi `.em-spotlight` thành `.em-spotlightXX` thì regex `\.(em-[a-z0-9-]+)` **vẫn khớp** phần `em-spotlight` (chữ hoa không nằm trong lớp ký tự) nên bộ kiểm báo 124/0 và tôi tưởng nó mù — phải **xoá hẳn rule** mới là phép thử thật.
  - ⚠️ **Còn treo:** vẫn chưa biên dịch/chạy thử (không có toolchain — `tsc` báo lỗi `Cannot find module 'react'` là đúng trạng thái repo, xem mục 1) · `dashboard.html` **chưa mount** `CometGuidance` (trang vanilla; `attachChainBridge()` đã mở `window.AstroQChains` sẵn cho nó) · cờ chuỗi vẫn chỉ là **cache trong máy**, có phiên đăng nhập thì phải gọi `chainStore.syncFromServer()` bằng `missions.earth.steps` từ `GET /me/missions` · chưa có bộ smoke Playwright cho overlay (ô sáng trùng ô thẻ, box thoại không đè thẻ) như `smoke_onboard.py` đã làm cho tour. — `../react/earth-mission-2d/{CometGuidance.tsx,missionChains.ts,EarthMission2D.tsx,earth-mission-2d.css,index.ts}`, `scratchpad/check_earth2d.py`
- **Dựng lại NHIỆM VỤ 01 thành BẢN 2D ẢNH NASA, bỏ hẳn three.js** — `../react/earth-mission-2d/` (6 file, 2.359 dòng): `nasaPhotos.ts` · `PhotoStage.tsx` · `earthMission2dSteps.tsx` · `EarthMission2D.tsx` · `earth-mission-2d.css` · `index.ts`. Thay `mission-earth.html` + `js/earth3d.js` (51 KB) + three.js 0.160 nạp từ unpkg. Ảnh đường tải đầu **68 KB**, 0 canvas, 0 WebGL, 0 importmap (phép kiểm mục [2] canh 9 dấu vết 3D).
  - ⚠️ **ĐỀ BÀI 6 BƯỚC NHƯNG SERVER ĐÒI 8, VÀ TÔI GIỮ 8.** `Missions.All` khai đúng thứ tự `scan · timeline · sun · energy · rotation · life · eco · core`. Bỏ `sun` + `rotation` thì: mất 40 tt + 60 XP · hai bài đọc `sun`/`rotation` trong `earth_codex.json` thành mồ côi và phép kiểm [3c] báo hỏng · **nặng nhất: nhiệm vụ không bao giờ đủ bước để hoàn thành**, nên `DoneMeteors:100`, `DoneXp:120` và `Unlocks:"moon"` không bao giờ nổ ra — tức là chính màn tổng kết và việc mở khoá Mặt Trăng mà đề bài yêu cầu lại không tới được. Nên 6 màn trong đề được dựng đúng như yêu cầu, cộng 2 màn 2D nhẹ cho `sun`/`rotation`. Muốn đúng 6 thì phải sửa server trước; đường lùi gói trong `SERVER_ONLY_STEPS`.
  - **`PhotoStage.tsx` là MỘT khung ảnh cho cả 8 bước** (kéo/phóng/điểm nóng/lớp phủ), không mỗi bước một bản. Ba chi tiết bắt buộc: **điểm nóng phải nằm TRONG lớp bị biến hình** (đặt ngoài thì kéo xong trẻ chạm "Amazon" nhưng dưới ngón tay đã là đại dương) · **nhưng dấu hiệu phải chống-phóng `scale(1/zoom)`** (không thì phóng 3× là vòng ngắm trùm kín nửa hành tinh) · **kéo xong không được tính là một cú bấm** (ngưỡng 6px, không thì bước tự hoàn thành khi trẻ chỉ đang kéo ảnh).
  - ⚠️ **PHÁT HIỆN QUYẾT ĐỊNH: ảnh Blue Marble là QUẢ CẦU tâm Bắc Mỹ, không phải bản đồ phẳng.** Tôi tải về xem chứ không đoán theo tỉ lệ — khung 640×640, thấy Bắc Mỹ/Caribe/rìa Greenland, còn **Amazon, Himalaya, Nam Cực, Great Barrier Reef đều ở nửa bên kia**. Đặt 4 điểm mẫu vật lên đó là **dạy sai địa lý**, đúng lỗi đã ghi ở bản 3D. Nên bước `life` — bước DUY NHẤT khẳng định vị trí — dùng bản đồ phẳng `world.topo.bathy.200401` + `latLonToPct()` (một phép chia, đúng tuyệt đối), các bước khác dùng ảnh quả cầu 68 KB. Phép kiểm canh `latLonToPct` không lọt sang bước khác.
  - ⚠️ **KHÔNG TỒN TẠI ẢNH CHỤP TRÁI ĐẤT 4,5 TỶ / 3,8 TỶ / 66 TRIỆU NĂM TRƯỚC**, và NASA Image Library tôi đã tra thì không có tranh dựng nào cho Trái Đất sơ khai. Nên 3 mốc đầu của thanh thời gian là **ảnh NASA thật đổi tông bằng CSS filter, có nhãn "MINH HOẠ" trên khung** + một dòng nói rõ; chỉ mốc "ngày nay" là ảnh chụp. Dán nhãn "ảnh NASA" cho một bức minh hoạ tiền sử là dạy trẻ một điều sai.
  - **Ba đo đạc làm đổi thiết kế:** (1) **biến thể ảnh không suy ra được theo mẫu URL** — `PIA03401` không có `~large` (403), `200910220008HQ` không có cả `~medium`/`~large`/`~orig`; nhồi `~medium` vào `srcSet` cho mọi ảnh là ảnh Nam Cực chết ở đúng cỡ màn hay dùng nhất, nên `src2x` là **tuỳ chọn, chỉ điền khi đã đo**. (2) **`~thumb` KHÔNG phải ảnh nhỏ** — đo cả 5 ảnh thì `~thumb` và `~small` trả về **đúng cùng số byte**, NASA phục vụ hai tên cho một file; tin cái tên là tải 2 lần cùng một ảnh, nên placeholder là gradient CSS 0 byte. (3) chỉ còn **một** biến thể bản đồ phẳng sống (5400×2700, **2,57 MB**) — các URL nhỏ hơn đều 404, nên nó nạp trễ và có kèm lệnh hạ cỡ một lần thành asset nội bộ (đúng lối `Khoangluna.png` 6,9 MB → 99 KB).
  - **Dùng lại, không chép:** `ERAS`, `ENERGY_SOURCES` và **cả component `EcoHeroSorting`** lấy nguyên từ `../EarthMissionSteps.tsx` — bước Eco-Hero vốn đã là 2D kéo-thả thuần nên refactor bỏ 3D không đụng tới nó. Kéo-thả dùng **Pointer Events** (HTML5 DnD không chạy trên đa số trình duyệt di động, mà đây là app cho trẻ dùng máy tính bảng) và giữ đúng hợp đồng `data-want`/`data-zone`.
  - **Sửa được một lỗi thiết kế của bản 3D:** bước `rotation` từng "trẻ không thể hoàn thành" vì kéo là quay CAMERA nên góc trạm–vệ tinh không đổi. Ở 2D thứ trẻ kéo và thứ được chấm điểm là **cùng một con số** (`pan` của khung ảnh), không còn cửa cho lỗi đó.
  - **Kiểm thử `scratchpad/check_earth2d.py`: 83/83 đạt**, gồm **9 URL ảnh NASA kiểm 200 trực tiếp** và phép kiểm quan trọng nhất: `STEP_IDS` khớp `Missions.cs` **đúng thứ tự**. Hồi quy `check_pages` 219/219 · `check_codex` 90/90.
  - ⚠️ **Ba lỗi trong bộ kiểm, cùng MỘT gốc — phép kiểm "không được chứa X" đem đi khớp VĂN BẢN THƯỜNG:** `\bthree\b` khớp chữ "three" trong **câu tiếng Anh của giao diện** ("These three together…") · phép quét class chỉ đọc `className=` nên báo oan 5 rule "bỏ không" vốn được dùng qua `imageClassName=` và qua một mảng dữ liệu, **nhưng cũng không được quét bừa cả file** vì `em-win-title` là ID chứ không phải class · một điều kiện gõ chuỗi không dấu trong khi file viết có dấu. **Đây là lần thứ ba cùng loại lỗi này (xem mục Astronaut Codex): phép kiểm phải khớp HÌNH DẠNG CỦA CODE, đừng khớp chữ.**
  - ⚠️ **Còn treo:** chưa biên dịch/chạy thử (không có toolchain) · bản đồ phẳng 2,57 MB nên hạ cỡ trước khi phát hành · nếu thay `mission-earth.html` bằng bản này thì phải chuyển `AstroQProgress.missionStep` + `js/badges.js` vào prop `onReportStep`/`badgeLabelOf` và chạy lại `smoke_mission_earth.py` (bộ đó lái bằng `window.__mission`, bản React chưa mở bề mặt đó). — `../react/earth-mission-2d/*`, `scratchpad/check_earth2d.py`
- **⚠️ ĐÃ XOÁ 30/07/2026, port sang `codex.html` + `js/codex-terms.js`** — Dựng "Astronaut Codex" bằng React/TS/Tailwind ở `../react/astronaut-codex/`.** 9 file: `termsData.ts` · `useTermStore.ts` · `TermIcons.tsx` · `TermIllustration.tsx` · `TermCard.tsx` · `TermCodexModal.tsx` · `AstronautCodex.tsx` · `astronaut-codex.css` · `index.ts` (+ `AstronautCodex.astro` là MẪU). 14 thuật ngữ: 10 nhóm `space` dùng lại đúng bộ nguồn NASA của `js/quiz-questions.js`, 4 nhóm `ai`/`quantum` là **bản nháp** `reviewed:false` không có nguồn.
  - ⚠️ **KHÔNG chạy được trong repo này và cố ý không cố nhồi vào.** `AstroQhtml/` là web tĩnh, không build step (mục 1); cả máy không có Node, cả repo không có `package.json`/`tsconfig.json`. Bộ này là **bản chuyển giao chưa biên dịch, chưa chạy thử**, cùng dạng với `../react/SpecimenVault.tsx` và `EarthMissionSteps.tsx` có từ trước.
  - ⚠️ **DÂY NỐI "làm quiz xong tự giải mã" CÒN THIẾU MỘT MẢNH Ở SERVER.** `AstroQProgress.quiz()` chỉ gửi `{correct, total, meteors}` — server **không biết trẻ trả lời đúng thuật ngữ nào**, nên chưa có tín hiệu thật để mở khoá. Cần thêm `terms: string[]` vào payload `POST /me/progress` rồi map qua `QUIZ_BANK_TERM_TO_CODEX_ID`; chỗ nối phía client là `showSummary()` trong `quiz.html`. Trước khi có mảnh đó, sổ tay chạy đúng nhưng chỉ dựa vào cache trong máy.
  - **Quyết định kiến trúc: store ở tầm module + `useSyncExternalStore`, KHÔNG dùng Context.** Astro dựng mỗi island thành một React root riêng nên Context không đi xuyên ranh giới island — lưới thẻ ở island A và huy hiệu tiến độ ở island B sẽ không thấy nhau. Kèm `getServerSnapshot` (thiếu là vỡ hydration khi Astro SSR) và `ensureHydrated()` ở đầu **mọi hàm ghi** (gọi `markQuizCompleted` trước khi có component nào mount thì snapshot đang rỗng và cú ghi sẽ **xoá sạch** tiến độ cũ trong cache). ⚠️ Store là singleton nên **seed từ server chỉ chạy ở trình duyệt** — seed ở Node của SSR là tiến độ học sinh A rò sang trang học sinh B; đây là lý do `.astro` mẫu dùng `client:only="react"`.
  - **Mạng yếu thì KHÔNG TẢI ẢNH, chứ không phải tải rồi thay thế.** `navigator.connection.saveData` / `slow-2g` / `2g` → vẽ thẳng sơ đồ kỹ thuật SVG. Tải rồi bỏ thì đã tốn dung lượng của đứa trẻ đang dùng 3G. Đường ảnh đầy đủ vẫn có (`<picture>` AVIF→WebP, `loading="lazy"`, `decoding="async"`, placeholder mờ dựng bằng SVG inline nên **0 byte thêm**, lỗi ảnh → về sơ đồ), nhưng **mọi thuật ngữ đang để `illustration: undefined`** vì `img/` chưa có asset nào — trỏ vào file không tồn tại là 404 + ô ảnh vỡ trước mặt trẻ.
  - **Hai lỗi dẫn nguồn tự tìm ra, và cả hai chỉ lộ ra vì phép kiểm đếm số URL:** `term_planet` dẫn trang *About the Planets* cho **ba tiêu chí IAU** — trang đó **không** liệt kê ba tiêu chí (WebFetch trả về "[Unverified]"), chúng nằm ở trang *Dwarf Planets*; `term_moon` dẫn trang *Moons* cho câu "Ganymede lớn hơn Sao Thuỷ" vốn đọc ở trang *Ganymede*. Sửa bằng cách đổi `source` thành **`sources` (mảng)** — một đoạn định nghĩa hoàn toàn có thể dẫn từ hai trang, ghi một nguồn là dẫn nguồn cho một câu mình không đọc ở đó.
  - **Kiểm thử `scratchpad/check_codex.py`: 90/90 đạt.** Nó làm thay việc `tsc` không chạy được: đối chiếu `TermIconKey` ↔ `TERM_ICONS` (thay cho `satisfies`), cấu trúc 14 thuật ngữ, class `ac-` ↔ CSS **hai chiều**, và phép kiểm mà TypeScript không làm được — **`quizBankTerms` phải trỏ vào `term` CÓ THẬT trong `js/quiz-questions.js`** (20 khoá thiên văn, không khoá nào bị hai thuật ngữ cùng nhận, không nhận bừa 5 câu lập trình).
  - ⚠️ **Bốn lỗi trong CHÍNH BỘ KIỂM, đáng ghi vì đều là loại "báo oan / đạt rỗng":** (1) ba phép kiểm dạng *"không được dùng X"* đếm cả chữ trong **ghi chú của tôi giải thích vì sao không dùng X** → `dangerouslySetInnerHTML`, `grayscale`, `auto-fill minmax` đều bị báo vi phạm dù code làm đúng; phải bóc comment trước khi tìm. (2) regex tách khối thuật ngữ sai mức thụt lề → đọc ra **0 thuật ngữ**, và 8 phép kiểm sau đó **đạt hết một cách rỗng** vì chúng chạy trên danh sách rỗng — đã thêm `sys.exit(1)` chặn ngay. (3) phép kiểm class `ac-` quét cả file nên gom luôn **id HTML** (`ac-codex-title`, `ac-lqip-blur`) rồi báo thiếu CSS oan. (4) phép đếm `count("{") == count("}")` báo `termsData.ts` lệch 36/34 trong khi file **hoàn toàn cân** — đã thay bằng bộ quét từng ký tự có hiểu chuỗi/comment và kiểm **đúng cặp**. **Một phép kiểm hay báo oan thì người ta sẽ bỏ qua nó, tức là mất luôn.**
  - ⚠️ **Còn treo:** 4 thuật ngữ AI/Lượng tử cần bổ sung nguồn + giáo viên rà trước khi phát hành · chưa có ảnh minh hoạ nào · chưa chạy thử trên trình duyệt (không có toolchain) · chưa có bản EN (các bản React trong `react/` đều VI-only như `SpecimenVault.tsx`). — `../react/astronaut-codex/*`, `scratchpad/check_codex.py`
- **Bổ sung câu hỏi trắc nghiệm cho 10 thuật ngữ thiên văn nền, lấy số liệu từ nguồn uy tín.** Mười thuật ngữ: Star · Planet · Dwarf Planet · Moon/Natural Satellite · Asteroid · Comet · Meteoroid · Meteor · Meteorite · Exoplanet — **mỗi thuật ngữ 2 câu, tổng 20 câu mới**, bank lên **25 câu**. File mới `js/quiz-questions.js` (mô tả đầy đủ ở mục 2) + dòng nguồn ở popup giải thích.
  - **Nguồn: 9 trang `science.nasa.gov`, ĐỌC TRƯỚC KHI VIẾT chứ không viết theo ký ức**, mỗi câu gắn `src` trỏ về đúng trang đã đối chiếu và cả 9 URL đã kiểm **trả 200**. Những chỗ số liệu đều dẫn nguyên văn NASA: 3 tiêu chí hành tinh của IAU (2006) và **5 hành tinh lùn** Ceres/Pluto/Haumea/Makemake/Eris · **8 hành tinh** · Ceres là hành tinh lùn **duy nhất ở vùng trong** hệ Mặt Trời · Ganymede **lớn hơn cả Sao Thuỷ** · vành đai chính **1,1–1,9 triệu** tiểu hành tinh >1 km, "mảnh đá còn sót" từ **4,6 tỉ năm** trước · sao chổi "quả cầu tuyết bẩn", đuôi bị thổi **ra xa Mặt Trời** · sao băng sáng hơn Sao Kim = **fireball** · **dưới 5%** khối lượng tới được mặt đất, **48,5 tấn/ngày** · **hơn 6.000** ngoại hành tinh đã xác nhận, phương pháp **quá cảnh (transit)**.
  - ⚠️ **Ba con số tôi CỐ Ý không đưa vào câu hỏi vì không xác minh được từ trang NASA đang dẫn:** "Sao Kim/Sao Thuỷ không có vệ tinh nào" (trang *About the Planets* không nói), "Sao Diêm Vương có 5 vệ tinh", và tốc độ sao băng quy sang km/h (NASA ghi "tens of thousands of **miles** per hour" — quy đổi rồi ghi km/h là tự tạo ra một con số không có nguồn). Ba chỗ đó viết định tính. **Đừng "làm giàu" câu giải thích bằng số liệu nhớ được mà không mở nguồn.**
  - **Quyết định thiết kế: KHÔNG hỏi hết bank mỗi lượt.** Nhồi 25 câu vào một lượt thì nhiệm vụ dài gấp 5 và bình năng lượng/bảng tổng kết mất nhịp; nên `pickRound(5)` rút đề mỗi lượt và **ưu tiên không trùng thuật ngữ**. Đổi lại "Làm lại" giờ ra đề khác — đo được **8/8 lượt đổi đề**. Cân nhắc đã bỏ: bốc ngẫu nhiên đơn thuần (một lượt hỏi Sao chổi hai lần, bỏ qua 9 thuật ngữ khác).
  - **Popup giải thích thêm dòng nguồn** (`.sheet-src` trong `css/quiz.css`, khoá i18n `src_label` ở cả `vi` và `en`): mono 11px, `overflow-wrap:anywhere`, mở tab mới với `rel="noopener"`. Câu không có `src` thì **ẩn hẳn dòng đó** chứ không hiện nhãn trống. Đo trên màn 390px: dòng nguồn rộng 316px trong thẻ 362px, không tràn.
  - **Kiểm thử `scratchpad/check_quiz_bank.py`: 54/54 đạt, 0 lỗi console.** Chạy trên Chromium thật (dữ liệu là JS, đọc chuỗi trong file không chứng minh được cấu trúc): mọi câu đủ `term/topic/q/opts[4]/a/ok/no/hint` ở **cả vi và en**, không câu nào có 2 lựa chọn trùng chữ, 10 thuật ngữ đủ 2 câu, **400 lượt `pickRound` không lượt nào trùng thuật ngữ hay trùng câu** và cả 25 câu đều từng được rút, `pickRound` **không trộn tại chỗ** mảng `ALL`, 9 URL nguồn **trả 200**; rồi chơi thật: badge chủ đề và 4 lựa chọn trên màn **khớp đúng thứ tự** dữ liệu, dòng nguồn hiện đúng `href` với câu có nguồn và **ẩn** với câu không có, bản EN dịch cả câu hỏi/lựa chọn/nhãn "Source:", điện thoại 390×844 không tràn ngang.
  - ⚠️ **Hai phép kiểm của tôi lúc đầu ĐẠT MÀ VÔ NGHĨA, đã sửa:** (1) phép đo "dòng nguồn không tràn trên điện thoại" rơi vào một câu **không có nguồn** nên dòng đó đang `hide` → mọi số đo bằng 0 và điều kiện đúng một cách rỗng; giờ script **đi tìm cho được một câu CÓ nguồn** rồi mới đo. (2) phép kiểm "nhiều lượt ra nhiều thuật ngữ khác nhau" đọc biến của một vòng lặp **thoát sớm** ngay khi gặp đủ hai loại câu, nên nó chỉ thấy 5 thuật ngữ của một lượt duy nhất; giờ tách thành phép đo riêng, tải lại trang 8 lần và xem câu mở đầu có đổi không (**6/8 khác nhau**). Đúng bài học đã ghi ngày 29/07: *đo bằng cách không phản ánh việc thật thì phép kiểm không chứng minh gì*.
  - **Hồi quy: 0 hỏng.** `check_pages` **219/219** · `smoke_profile_pages` **125/125** (trong đó phép kiểm "Quiz không gửi xp/level/badges" vẫn đạt với đề rút ngẫu nhiên) · `regress_auth_pages` **19/19** — 0 lỗi console ở mọi bộ.
  - ⚠️ **Còn treo:** nội dung 20 câu **chưa qua rà soát chuyên môn** (số liệu có nguồn NASA, nhưng cách diễn đạt cho trẻ cần giáo viên đọc lại) — cùng ghi chú như `learningdata/` và `js/specimens.js`. `game-defender.html` vẫn giữ mảng `QUIZ` 8 câu **riêng** của nó; gộp về bank chung là việc nên làm nhưng sẽ đụng bộ smoke test của game đó, nên để thành một việc riêng. — `js/quiz-questions.js`, `quiz.html`, `css/quiz.css`, `scratchpad/check_quiz_bank.py`

### 2026-07-29
- **ĐỔI TÊN 4 KHU VỰC + THÊM 3 KHU VỰC MỚI trên dashboard, và dựng `missions.html`.** Bảng tên đầy đủ ở mục 2 (khối `dashboard.html`). Đổi: *Khoang Lái → **Trung Tâm Điều Hướng** (Navigation Hub)* · *Trạm Tri Thức → **Tri Thức*** · *EN Training Bay → **Training Simulator***. Thêm: **Trung Tâm Nhiệm Vụ** (Mission Control, MOD-04 → `missions.html` MỚI) · **Phòng Nghiên Cứu** (Research Lab, MOD-05, sắp ra mắt) · **Thư Viện Thiên Văn** (Star Archive, MOD-06, sắp ra mắt).
  - **Quyết định quan trọng nhất: KHÔNG đổi những chỗ "khoang lái" nghĩa là BUỒNG LÁI CỦA CON TÀU.** Đổi mù cả 61 chỗ khớp chuỗi thì `specimen-vault.html` thành "Mẫu vật trưng ở Trung Tâm Điều Hướng" (vô nghĩa — đó là cái bàn trong buồng lái) và lời SEO ở `index.html` bị sửa kèm, mà trang đó **đã go-live** với JSON-LD phải khớp 1-1 phần FAQ hiện ra. Nên chỉ đổi đúng những chuỗi dùng nó làm **TÊN TRANG** (`Về Khoang Lái`, `Back to Cockpit`, `home_btn:"Khoang Lái"`) — 10 file. Phép kiểm mục [7] cũng hỏi đúng câu đó, không cấm chữ "khoang lái" nói chung.
  - **`wiki/` cố ý chưa sửa** (1 chỗ nhắc "Trạm Tri Thức"): đó là bộ trang SEO **sinh ra** từ `scratchpad/gen_wiki_data*.py`, sửa tay là lần sinh sau mất — phải đổi generator rồi sinh lại + cập nhật `sitemap.xml`, để thành một việc riêng.
  - **Tiếng Việt: trong câu văn dùng "khu Tri Thức", không dùng "Tri Thức" trơn.** Bỏ chữ "Trạm" mà không thêm danh từ chỉ loại thì ra "Hãy ghé Tri Thức làm Quiz" — sai ngữ pháp. Vẫn là một tên gọi, chỉ thêm từ chỉ loại; nhờ vậy không sinh ra hai tên cho một chỗ.
  - **Số hiệu `MOD-nn` giữ nguyên cho 3 card cũ**, card mới lấy số kế tiếp (MOD-04/05/06); **thứ tự hiển thị khác số hiệu** — Trung Tâm Nhiệm Vụ lên đầu, 2 card "sắp ra mắt" xuống cuối. Đúng bài học đã rút ra với `code:"ARCADE-nn"` ở `games.html`.
  - **Hai khu chưa có trang thì nói thật, không dẫn đi đâu**: nút `disabled`, đèn + nhãn `.standby` hổ phách (`SYS.STANDBY`), đèn **ngừng nhấp nháy**, bỏ `hudPulse` và bỏ cả hiệu ứng nhấc-lên khi hover. Không dùng `filter:grayscale()` — trên nền gradient sáng nó ra khối xám SÁNG HƠN card dùng được.
  - **Màn Comet dẫn tham quan GIỮ 7 BƯỚC** dù dashboard lên 6 card: hai khu "sắp ra mắt" thì dẫn tới rồi nói "chưa mở" là vô nghĩa, còn thêm bước cho Mission Control thì phải **đo lại chỗ đặt box thoại** (thẻ HUD ~375px + box ~245px đã vượt màn 900px, phải nhờ nhánh "đặt sang BÊN"). Lời chào của Comet nay gọi đúng tên khu — mọi trang khác đều có nút "Về Trung Tâm Điều Hướng" trỏ về đây, Comet gọi tên khác thì trẻ không nối được hai thứ.
  - **`missions.html` đọc tiến độ THẬT** qua `AstroQProgress.missions()` (`GET /me/missions`, wrapper mới ở `js/progress.js`). Chưa đăng nhập / mất mạng → **hiện dấu `—`, không hiện 0**: "0/8 bước" là lời khẳng định SAI về tiến độ người chơi. Nhãn nút cũng theo tiến độ thật ("Bắt đầu" khi chưa biết gì, mới đổi sang "Tiếp tục"/"Chơi lại" khi đọc được). Số bước lấy từ server, không gán cứng.
  - **Kiểm thử:** `check_pages.py` **219/219 đạt** (thêm mục [7] đổi tên hai chiều · [7b] 6 card + 2 card khoá · [7c] `missions.html` khớp `Missions.cs` + không bịa tiến độ; `missions.html` vào cả bộ kiểm mục [1]). `scratchpad/smoke_missions.py` MỚI: **70/70 đạt** — đo trên Chromium rằng "Về Trung Tâm Điều Hướng" (dài hơn tên cũ 10 ký tự) **không cắt chữ và không tràn header** ở 390px trên cả 7 trang, 6 card xếp 1 cột đều nhau, 2 nút "sắp ra mắt" thật sự bấm không được, và 3 ô tổng quan hiện `—` khi chưa đăng nhập.
  - ⚠️ **Hai bộ test cũ báo hỏng vì GÁN CỨNG con số, không phải vì trang sai:** `smoke_vault.py` gán cứng "20 mẫu vật" ở 8 chỗ (nay suy ra `N = len(CAT)` từ chính `Specimens.cs`), `smoke_onboard.py` đòi đúng 3 card HUD (ý nó là "dashboard còn nguyên sau màn loading" → đổi sang `>= 3`, số card đã có phép kiểm riêng ở [7b]). **Bài học: phép kiểm nên hỏi ĐIỀU MÌNH MUỐN BIẾT, đừng gán cứng con số mà nơi khác đã là nguồn sự thật.**
  - ⚠️ **Nhãn của `check()` trong `scratchpad/*.py` PHẢI KHÔNG DẤU.** Console Windows mặc định cp1252; in một chữ có dấu là `UnicodeEncodeError` **ném giữa lúc chạy** và bỏ dở script — mất luôn mọi phép kiểm phía sau, mà nhìn output thì tưởng "chạy xong". Chữ có dấu chỉ được nằm trong ĐIỀU KIỆN. (Chạy bộ smoke thì đặt `PYTHONIOENCODING=utf-8`.)
- **Dựng NHIỆM VỤ 01 "HÀNH TINH XANH" — trang nhiệm vụ riêng, 5 bước, chuyển cảnh bằng camera Three.js.** File mới: `mission-earth.html`, `css/mission-earth.css`, `js/earth3d.js`, `js/sfx.js`. Mô tả đầy đủ ở mục 2. Backend `Services/Missions.cs` + `POST /me/missions/step` + huy hiệu `rookie-astronaut` (đã deploy, **297/297 đạt trên AWS**).
  - **Quyết định lớn nhất: thưởng do SERVER quyết HOÀN TOÀN, khác hẳn game.** Điểm mini-game là con số client tự khai (server chỉ kẹp trần) vì màn chơi chạy ở máy người dùng. Nhiệm vụ thì không: mỗi bước có id cố định, nên client chỉ gửi `{mission, step}` và server tra bảng luật. Ghi có điều kiện `attribute_not_exists(#m.#q.#s)` nên **mỗi bước chỉ được tính một lần**, `opId` chống cộng hai lần khi gửi lại từ hàng chờ. Trong `mission-earth.html` không có một con số thưởng nào — phép kiểm "mất mạng thì hiện +0 chứ không bịa" canh chuyện đó.
  - **Kiểm thử `scratchpad/smoke_mission_earth.py`: 91/91 đạt, 0 lỗi console** — chơi THẬT hết 5 bước ở desktop/EN/điện thoại 390×844/`reduced-motion`/mất mạng. Đo trên trang chứ không đọc code: bấm **thật** vào toạ độ marker (`world.screenOf`) để raycast và vùng chạm được kiểm; đo pixel bằng `readPixels` **trong chính khung vẽ** (hiệu ứng sống 0,9–1,7s nên `screenshot()` ~200ms quá chậm); chứng minh KHÔNG tải lại trang bằng một biến gieo trong `window` còn nguyên sau cả 5 bước; ranh giới ngày/đêm đo bằng **profile sáng 32 cột** ngang qua đĩa hành tinh (cột sáng nhất 104 vs tối nhất 6, bước nhảy 85) chứ không đo 2 điểm.
  - ⚠️ **Lỗi nghiêm trọng nhất, chỉ lộ ra khi ĐỔI CÁCH ĐO: bước 3 trước đây TRẺ KHÔNG THỂ HOÀN THÀNH.** Yêu cầu là "kéo để xoay Trái Đất cho trạm phát sóng hướng về vệ tinh", nhưng OrbitControls kéo là quay **camera**, `earth.quaternion` không đổi → góc giữa trạm và vệ tinh y nguyên. Bước đó chỉ tự xong vì hành tinh **tự quay** (tức là trẻ ngồi chờ, không phải giải), và ở `prefers-reduced-motion` (không tự quay) thì **treo vĩnh viễn**. Bản test đầu gọi `world.setSpin()` rồi đợi — và "đạt". Đổi sang **kéo thật** (thứ duy nhất trẻ làm được) là lỗi hiện ra ngay. Sửa: thêm `world.setEarthDrag(true)` — ở bước 3 cú kéo xoay CHÍNH hành tinh, `controls.enableRotate` tắt. **Bài học: đo bằng cách người dùng không làm được thì phép kiểm không chứng minh gì.**
  - ⚠️ **Lỗi thật thứ hai: chạm vào Mặt Trời không kiểm che khuất.** `ray.intersectObject(sunCore)` được kiểm TRƯỚC rồi `return` ngay, mà `intersectObject` không biết gì về vật chắn → Mặt Trời nằm sau lưng Trái Đất thì **chạm vào chính hành tinh cũng tính là "đã tìm ra ngôi sao"**, nhiệm vụ 2 tự thắng mà trẻ chưa hề xoay camera.
  - ⚠️ **Lỗi thật thứ ba: điểm tín hiệu ở gần MÉP hành tinh hiện ra nhưng chạm không ăn.** Marker đặt ở `R·1,012` tức nhô lên trên bề mặt; camera cách 2,5·R thì đường bao quả cầu chỉ rộng `0,92·R`, nên marker gần mép **chiếu ra ngoài** đường bao và tia chiếu trượt khỏi quả cầu. Trẻ thấy đốm đang nhấp nháy, chạm đúng vào nó, không có gì xảy ra. Sửa: thêm đường bắt theo **khoảng cách trên màn hình** (ngưỡng 22px hoặc 5,5% cạnh ngắn) khi raycast trượt.
  - ⚠️ **Bản đồ Trái Đất phải là bản đồ THẬT.** Bản đầu sinh lục địa bằng nhiễu fBm, đo được **đất chiếm ~70% mặt cầu** (thực tế nước mới 70%) trong khi thẻ mẫu vật bước 4 ghi "Nước bao phủ khoảng 70% Trái Đất" — trẻ nhìn quả cầu là thấy ngược; và 4 thẻ 🌊 Đại Tây Dương / 🌳 Rừng Amazon / 🐧 Nam Cực / 🏔️ Himalaya neo vào lat/lon THẬT nên rơi vào giữa đại dương ngẫu nhiên. Thay bằng `COASTS` — đường bờ thô của 14 khối lục địa/đảo, vẽ bằng code, **không thêm file ảnh**. Nhiễu chỉ còn để tô khí hậu.
  - **Chín lỗi hình ảnh, tất cả chỉ SOI ẢNH CHỤP mới thấy** (26 ảnh × desktop/điện thoại): (1) lục địa kéo thành **vệt dọc** vì nhiễu lấy theo `(cos lon, sin lon + lat)` — vĩ độ cộng vào cùng trục với `sin(lon)`; (2) tỉ lệ đất/nước ngược (nói trên); (3) bờ biển **răng cưa bậc thang** ở mức phóng to → khổ texture 1024→2048 + trộn mềm ở mép; (4) **vòng sóng vệ tinh thành dải cyan đặc chiếm 1/4 màn hình** — vành dày 23–43% bán kính nhân 9 lần (đúng bài học vệt lửa ARCADE-01), sửa thành nét mảnh 5% và chỉ phóng 4,5 lần; (5) vệ tinh vẽ ra **một thanh xám ~14px**, đọc không ra hình gì → to 1,8× + tấm pin sáng hơn, và **ẩn ngoài bước 3** vì nó lấp ló nửa cái ở mép màn hình khi camera pan, nhìn như giao diện bị lỗi; (6) **màng khí quyển thành cái vòng cyan ĐẶC lơ lửng cách hành tinh** (vẽ ở `1,22·R`) — đúng lỗi "cung nét dày trông như vòng rời" đã ghi ở `warp-screen`; (7) **tia laser của drone gần như không thấy**: chiều dài tính bằng `position.length() - R` mà drone treo ở `1,06·R` nên tia chỉ dài 0,06 đơn vị; treo lên `1,34·R` thì thấy, nhưng **chiều quay của hình nón sai** (−90° cho tia chiếu ngược lên trời, phải là +90° — dấu này không suy được từ mã, phải nhìn ảnh); (8) **drone là một bóng đen** trên nửa tối — đặt `PointLight` vào tâm drone thì vô ích vì đèn nằm BÊN TRONG quả cầu, phải dùng `emissive`; (9) **hai mascot ở màn tổng kết chỉ 39×58px**, đọc thành hai emoji lạ chứ không ra "Comet và Byte nhảy lên vui mừng" — hộp vuông 58×58 + `object-fit:contain` trên ảnh dọc 2:3. Thêm: **đèn thành phố thành đốm dung nham** (bán kính 3–10px ở khổ 2048, phóng to thành ~30px) → 1,4–3,4px và tăng số lượng; **dải hoang mạc liền mạch vắt ngang hành tinh** vì nhiễu quá nhẹ so với dải vĩ độ; **nút "MẶT TRĂNG · SẮP RA MẮT" đang vô hiệu lại hút mắt hơn nút bấm được** (`grayscale` trên gradient vàng vẫn ra khối be sáng).
  - ⚠️ **3 điểm quét của bước hướng dẫn đặt TƯƠNG ĐỐI với chỗ camera đang nhìn** (`world.facingLatLon()`), không phải lat/lon cố định: đo được ở góc mở màn trẻ chỉ thấy **1 trong 3** đốm. Đặt cứng kiểu nào cũng không chắc vì hành tinh tự quay. Bước 1 cũng cho hành tinh **đứng yên** — bước dạy "kéo để xoay" thì chuyển động duy nhất trên màn hình nên là chuyển động do trẻ tạo ra.
  - ⚠️ **Sửa một lỗi ở `js/mission-intro.js`**: `markSeen()` bắn PUT `intro01Seen` rồi 500ms sau `location.href` sang trang nhiệm vụ — **điều hướng huỷ request đang bay**, cờ không tới DynamoDB, lần sau đăng nhập ở máy khác là xem lại cả màn phim. Cache trong máy che mất lỗi này nên chỉ thấy khi đổi máy. Giờ `markSeen()` trả Promise và `activate()` chờ nó (hạn 1,4s) mới gọi `onActivate`.
  - ⚠️ **Bốn bài học về CÁCH ĐO, không phải về sản phẩm:** (a) `AstroQ.getLang()` lùi về `navigator.language`, mà Chromium của Playwright mặc định `en-US` → **cả phần "tiếng Việt" của bộ test lặng lẽ chạy bằng tiếng Anh** và mọi phép kiểm chữ Việt vô nghĩa; phải ghim `astroq-lang`. (b) Bản giả `AstroQProgress` gán thẳng `window.AstroQProgress` thì bị `js/progress.js` ghi đè → phải `Object.defineProperty` có setter nuốt lời gán (đúng cách đã dùng cho `AstroQAuth`). (c) `add_init_script` **gieo lại sau MỖI lần điều hướng**, nên nó xoá đúng cái cờ vừa ghi khi trang chuyển sang `mission-earth.html` — lần thứ TƯ gặp cùng bẫy này. (d) Đo ranh giới ngày/đêm bằng cách chiếu "điểm ngay dưới Mặt Trời" ra màn hình là sai: muốn thấy Mặt Trời thì camera đang nhìn về phía nó, nên nửa được chiếu sáng nằm ở PHÍA SAU hành tinh → ra "ngày tối hơn đêm".
  - **Âm thanh có phép kiểm riêng** (mục [13]): Chromium headless không cho nghe tiếng, nên **đếm số dao động WebAudio** một lời gọi dựng ra — đã tắt tiếng thì `fanfare + beep + pickup` dựng **0** node, bật lại thì `fanfare` dựng **9** node. Nút 🔊/🔇 ghi đúng khoá `astroq-sfx` dùng chung với 3 mini-game.
  - **Hồi quy: 0 hỏng.** `check_pages` **135/135** (thêm mục [3b]: trang nhiệm vụ không tự cộng tt/XP bằng số viết cứng, chỉ đọc `r.data.awarded`/`r.data.xpGained`) · `smoke_onboard` **100/100** · `smoke_mission_intro` **57/57** · `smoke_profile_pages` **125/125** · `smoke_vault` **83/83** · `regress_auth_pages` **19/19** (thêm `specimen-vault.html` và `mission-earth.html` vào danh sách) · `shoot_dodge` và `shoot_defender` đều đạt hết — **0 lỗi console ở mọi bộ**.
  - ✅ **ĐÃ BỎ HẲN KHỐI MẶT TRĂNG (30/07/2026).** Trước đó màn tổng kết có một `.sec` "HÀNH TINH MỚI ĐÃ MỞ KHOÁ 🌙 MẶT TRĂNG" + nút `win-moon` để `disabled`. Nay thay bằng khối **VIỆC TIẾP THEO** chỉ sang Trung Tâm Nhiệm Vụ + nút `win-missions` **bấm được thật**. Xem mục *Bỏ khối Mặt Trăng* trong nhật ký. `Missions.All` hiện chỉ có `earth`; thêm hành tinh thì thêm một dòng ở đó rồi dựng trang tương ứng. Nội dung khoa học của 4 thẻ mẫu vật và 3 câu Codex **chưa qua rà soát chuyên môn** (viết định tính, không có con số ngoài "70%"). — `mission-earth.html`, `css/mission-earth.css`, `js/earth3d.js`, `js/sfx.js`, `js/mission-intro.js`, `js/badges.js`, `js/progress.js`, `js/firebase-auth.js`, `dashboard.html`, `AstroqSV/src/AstroqSV.Api/Services/Missions.cs`, `AstroqSV/src/AstroqSV.Api/Services/Achievements.cs`, `AstroqSV/src/AstroqSV.Api/Services/Wallet.cs`, `AstroqSV/src/AstroqSV.Api/Endpoints/MeEndpoints.cs`, `AstroqSV/src/AstroqSV.Api/Data/DynamoContext.cs`
- **Màn mở đầu luồng nhiệm vụ tại Trái Đất: PHẦN MỞ ĐẦU (INTRO CUTSCENE) của Nhiệm Vụ 01 "Hành Tinh Xanh".** Chạy ngay sau màn loading Luna: khoang lái mờ dần → Luna vòng vào quỹ đạo Trái Đất (động cơ rầm rì + bip radar) → Comet gõ từng chữ lời giao nhiệm vụ → pop-up HUD `☄️ MISSION 01: HÀNH TINH XANH ☄️` + `[ NHẤN ĐỂ KÍCH HOẠT SỨ MỆNH ]`. File mới: `js/mission-intro.js`, `css/mission-intro.css`, và **`js/space-scene.js`** (mô tả đầy đủ ở mục 2).
  - **Quyết định lớn nhất: TÁCH bộ vẽ cảnh không gian ra dùng chung thay vì vẽ Trái Đất lần thứ hai.** Người chơi đi từ màn loading sang cutscene chỉ trong vài giây — hai Trái Đất khác nhau một chút là thấy ngay. `js/warp-screen.js` được refactor để dùng `js/space-scene.js`, **546 → 246 dòng**, và giờ chỉ còn NHỊP PHIM. Refactor code đang chạy tốt là có rủi ro, nên **chứng minh bằng đo lại**: `smoke_onboard.py` **100/100 đạt** với đúng những con số cũ (vệt sao lúc tăng tốc 4.917 px vs 53 px lúc dừng, Trái Đất 98.936/139.876 px lam, Luna đậu bên trái) — không hồi quy.
  - **Backend:** thêm cờ `intro01Seen` vào bản ghi PROFILE, dùng LẠI route `GET|PUT /me/onboarding` thay vì mở route mới. **Hai cờ phải ĐỘC LẬP** — `SetOnboardingAsync(tourSeen?, intro01Seen?)` chỉ ghi cờ được truyền vào (null = không đụng tới); gộp chung thì xem cutscene nhiệm vụ sẽ xoá dấu "đã xem tour" và Comet dẫn tham quan lại từ đầu. Giữ nguyên hành vi cũ "PUT body rỗng = tourSeen true" để client cũ không vỡ. `SetOnboardingAsync` trả về trạng thái **đọc lại từ bản ghi** (`ReturnValue.ALL_NEW`), không ghép từ tham số — ghép tay thì cờ không được truyền vào sẽ bị báo là false.
  - **Kiểm thử API độc lập trước khi nối giao diện:** `test_onboarding.py` thêm mục [6b] → **36/36 đạt ở máy, 36/36 đạt lại trên AWS** (đặt riêng từng cờ không làm mất cờ kia, gửi cả hai một lượt, body rỗng, `false` để xem lại, đọc thẳng DynamoDB để xác minh). Chạy lại 3 bộ còn lại sau khi đổi record `Onboarding`: `test_profile` **74/74**, `test_wallet` **50/50**, `test_specimens` **65/65** — cả ở máy và trên AWS (**tổng 225/225 trên bản thật**).
  - **Kiểm thử giao diện:** `scratchpad/smoke_mission_intro.py` **60/60 đạt, 0 lỗi console**. Đo trên trang chứ không đọc code: **tàu có THẬT SỰ bay vòng quanh Trái Đất** — đo khối pixel sáng bên ngoài đĩa hành tinh qua 6 khung, khoảng cách tới tâm giữ **1,47–1,49 R (biên độ 0,016 R)** trong khi **góc thì đổi** (bước lớn nhất 0,063 rad) → đúng là quỹ đạo, không phải đứng im hay bay thẳng; lời Comet khớp từng cụm với bản mô tả và **không lọt thẻ `<b>` ra ngoài** khi đang gõ; pop-up đúng chữ trong ảnh yêu cầu (2 ngôi sao ☄️, nút trong ngoặc vuông); cờ `intro01Seen` **độc lập** với `tourSeen` (kịch bản `tourSeen=true, intro01Seen=false` → chỉ cutscene chạy, tour và màn loading không chạy lại); `reset()` + F5 hiện lại; EN, điện thoại 390×844, `prefers-reduced-motion` đều đạt.
  - ⚠️ **Bài học lặp lại lần thứ ba về test:** bản giả `AstroQAuth` giữ trạng thái trong biến `window` thì **init script gieo lại mỗi lần tải trang**, nên phép kiểm `reset() + F5` không bao giờ đúng. Phải lưu trạng thái vào `localStorage` và **chỉ gieo khi khoá còn trống**. Cùng lỗi này đã gặp ở `smoke_onboard.py` (mục 29/07 bên dưới) — lần này ghi thành ghi chú ngay trong hàm `stub()`.
  - ⚠️ **Một test cũ hỏng ĐÚNG chỗ nên hỏng:** `smoke_onboard.py` báo "không bấm được nút trên dashboard" vì cutscene mới che dashboard — đó là hành vi đúng của sản phẩm. Đã thu hẹp phạm vi bộ test đó (đánh dấu đã xem cutscene) chứ không nới lỏng phép kiểm.
  - ✅ **Nội dung nhiệm vụ ĐÃ làm cùng ngày** — xem mục "Dựng NHIỆM VỤ 01" ở trên; nút kích hoạt giờ dẫn sang `mission-earth.html`. Vẫn cần chốt: ảnh mẫu dùng icon đỏ nhỏ hai bên tiêu đề, tôi chọn **☄️** cho khớp mô-típ thiên thạch của app (nếu là 🚨 thì sửa `TXT.title` — không, sửa 2 thẻ `.cm` trong `js/mission-intro.js`). — `js/space-scene.js`, `js/mission-intro.js`, `css/mission-intro.css`, `js/warp-screen.js`, `dashboard.html`, `css/dashboard.css`, `js/firebase-auth.js`, `AstroqSV/src/AstroqSV.Api/Data/DynamoContext.cs`, `AstroqSV/src/AstroqSV.Api/Endpoints/MeEndpoints.cs`
- **Dựng Kho Mẫu Vật Vũ Trụ (Specimen Vault) — trang `specimen-vault.html` + 2 route API.** Phòng bảo tàng của phi thuyền: 20 mẫu vật ngủ đông trong khoang kính tím, 4 nhóm quyển, màn soi chi tiết kiểu kính hiển vi, bàn điều khiển 3 chỗ. Mô tả đầy đủ ở mục 2. Làm **cả backend** theo quy tắc 1–2 mục 6.
  - **Quyết định lớn nhất: KHÔNG có route "đã thu thập mẫu vật".** Trạng thái mở khoá **suy ra từ bộ đếm `SK=PROGRESS` đã có** (`Services/Specimens.cs`), y như huy hiệu. Ba cái được cùng lúc: (1) gọi API kiểu nào cũng **không thêm được mẫu vật** — test gửi kèm `specimens:[…]`, `desk:[…]`, `unlocked:true` vào `POST /me/progress` và cả ba **bị bỏ qua**; (2) đổi điều kiện thì người dùng cũ tự tính lại đúng, **không cần migration**; (3) không phải viết thêm điểm nối dữ liệu nào ở 7 chỗ sinh sự kiện. Thứ DUY NHẤT ghi được là **bàn điều khiển** (`desk`, list trên bản ghi PROGRESS) — đó là lựa chọn trang trí nên client quyết, nhưng server vẫn kiểm từng id **phải có thật VÀ đã mở khoá**.
  - ⚠️ **Điều kiện mở khoá phải gắn với việc app THẬT SỰ có.** Bản mô tả ban đầu ghi *"Mở khoá tại Mission 02: Mặt Trăng"* — app **chưa có hệ nhiệm vụ nào**, đặt thế là mẫu vật khoá vĩnh viễn và hứa với trẻ một thứ không tồn tại. Nên mỗi mẫu gắn vào một việc có thật: ghé đúng hành tinh ở explorer (`planet:<id>`), ghép đúng chòm sao (`const:<id>`), đọc bài, làm Quiz, chơi game, kỷ lục game. Mặt Trăng mở theo **số hành tinh đã ghé** vì `explorer.html` cố ý bỏ qua Mặt Trời/Mặt Trăng (chúng không nằm trong 8 hành tinh của `js/planets.js`). Đã thêm phép kiểm canh không ai viết lại câu kiểu "Mission n".
  - **Sửa một lỗi thật do chính test bắt được:** `PUT /me/specimens/desk` lúc đầu **im lặng loại bỏ id trùng** rồi trả 200 → client tưởng đã lưu đúng cái nó gửi, mà thực ra bàn khác đi, và **lựa chọn cũ bị ghi đè**. Giờ `CheckDesk` trả về cả danh sách bị loại (trùng · chưa mở khoá · không tồn tại · vượt 3 chỗ) → **400 kèm `rejected`, không ghi gì**. Bài học: hàm "lọc rồi trả về phần hợp lệ" mà không trả về phần bị loại thì chỗ gọi không có cách nào biết mình vừa bị sửa dữ liệu.
  - **Tách 2 thứ dùng chung** thay vì copy sang trang thứ hai (quy tắc 2 mục 6): **`js/constellations.js`** (tên 4 chòm sao — trước nằm trong `achievements.html`, giờ 3 nơi dùng chung: achievements, specimen-vault, điều kiện `const:` ở server) và **`.filters`** chuyển từ `css/achievements.css` sang `css/page-shell.css`.
  - **Kiểm thử API ĐỘC LẬP trước khi nối giao diện** (`scratchpad/test_specimens.py`): **65/65 đạt ở máy, 65/65 đạt lại trên AWS**. Gồm nhánh hỏng: 2 route đều 401 khi thiếu token, token rác 401, **dọn bàn trống khi chưa có tiến độ thì KHÔNG sinh bản ghi PROGRESS trắng**, đặt mẫu chưa mở khoá → 400 và **bàn giữ nguyên mẫu cũ**, id không tồn tại / trùng / 4 mẫu / mảng 40 phần tử / `desk:null` / id toàn khoảng trắng → 400 đúng mã lỗi, `uid` gửi trong query và body **bị bỏ qua**, `current` luôn bị kẹp về `goal`, `DELETE` → 405. Chạy lại test cũ sau khi đổi record `Progress`: `test_wallet` **50/50**, `test_profile` **74/74**, `test_onboarding` **25/25** — cả ở máy và trên AWS (**tổng 214/214 trên bản thật**). Dữ liệu test dọn sạch (bảng còn đúng 3 dòng của tài khoản thật).
  - **Kiểm thử giao diện bằng Chromium thật** (`scratchpad/smoke_vault.py`): **83/83 đạt, 0 lỗi console**. Script **đọc danh mục thẳng từ `Services/Specimens.cs`** chứ không gieo tay, nên client/server lệch nhau là bắt được ngay (đối chiếu id, nhóm, `origin`, số mẫu hiếm, và mọi mẫu đủ tên/fact/nguồn/lời linh vật ở **cả 2 ngôn ngữ**). Đo trên trang: chưa đăng nhập thì 20 khoang đều khoá + **không bịa số nào**, zoom làm mẫu vật to thật (96px→170px), đặt/lấy mẫu gọi PUT **đúng 1 lần**, đầy 3 chỗ thì nút bị vô hiệu + có câu giải thích, **server từ chối thì bộ đếm trả về đúng số cũ + toast lỗi** (không im lặng), Escape đóng modal và **trả tiêu điểm về đúng khoang vừa xem**, EN dịch cả câu nhắc mở khoá do JS sinh, `prefers-reduced-motion` bỏ hết hiệu ứng vô hạn. Chạy lại test cũ: `check_pages` **124/124**, `smoke_profile_pages` **125/125**, `smoke_onboard` **100/100**, `regress_auth_pages` **17/17**.
  - **Ba lỗi tự tìm ra, cả ba chỉ nhìn/ĐO ẢNH CHỤP mới thấy:** (1) **Lưới khoang rơi về 1 CỘT trên điện thoại** — `auto-fill` + `minmax(196px,1fr)` cần 406px mà `.wrap` ở màn 390px chỉ còn ~362px, nên mỗi thẻ to bằng cả màn hình, cuộn mãi không hết 20 khoang; đổi sang số cột cố định 2→3→4. (2) **Vệt quét của kính hiển vi dày thành một DẢI cyan ~60px** vắt ngang ống kính, trông như lỗi vẽ chứ không như tia quét (khoảng sáng 42–58% → thu còn 48–52%) — đúng bài học vệt lửa ở ARCADE-01. (3) **Bóng mẫu vật trong khoang khoá không hề hiện**: nó nhỏ hơn ổ khoá nên bị che kín, và `grayscale` làm nó chỉ chênh nền navy vài đơn vị sáng; đọc CSS thì tưởng `opacity` là đủ, phải **chụp 2 lần rồi so pixel** mới thấy — sau khi bỏ grayscale + nới cỡ chữ lớn hơn ổ khoá thì đo được **4.706 pixel khác biệt** giữa "có bóng" và "ẩn bóng".
  - ⚠️ **Bốn điểm phải biết:** (a) **Nội dung `fact` chưa qua rà soát chuyên môn** — viết ở dạng định tính (tránh con số cụ thể) và **20 URL nguồn đã curl kiểm trả 200**, nhưng cần giáo viên rà lại trước khi phát hành, như ghi chú đã dùng cho `learningdata/`. (b) Nút `[ 📖 Sổ Tay Khám Phá ]` trong bản mô tả trỏ sang **`library.html`** (Thư Viện Khám Phá) vì chưa có trang "Sổ tay" riêng. (c) Thu thập mẫu vật **không thưởng Thiên thạch tím** — nó là bộ sưu tập suy ra từ việc đã làm, thưởng đã trả ở chính việc đó rồi. (d) **CỐ Ý không thêm card HUD thứ 4 ở dashboard** — 3 card là bố cục Comet đang dẫn tham quan (`js/onboard-tour.js`), thêm card là phải sửa tour; đường vào đặt cạnh "Mở Kho Thành Tích" ở cột stat. Việc đó làm dashboard dài thêm ~40px nên `#btn-game` tụt xuống dưới mép màn 900px — 2 phép đo trong `smoke_onboard.py` dùng `elementFromPoint` ở toạ độ nút đã phải sửa lại (đo ở 4 điểm giữa khung nhìn / `scrollIntoView` trước khi đo). — `specimen-vault.html`, `css/specimen-vault.css`, `js/specimens.js`, `js/constellations.js`, `js/progress.js`, `js/firebase-auth.js`, `css/page-shell.css`, `css/achievements.css`, `achievements.html`, `dashboard.html`, `AstroqSV/src/AstroqSV.Api/Services/Specimens.cs`, `AstroqSV/src/AstroqSV.Api/Endpoints/MeEndpoints.cs`, `AstroqSV/src/AstroqSV.Api/Data/DynamoContext.cs`
- **Dọn 3 việc còn treo: ví Thiên thạch tím lên server · chòm sao lên server · gộp CSS trùng lặp.**
  - **VÍ LÊN SERVER — đây là lần đầu tiền tệ của app đi qua API** (`GET /me/wallet`, `POST /me/wallet/spend`, và `POST /me/progress` cộng thưởng + trả về số dư). `Services/Wallet.cs` mới giữ bảng phí và trần thưởng. **Phân công dứt khoát:** *phí* thì server quyết HẲN — client chỉ gửi tên game, server tra `Wallet.Fees`; *thưởng* thì server đặt TRẦN — số tt thu trong lượt phụ thuộc người chơi bay qua đâu, mà game chạy ở máy người dùng nên không tự tính lại được, nên nhận con số client báo rồi kẹp xuống trần theo loại việc (quiz 220 · game 60 · bài 12). Đây là đánh đổi có ý thức, ghi rõ ở đầu `Wallet.cs`; muốn thưởng không thể bịa thì phải để server sinh và kiểm màn chơi — việc lớn, chưa làm.
    - **Ba chốt chặn nguyên tử, không dựa vào tầng ứng dụng:** (1) trừ ví bằng `UpdateItem` + `ConditionExpression meteors >= :n` — đọc-rồi-so-rồi-ghi thì hai lời gọi song song cùng thấy "đủ tiền"; test bắn **6 lời gọi cùng lúc với ví = 5** và đo được **đúng 1 thành công / 5 bị từ chối, ví về 0 không âm**. (2) cộng ví bằng `ADD` nên hai tab nộp cùng lúc không mất lượt. (3) **chống trùng bằng `opId`** (`SK=OP#<opId>`, TTL 7 ngày): client sinh opId MỘT LẦN lúc tạo việc, nên gửi lại từ hàng chờ mà server đã xử lý xong (phản hồi mất giữa đường) thì **không cộng tiền / trừ phí lần hai** — đã đo cả hai đường.
    - **Client:** `economy.js` thành *cache + đồng bộ* — thêm `setFromServer()` (server ghi đè cache, chữa mọi sai lệch kể cả cache bị sửa bằng DevTools) và `spend(game)`; hàm đọc/ghi cache **vẫn đồng bộ** nên 3 game không phải đổi một dòng logic chặn phí. `js/progress.js` mở rộng hàng chờ để mang cả việc `spend`, nên **chơi game khi chưa đăng nhập vẫn bị trừ tiền đúng** — trước đây đó là lỗ hổng: cache trừ mà server không biết, mở dashboard là số dư nhảy về, tức chơi miễn phí.
  - **CHÒM SAO LÊN SERVER** — thêm `PROGRESS.consts` (`{ id chòm: giây nhanh nhất }`, cập nhật bằng **min** vì nhỏ hơn là tốt hơn — ngược với `bests`). ⚠️ **Sửa một lỗi thật:** `achievements.html` dùng **tên tiếng Việt** làm khoá ("Đại Hùng") trong khi game ghi bằng **id** (`ursa-major`/`cassiopeia`/`orion`/`scorpius`) → bộ sưu tập **luôn hiện 0/4 với người chơi thật**. Test cũng gieo sai khoá y như vậy nên không bắt được. **Bài học: dữ liệu dùng chung giữa 2 trang thì đọc lại chỗ GHI, đừng đoán theo cái nhìn thấy trên giao diện.** Đã thêm phép kiểm đối chiếu khoá hai bên (`check_pages.py` mục [10]).
  - **GỘP CSS TRÙNG LẶP** — `games.css` 110→83 dòng, `learn.css` 134→113, `library.css` 147→124; cả 3 nạp `css/page-shell.css`, phần riêng còn lại đều có ghi chú lý do lệch khỏi khung. **Hai lỗi giao diện tự tìm ra, cả hai chỉ nhìn ẢNH CHỤP mới thấy:** (1) `.hub-tag` có `flex:1` + `overflow:hidden` → trên màn 390px nhãn header bị bóp còn **`[ …`** không đọc được gì; lỗi này ở khung chung nên **ảnh hưởng cả `profile.html` và `achievements.html`** vừa làm hôm nay — sửa bằng `min-width:max-content`. (2) Thẻ bài viết nổi bật của `library.html` mang class `.hero`, **trùng với `.hero` của khung chung** → đổi thành `.feat`. Sửa thêm một lỗi có từ trước: ô tìm kiếm ở `library.html` **tràn ngang trên điện thoại** (`<input>` không co xuống dưới bề rộng mặc định ~20 ký tự) — trước là 23px, đã về 0 bằng `min-width:0`.
  - ⚠️ **Cách đo cho việc dọn dẹp:** viết `scratchpad/shot_pages.py` chụp 3 trang × desktop/mobile rồi **so pixel trước/sau** — giao diện phải KHÔNG đổi. Hai bài học: **phải CHẶN mọi yêu cầu ra ngoài** (ảnh bài viết của `library.html` tải từ NASA nên hai lần chụp lệch **19% pixel** dù bố cục y hệt → báo hỏng oan); và **đừng dùng `git checkout` để lấy mốc so sánh** — bản commit gần nhất cũ hơn nhiều (mất cả việc chưa commit của phiên trước), tôi đã phải khôi phục từ bản sao lưu. Kết quả cuối: library desktop lệch **0,038%**, learn **0,35–0,39%** (chỉ do cỡ chữ hero), games cao hơn/thấp hơn vài pixel do `line-height` — không có thay đổi bố cục nào.
  - **Kiểm thử:** `test_wallet.py` **50/50 đạt ở máy, 50/50 đạt lại trên AWS** (gồm: chưa có ví → 409 chứ không tạo ví âm; client gửi `amount/fee/cost:0` → **vẫn trừ đúng 5**; `reason` lạ → 400; tiêu tới hết rồi tiêu nữa → 409 và **không âm**; 6 lời gọi song song → 1 thắng; gửi lại cùng `opId` → không trừ/không cộng lần hai; đòi 999999 tt → kẹp về trần; đọc lại bài cũ → không cộng tiền; chòm sao nhanh hơn thì cập nhật, chậm hơn thì giữ; id chòm có ký tự lạ → bỏ qua; 3 route báo cùng một số dư). Chạy lại: `test_profile.py` **74/74** và `test_onboarding.py` **25/25** trên AWS. Giao diện: `smoke_profile_pages.py` **125/125**, `check_pages.py` **110/110**, `smoke_onboard.py` **100/100**, `regress_auth_pages.py` **17/17**, `shoot_dodge.py` **27 PASS**, `shoot_defender.py` **44 PASS** — **0 hỏng, 0 lỗi console**. Dữ liệu test đã dọn sạch (0 dòng sót, 0 tài khoản tạm sót).
  - ⚠️ **Còn treo:** phần thưởng vẫn do client khai (server chỉ đặt trần) — muốn không thể bịa thì server phải sinh và kiểm câu hỏi / màn chơi. — `economy.js`, `js/progress.js`, `js/firebase-auth.js`, `js/api.js`, `achievements.html`, `profile.html`, `games.html`, `learn.html`, `library.html`, `game-dodge.html`, `game-defender.html`, `game-constellation.html`, `css/page-shell.css`, `css/games.css`, `css/learn.css`, `css/library.css`, `AstroqSV/src/AstroqSV.Api/Services/Wallet.cs`, `AstroqSV/src/AstroqSV.Api/Endpoints/MeEndpoints.cs`, `AstroqSV/src/AstroqSV.Api/Data/DynamoContext.cs`
- **Đổi tên "Phòng Trái Lực" → "Khu Huấn Luyện" ("Training Bay") + dựng 2 trang riêng: Kho Thành Tích và Hồ sơ Phi Hành Gia.** Gói gọn trong `dashboard.html` (thẻ MOD-02 + 3 chuỗi i18n + toast + modal thiếu tt); class `card--game` giữ nguyên nên không phải sửa CSS. Hai trang mới **làm cả backend** theo quy tắc 1–2 mục 6.
  - **Backend (đã deploy):** `Services/Achievements.cs` mới (luật chơi) + 4 route trong `MeEndpoints.cs`: `GET|PUT /me/profile`, `GET /me/achievements`, `POST /me/progress`. Bản ghi mới `PK=USER#<uid>, SK=PROGRESS` **chỉ chứa bộ đếm thô** + danh sách huy hiệu đã mở; cấp độ, %, điều kiện huy hiệu đều **tính ra** — đổi công thức hay đổi mốc thì người dùng cũ tự tính lại đúng, không cần migration. Bộ đếm tăng bằng `ADD` (cộng nguyên tử) nên 2 tab nộp cùng lúc không mất lượt. **Sáu quyết định đáng ghi:** (1) `POST /me/progress` nhận **VIỆC ĐÃ LÀM** (`quiz`/`game`/`lesson`/`planet`), không nhận XP — test có kịch bản client gửi `xp:999999, badges:["level-20"]` và cả hai **bị bỏ qua**. (2) **Trần XP 300 mỗi lượt nộp**: game chạy ở máy người dùng nên điểm là con số client tự khai; không có trần thì một lời gọi `{"type":"game","score":1000000}` cộng **50.015 XP và nhảy lên cấp 32** — đo được đúng như vậy, đó là lý do trần này tồn tại. 300 = đủ cho lượt tốt nhất có thật (quiz 10 câu đúng hết = 280). (3) **Chống farm**: bài học ghi bản ghi `READ#<id>` có điều kiện `attribute_not_exists`, đọc lại trả `counted:false` và **không cộng gì**; hành tinh lưu string set nên ghé lại không tăng, và **kiểm trước khi cộng XP**. (4) Kỷ lục **không bao giờ tụt** (nộp 10 sau khi đã có 1.000.000 thì giữ nguyên). (5) `GET /me/achievements` **tự cấp bù** huy hiệu đã đủ điều kiện nhưng chưa ghi nhận, nên người dùng có tiến độ từ trước không phải chờ lượt sau. (6) Ghi hồ sơ dùng `attribute_exists(PK)` để **không âm thầm sinh hồ sơ rỗng**; avatar phải khớp `^ava/…\.(png|webp|jpg)$` — không kiểm thì client ghi được URL tên miền khác vào hồ sơ.
  - **Kiểm thử API ĐỘC LẬP trước khi nối giao diện** (`scratchpad/test_profile.py`): **74/74 đạt ở máy, 74/74 đạt lại trên AWS**. Gồm nhánh hỏng: 4 route đều 401 khi thiếu token, token rác 401, chưa có hồ sơ → 404 `no-profile` **và không tạo bản ghi nào**, tên rỗng/25 ký tự → 400, avatar `https://ke-khac.example/anh.png` và `../../etc/passwd` → 400, id nhân vật có ký tự lạ → 400, `type` sai → 400, điểm âm bị kẹp về 0, điểm 1 tỉ kẹp còn 1.000.000, 10 triệu giây kẹp còn 21.600, `uid` gửi trong query/body **bị bỏ qua**. Test tự tạo tài khoản Firebase tạm để có ID token thật, tự dọn **mọi** bản ghi của mình trong `finally` (đã kiểm lại: 0 dòng sót, 0 tài khoản sót).
  - ⚠️ **Deploy lần đầu THẤT BẠI và tự rollback** — `sam build` chết giữa chừng với `WinError 5` (thư mục `.aws-sam/build` cũ bị giữ, dự án nằm trong OneDrive), rồi `sam deploy` gói đúng cái thư mục rỗng đó → Lambda trả `Uploaded file must be a non-empty zip`, CloudFormation `UPDATE_ROLLBACK_COMPLETE`. **Bản thật không bị ảnh hưởng** (đã kiểm `/health` + `/me/onboarding` vẫn 401 đúng ngay sau đó). Xoá `.aws-sam` rồi build lại là xong. **Bài học: sau `sam build` phải kiểm thư mục artifact không rỗng trước khi deploy** — `sam deploy` không tự phát hiện.
  - **Client:** `profile.html` + `achievements.html` + `css/profile.css` + `css/achievements.css`, và **4 file dùng chung mới**: `css/page-shell.css` (khung trang nội dung), `js/badges.js` (tên huy hiệu song ngữ), `js/progress.js` (báo việc đã làm + hàng chờ), `js/planets.js` (8 hành tinh). **Tách `js/characters.js`** ra khỏi `js/auth-flow.js` vì trang Hồ sơ cũng cho đổi trang phục từ cùng roster — copy 10 dòng dữ liệu sang trang thứ hai là chắc chắn có ngày hai bên lệch. Mô tả đầy đủ ở mục 2.
  - **Nối 7 điểm sinh dữ liệu** vào đúng chỗ "chốt lượt" đã có sẵn: `quiz.html` (`showSummary`), `learn.html` + `library.html` (lúc nhận thưởng đọc bài), 3 game (trong `onFinishGame`, đã thêm tham số `score`/`seconds`), `explorer.html` (`selectBody` → ghé hành tinh). ⚠️ **Cố ý không nạp SDK Firebase (233 KB) ở những trang đó** — việc được xếp hàng chờ trong `localStorage`, dashboard/profile gửi nốt. Đây cũng là lý do các game vẫn mượt như trước.
  - **Dashboard: BỎ HẾT SỐ BỊA.** Trước đây khối `STATS` cắm cứng "cấp 2 · Sao Hoả · 12/30 huy hiệu · 12,5 giờ bay · 88% · Mở khoá huy hiệu 'Nhà Du Hành' 2 phút trước" và gauge "CHUỖI BAY 7 ngày". Hiện số bịa cạnh hai trang có số thật thì hai bên lệch nhau và trẻ không biết tin cái nào. Giờ tất cả lấy từ `GET /me/achievements`; radar **suy ra từ bộ đếm** (công thức trong `radarData()`); nhật ký = **huy hiệu mở gần đây nhất** theo `earnedAt` thật, chưa có gì thì hiện ô rỗng chứ không bịa dòng nào; gauge "CHUỖI BAY" đổi thành **XP** vì không có bộ đếm chuỗi ngày nào trong DB.
  - **Kiểm thử giao diện: `scratchpad/smoke_profile_pages.py` 115/115 đạt · `scratchpad/check_pages.py` 90/90 đạt · 0 lỗi console.** Đo trên trang chứ không đọc code: vòng cấp độ vẽ đúng 34% (`stroke-dashoffset` = 2πr·0,66), nhân vật lấy theo **server** chứ không theo `localStorage`, bấm trang phục gửi đúng patch `{character, avatar}` và **bấm lại ô đang chọn thì KHÔNG gọi API lần nữa**, tên rỗng thì không gọi API, lọc 2 chiều (6 đã mở / 4 chưa mở / tổ hợp nhóm) đúng từng tổ hợp, **đã mở luôn nằm trước chưa mở**, mất mạng và chưa đăng nhập đều hiện dải nhắc đúng câu và **không bịa cấp độ** (vẫn là 1), kho thành tích **không tự đoán huy hiệu nào** khi không có server, hàng chờ giữ đúng nội dung sự kiện và **bản sao trong máy không đếm trùng một bài đọc**, quiz gọi `postProgress` **đúng một lần** và **không gửi xp/level/badges**. Chạy lại toàn bộ test cũ: `smoke_onboard.py` **100/100**, `regress_auth_pages.py` **17/17** (13 trang, 0 lỗi console), `shoot_dodge.py` **27 PASS / 0 FAIL**, `shoot_defender.py` **44 PASS / 0 FAIL**, `test_onboarding.py` **25/25** trên AWS.
  - **Ba lỗi tự tìm ra, hai trong số đó chỉ nhìn ẢNH CHỤP mới thấy:** (1) **Thanh tiến độ ở thẻ huy hiệu chưa mở KHÔNG hiện** — `.prog` và `.bar` đều là `<span>`, mà `<span>` mặc định `inline` nên `height:6px` bị bỏ qua và thanh tụt về 0px; style vẫn ghi đúng `width:78%` nên đọc code hay đọc style đều không thấy sai. Đã thêm phép kiểm **đo chiều cao thật** để không lặp lại. (2) **Nhãn ô thống kê bị cắt đuôi** ("LƯỢT HUẤN LUY…", "HÀNH TINH ĐÃ …") vì `minmax(148px,1fr)` cho 4 cột trên desktop → nới lên 172px, và thêm phép kiểm `scrollWidth > clientWidth`. (3) **Hai trang hiện cùng con số nhưng khác chữ**: `profile.html` ghi "1.3 giờ" còn `dashboard.html` ghi "1.3 Giờ" — chốt chữ thường ở cả hai và thêm phép kiểm chéo.
  - ✅ **Ba chỗ treo ở đây ĐÃ xử lý cùng ngày** (xem mục trên cùng): ví Thiên thạch tím đã lên server (`/me/wallet`, phí do server quyết, chống trùng bằng `opId`); chòm sao đã ghép đã lên `PROGRESS.consts` (và sửa lỗi khoá dùng tên tiếng Việt); `games.css`/`learn.css`/`library.css` đã chuyển sang `css/page-shell.css`. Còn treo: phần thưởng vẫn do client khai, server chỉ đặt trần.
- **Comet dẫn phi hành gia mới tham quan khoang tàu, rồi màn loading Luna bay tới Trái Đất.** Sau khi chọn nhân vật và được APPROVED, vào dashboard là Comet chào theo tên rồi chỉ lần lượt 5 khu vực bằng box thoại + ô sáng, kết thúc bằng "khởi động động cơ" → màn loading. File mới: `js/onboard-tour.js`, `css/onboard-tour.css`, `js/warp-screen.js`, `css/warp-screen.css` (mô tả đầy đủ ở mục 2). **Làm CẢ backend theo quy tắc 1–2 mục 6** — đây là tính năng đầu tiên có state ĐI QUA API thay vì nằm trong `localStorage`.
  - **Backend (đã deploy, `sam deploy` lên stack `astroqsv`, URL API không đổi):** `Endpoints/MeEndpoints.cs` mới với `GET|PUT /me/onboarding`, `DynamoContext.GetOnboardingAsync/SetOnboardingAsync`. **Ba quyết định:** (1) cờ đặt **ngay trên bản ghi PROFILE** chứ không tạo SK riêng — nó là thuộc tính của hồ sơ, và `GetUserAsync` đã đọc PROFILE nên không tốn thêm lần đọc; (2) `RequireAuthorization()` gắn ở **cấp group `/me`** để thêm route mới là tự có bảo vệ, không phụ thuộc việc người viết có nhớ gắn hay không; (3) **uid lấy TỪ TOKEN**, không bao giờ từ thân request — client gửi uid lên thì ai cũng đọc/ghi được hồ sơ người khác (test có kịch bản này). `SetOnboardingAsync` dùng `attribute_exists(PK)` để **không âm thầm sinh ra hồ sơ rỗng** khi uid không tồn tại; `PUT` nhận cả `tourSeen:false` để xem lại màn giới thiệu mà không phải sửa DB bằng tay.
  - **Kiểm thử API ĐỘC LẬP trước khi nối vào giao diện** (`scratchpad/test_onboarding.py`): **25/25 đạt ở máy (`dotnet run`) và 25/25 đạt lại trên bản thật AWS**, gồm đủ nhánh hỏng — không token 401, token rác 401, uid chưa có hồ sơ thì `GET` trả `tourSeen:false` nhưng `PUT` trả 404 `no-profile` **và không tạo hồ sơ rỗng**, uid gắn trong query string / body **bị bỏ qua**, `DELETE` → 405. Test **tự tạo tài khoản Firebase tạm** (REST `signUp` bằng web apiKey) để có ID token thật, tự tạo/xoá bản ghi PROFILE bằng `aws dynamodb`, và **tự dọn cả hai trong `finally`** nên hỏng giữa chừng cũng không để lại rác. Chạy lại các route cũ sau deploy: `register` 400 `invalid-email`/`weak-password`, `resend` 404 `no-pending` — không hồi quy.
  - **Kiểm thử giao diện bằng Chromium thật** (`scratchpad/smoke_onboard.py`): **100/100 đạt, 0 lỗi console, ổn định qua 3 lượt liên tiếp**. Đo trên trang chứ không đọc code: ô sáng **trùng đúng `getBoundingClientRect()` của khu vực ±2px** ở cả 5 bước × desktop 1440×900 và điện thoại 390×844, box thoại **không tràn màn hình và không đè lên khu vực đang chỉ**, lớp phủ **chặn được cú bấm** vào nút phía dưới, đủ 7 chấm bước và chấm sáng đúng bước, EN dịch cả 7 bước lẫn chữ màn loading, **đổi ngôn ngữ ở tab khác thì tour dịch theo mà không mất bước đang xem**, F5 lần hai **không** dẫn tham quan nữa, `AstroQTour.reset()` + F5 thì hiện lại. Màn loading: đo pixel **ngay trong trang** — lúc tăng tốc vùng 40% bên trái có **~4.900 px sáng**, lúc đã dừng còn **~50 px** (chứng minh vệt là VỆT chứ không phải đốm), Trái Đất **>73% pixel trong đĩa là xanh-lam** + có lục địa xanh lá, Luna đậu **bên trái** hành tinh, thanh tiến trình đạt 100%, `prefers-reduced-motion` thì **không có vệt warp** và phim kết thúc sớm. Thêm `scratchpad/regress_auth_pages.py` **10/10 đạt** (6 trang + popup đăng ký ở landing-app) vì lần này sửa `js/api.js` và `js/firebase-auth.js` — hai file nằm trên đường đăng nhập.
  - **Sáu lỗi tự tìm ra và đã sửa, ba trong số đó chỉ nhìn ẢNH CHỤP mới thấy:** (1) **Trái Đất trông như quả bóng** — lục địa là đa giác 12 cạnh thẳng và mây là khối trắng đặc, nhìn đúng như hoa văn khâu trên bóng; đổi sang đường cong bậc hai qua trung điểm + ellipse rx≠ry cho lục địa dài theo một chiều, mây/mũ băng thành gradient toả. (2) **Vành khí quyển vẽ bằng cung stroke dày** thành cái vòng rời lơ lửng cạnh hành tinh → chuyển sang gradient ở mép trong hình cầu. (3) **Vệt lửa đuôi tàu dài 3,1× thân, alpha 0,72** → một mảng tím đặc kéo hết ra ngoài khung, nhìn như lỗi vẽ chứ không như luồng khí đang tan (đúng bài học đã ghi ở ARCADE-01) → rút còn 2,05× và hạ alpha. (4) **Box thoại đè lên thẻ Bản đồ Thiên Hà**: thẻ cao 375px + box 245px > màn hình 900px nên cả trên và dưới đều hết chỗ, box rơi vào giữa và che mất thứ Comet đang giới thiệu → thêm nhánh **đặt sang bên phải/bên trái** (mũi nhọn `.left`/`.right`). (5) **Mất mạng thì màn giới thiệu KHÔNG hiện**: `boot()` trong `firebase-auth.js` ném lỗi khi `import()` SDK từ gstatic hỏng, làm vỡ cả chuỗi promise của phía gọi → cho `boot()` trả `null` thay vì ném, `idToken()` bọc try/catch, `autoStart()` có `.catch()`. Đây là lỗi **im lặng và chỉ xảy ra khi mạng hỏng** — dễ lọt nhất. (6) `CONFIG.tEnd = 1.5` của nhánh reduced-motion **ghi đè vĩnh viễn vào CONFIG** nên lượt chạy sau trong cùng trang cũng bị cắt ngắn → đổi sang biến riêng. Sửa thêm: nhãn "Bỏ qua phần giới thiệu" xuống 2 dòng trên điện thoại làm méo hàng chân box → rút còn "Bỏ qua"; chữ màn loading rỗng ở khung đầu (tách `paintCaption()` gọi ngay trong `play()`).
  - ⚠️ **Ba bài học về cách ĐO, không phải về sản phẩm** (đều làm test báo hỏng oan): đo ô sáng bằng khoảng chờ cố định là đo **giữa lúc nó còn đang bay** (vừa cuộn mượt vừa transition 0,5s) → viết `wait_stable()` chờ tới khi đứng yên, **phải có `min_wait`** không thì 2 mẫu đầu còn là vị trí của bước TRƯỚC và cả bài lệch đúng một bước; đếm pixel ở **dải rìa hẹp 18%** dao động 189→3.000 px giữa các lượt (sao sinh lại gần điểm tụ, tốc độ tỉ lệ bán kính nên vùng ngoài cùng vốn ít sao), nới ra **50%** thì lại lọt vành khí quyển và Luna đang đậu làm nền cố định ~3.350 px → chốt **40%** và **so hai trạng thái với nhau** thay vì một ngưỡng tuyệt đối; bản giả `window.AstroQAuth` bị **module ES thật ghi đè** (module chạy sau script cổ điển) nên `PUT` đi vào module thật → cài bằng `Object.defineProperty` có setter nuốt lời gán.
  - ✅ **Đã xử lý cùng ngày** (xem mục ngay trên): "Kho Thành Tích" và "Hồ sơ Phi Hành Gia" giờ là **trang thật** (`achievements.html`, `profile.html`) và `data-tour` đã trỏ vào đúng đường vào của chúng; thẻ "Phòng Trái Lực" đã đổi tên thành **"Khu Huấn Luyện"** cho khớp lời Comet. Vẫn còn treo: cách gọi "Trạm" vs "phi thuyền" ở ARCADE-02 (ghi ngày 27/07). — `dashboard.html`, `js/onboard-tour.js`, `css/onboard-tour.css`, `js/warp-screen.js`, `css/warp-screen.css`, `js/api.js`, `js/firebase-auth.js`, `AstroqSV/src/AstroqSV.Api/Endpoints/MeEndpoints.cs`, `AstroqSV/src/AstroqSV.Api/Data/DynamoContext.cs`, `AstroqSV/src/AstroqSV.Api/Program.cs`

### 2026-07-28
- **Dựng mini-game giải đố "Ghép Chòm Sao" (ARCADE-04)** — trang mới `game-constellation.html` + `css/game-constellation.css`, nạp `game-shell.css` nên **không copy một dòng khung nào**. Mô tả đầy đủ ở mục 2. **Năm quyết định đáng ghi lại:** (1) **Dữ liệu chòm sao tách hẳn khỏi logic** (`SKY` = mảng `stars/sketch/fact`), toạ độ theo hệ ảo 800×500 + `.field` khoá tỉ lệ 8:5 — hình chòm sao mà đổi tỉ lệ theo màn hình thì **sai cả thiên văn**, không chỉ xấu. (2) **Thưởng 8–15 tt tính bằng công thức, không random**: `8 + nhanh + ít sai`. Trẻ phải hiểu được vì sao mình được 15 hay 8, không thì phần thưởng thành xổ số; chip tt trên HUD hiện luôn phần thưởng đang chờ nên thấy nó tụt dần khi chậm/sai. (3) **Chốt thưởng NGAY lúc ghép xong**, trước khi mở modal thông tin — đóng tab ở modal thì công sức vẫn được trả; vẫn đi qua **một hàm `onFinishGame()`** duy nhất. (4) **Chấp nhận nối cả 2 chiều** (2→1 tính là nối 1–2) vì DIFF: Dễ, trẻ nhỏ hay kéo ngược; bù lại có vòng nhấp nháy ở sao kế tiếp, dòng nhắc "TIẾP THEO: NỐI a → b" và **đường mờ chỉ lối sau 9 giây bí**. (5) **Vẽ hào quang bằng 3 lượt stroke chồng nhau (`lighter`) chứ không `shadowBlur`** — blur trên canvas mỗi khung hình đắt gấp nhiều lần; cùng lý do như vệt sáng ở ARCADE-01. **Nội dung thiên văn có thật** (Dubhe–Merak chỉ tới Polaris · supernova 1572 của Tycho trong Thiên Hậu · tinh vân M42 ~1.300 năm ánh sáng dưới Đai Lạp Hộ · Antares "đối thủ của Sao Hoả" trùm qua quỹ đạo Sao Hoả). **Kiểm thử:** `check_constellation.py` **56/56 đạt** (ngoặc cân, 32 `$("id")` tồn tại, **33 khoá i18n khớp vi/en và không khoá nào bỏ không**, token `{n}/{tt}/{a}/{b}` đủ ở cả 2 ngôn ngữ, asset, class↔CSS **hai chiều** — cả "class thiếu CSS" và "CSS bỏ không", và **dữ liệu chòm sao**: id chạy 1..n liên tục, sao nằm trong sân, **mọi cặp sao cách nhau >55px** để hitR=30 không bấm trúng nhầm). `play_constellation.py` **49/49 đạt, 0 lỗi console** — chơi thật bằng chuột và cảm ứng, **toạ độ sao đọc thẳng từ `SKY` nên test không phải đoán**: nối sai đo được **223 px đỏ** rồi **về 0** (đường sai bị huỷ, không khoá), nối đúng **389–444 px cyan** và **vẫn còn sau khi nối tiếp** (đã khoá), cả **bấm-2-lần** lẫn **kéo-thả** đều nối được, **tạm dừng thì đồng hồ dừng hẳn** (0:03 = 0:03), ghép xong **ví +14 ngay** và bảng kết quả ghi đúng +14, modal thông tin **tự bật đúng chòm vừa ghép**, "Chòm sao khác" trừ đúng 3 tt và **ra chòm khác**, EN dịch cả tên chòm sao lẫn đoạn thông tin do JS sinh, thiếu tt thì **không trừ tiền**, điện thoại 390×844 sân vẫn **đúng tỉ lệ 1.600** và chạm-2-lần nối được, `prefers-reduced-motion` vẫn giải xong. **3 lỗi tự tìm ra và đã sửa:** nút Pause còn hiện sau khi ghép xong (thiếu `syncBtns()` trong `complete()`); **kỷ lục làm tròn LÊN** nên bảng kết quả hiện "Thời gian 0:04 · Kỷ lục 0:05" — kỷ lục tệ hơn lượt vừa chơi, trông như lỗi tính điểm (đổi sang **cắt bớt** `Math.floor`); nút "Chòm sao khác · 3 🪐" **gãy chữ** khi chia 3 nút một hàng (cho nút chính chiếm hẳn một hàng). ⚠️ **Sửa 2 chỗ ở `css/game-shell.css` (ảnh hưởng CẢ 3 game)**: tag tên game `nowrap`+ellipsis, và **màn ≤560px toast neo xuống đáy** vì trên điện thoại header xuống 2 dòng làm thanh HUD tụt qua mốc `top:116px`, toast đè kín chip — đã chạy `smoke_shell.py` **60/60** (3 game × vi/en × desktop/mobile: tag 1 dòng, toast không đè chip, không tràn ngang, 0 lỗi console) và chạy lại **`shoot_dodge.py` 27/27**, `smoke_hub.py` **8/8** (mã ARCADE giữ nguyên 01/02, thẻ mới ARCADE-04, bấm vào mở đúng trang). ⚠️ Kỷ lục + ví vẫn ở `localStorage` (`astroq-constellation-best`, `economy.js`) — **trái quy tắc 2 mục 6**, API ví vẫn chưa tồn tại ở `../AstroqSV/` (mới chỉ có `AuthEndpoints.cs`); mọi điểm nối dồn vào `onFinishGame()` để lúc chuyển sang API chỉ sửa một chỗ. — `game-constellation.html`, `css/game-constellation.css`, `css/game-shell.css`, `games.html`
- **Né Thiên Thạch: nhân vật điều khiển đổi sang phi thuyền Luna NẰM NGANG + vệt sáng tím ở đuôi tàu, ảnh tối ưu cho game nhẹ.** (1) **Ảnh:** dùng đúng bản art người dùng đặt vào `img/luna ngang.png` (1,7 MB, 1934×1080, mũi đã chỉ sang phải) → sinh `img/luna-side.png` bằng Pillow: cắt theo bbox alpha (1503×796), thu về **192×102** (~1,7× cỡ vẽ lớn nhất: 56×30 ảo × DPR 2 = 113×60 px thật, vì `.field` rộng tối đa 800px), palette 256 FASTOCTREE → **8,7 KB, nhẹ hơn ảnh gốc 190 lần**. Đã so 5 cách nén (PNG rgba 31 KB · PNG-256 8,9 KB · PNG-192 8,2 KB · WebP q82 8,0 KB · q90 9,5 KB): WebP chỉ hơn 0,9 KB nên **chọn PNG-256 để dùng một file chạy mọi nơi**, không cần `<picture>` hay đường lùi. ⚠️ **Bản gốc 1,7 MB nên GIỮ NGOÀI repo** (repo này deploy công khai qua Pages, thêm 1,7 MB ảnh không ai dùng vào là tải thừa) — hiện nó đang untracked, đừng `git add` nếu chưa chuyển đi. (2) **Hitbox đổi theo hình: 46×34 → `byteW/byteH` = 50×30**, vì phi thuyền dài 1,88 : cao 1 chứ không vuông vức như robot cũ. Giữ nguyên hitbox cũ thì một trong hai đằng sai: vẽ cao 34 → dài 64, mũi chìa 9px ra ngoài vùng chết (đâm vào đá mà không chết); vẽ dài 50 → cao 26,5, thấp hơn hitbox 34 nên **chết oan khi mắt còn thấy hở**. Hộp mới 50×30 (sau `hitInset` còn 38×22,8) diện tích xấp xỉ hộp cũ (34×26,8), rộng và dẹt đúng như hình — dọc dễ hơn 4px, ngang khó hơn 4px, đổi lại **hình vẽ đúng bằng vùng chết**. Bot autopilot bay được **6,2s** (bản cũ 4,4s) → khe hẹp vẫn với tới được, không phá cân bằng. (3) **Vệt sáng tím:** điểm vệt **trôi trái đúng bằng `dx` của màn hình** nên vệt đứng yên trong không gian (dính cứng vào đuôi thì trông như cái đuôi cao su); bề rộng lấy theo **pháp tuyến** chứ không phải trục Y — tàu bổ nhào 540px/s trong khi màn chỉ cuộn 190–300px/s nên đoạn vệt hay dốc hơn 45°, lệch theo Y thì chỗ dốc phình thành mảng loe; gradient cũng chạy **dọc theo vệt** chứ không theo trục X. **Không dùng `shadowBlur`** (đắt, vẽ 60 lần/giây) mà dùng 2 lớp polygon + `"lighter"`. **Sửa sau khi soi ảnh chụp:** bản đầu để vệt dài 0,42s và thót chậm (`pow(k,0.85)`) → lúc tàu đảo chiều, khúc gấp của đường bay nằm giữa vệt còn dày và sáng, **trông như tia sét** chứ không phải luồng khí; đổi thành `trailTaper:1.6` + `trailLife:0.36` + hạ alpha là ra luồng khí đang tan. Lửa đẩy lúc bấm và hạt bung ở ống đẩy đổi từ vàng-cyan sang **tím** cho khớp (chồng 2 tông ở cùng chỗ trông như lỗi màu). **Kiểm thử thật trên Chromium — `verify_luna_trail.py` 19/19 đạt, 0 lỗi console:** đo pixel **ngay trong trang** qua các khung `requestAnimationFrame` liên tiếp (screenshot mất ~200ms, quá chậm cho hiệu ứng sống 0,36s), diện tích **quy về hệ ảo 800×500** nên một ngưỡng dùng được cho mọi DPR/cỡ màn. Kết quả khi đang bay: **382 px² tím ngay sau đuôi**, **0 px² trước mũi**, thân tàu 103 pixel tím vs 7 pixel cyan → đúng sprite Luna chứ không phải robot vector cũ; `prefers-reduced-motion` 243 px² (vẫn thấy vệt) và vùng hào quang mờ **217 vs 639 px²** → chứng minh lớp hào quang rộng đã bị bỏ đúng như thiết kế; điện thoại 390×844 đo được 249 px². **Đối chứng quan trọng: hết lượt (không vẽ tàu/vệt nữa) thì cửa sổ đo về 0 px²** — chứng minh con số đo được là của vệt chứ không phải của nền dải ngân hà. Nhịp khung 16,7ms (60fps) *trong khi vẫn đang đọc pixel mỗi khung*. ⚠️ **Cách đặt cửa sổ đo:** tàu dài 56px nên cửa sổ phải nằm **ngoài thân tàu** (sau đuôi 30–90px, trước mũi 30–90px); lần đầu để cửa sổ cách tâm 8px thì nó đếm cả vỏ tàu màu tím và báo "trước mũi cũng có vệt" (22%). Ở màn brief thì vệt **chỉ dài ~16px** (màn hình cuộn 46px/s) nên nằm gọn sau thân tàu, không đo được — đó là hành vi đúng, nên chỉ ghi số liệu chứ không đặt thành tiêu chí. **Chạy lại toàn bộ test cũ: `shoot_dodge.py` 27/27 đạt, `check_idents.py` 0 tên tự do, `check_dodge.py` mọi mục đạt** (mục "class có CSS" báo hỏng là **lỗi cũ của script**, nó chỉ đọc `css/game-dodge.css` mà khung đã tách sang `css/game-shell.css` từ 27/07). ⚠️ **Bài học về autopilot đọc pixel:** đổi hình là **hỏng con bot kiểm thử**, dễ đọc thành "game hỏng" — `verify_gem.py` báo 8/8 lượt không ăn được viên tt nào. Ba nguyên nhân đều ở bot, không ở game: nó tìm tàu bằng pixel `g>185` (thân robot trắng cũ, Luna tím thì mất dấu); **màu hào quang vệt (168,85,247) trùng tiêu chí nhận viên tt** nên bot lao theo chính cái đuôi của mình; và cửa sổ tìm tàu x=180–226 lấn vào vùng vệt nên tâm tàu bị kéo lệch. Sửa bot (nguỡng `>150` cả 3 kênh, chỉ quét **nửa trước** thân tàu x=199–225, bỏ vùng x<215 khi tìm viên tt) → **`verify_gem_luna.py` ĐẠT: ăn được viên tt, +10 điểm (122 = 112m + 10×1), ví chính +1 lúc kết lượt, toast "+1 tt", 0 lỗi console**. ⚠️ `verify_flame2.py` cũ **cố tình không chạy lại**: nó đo lửa **vàng ấm** (`r>195 && g>165 && b<205`) của bản cũ, giờ lửa là tím nên tiêu chí đó sai theo thiết kế mới — `verify_luna_trail.py` thay thế nó. ⚠️ Kỷ lục + ví vẫn ở `localStorage` (trái quy tắc 2 mục 6) — thay đổi lần này thuần phần vẽ, không thêm điểm nối dữ liệu nào. — `game-dodge.html`, `img/luna-side.png`

### 2026-07-27
- **Dùng phi thuyền Luna (`img/luna2.png`) làm nhân vật điều khiển ở Space Defender.** Bản vector (thân tròn + nòng pháo rời) đổi sang sprite qua `CONFIG.stationSprite` — hook đã dựng sẵn nên chỉ phải sửa phần vẽ. **Ba điểm phải xử lý, không chỉ là đổi đường dẫn ảnh:** (1) Ảnh nhìn từ trên xuống **mũi chỉ LÊN**, còn `aim=0` là hướng PHẢI → phải quay `aim - CONFIG.spriteUp` (`spriteUp = -90°`), để nguyên là tàu lệch 90° so với hướng bắn. (2) **Mũi tàu chính là nòng** nên khi có sprite thì bỏ hẳn nòng vector, không thì hai thứ chồng lên nhau; điểm sinh đạn đổi sang `muzzleDist()` tính theo cỡ ảnh (`shipH*0.48`) thay vì theo chiều dài nòng vẽ. (3) **Vòng khiên phải thu từ 44 → 31** để bám sát thân tàu: va chạm xảy ra khi *mép* vật thể chạm `r=stationR=26`, nên vòng khiên rộng 44 làm thiên thạch lọt hẳn vào trong khiên mà chưa mất giáp — nhìn như lỗi. `spriteScale` để 2.6 cho thân tàu sát hitbox; hitbox vẫn là hình **tròn** nên không đổi khi tàu quay. Giữ nguyên đường lùi: ảnh lỗi → `img/b1.png` → bản vẽ vector cũ vẫn còn trong nhánh `else`. **Kiểm thử `verify_luna.py` (5/5 đạt, 0 lỗi console):** đo **tâm khối pixel hồng của luồng lửa** ở đuôi tàu tại 5 góc ngắm (0/45/90/180/270°) — luôn nằm **ngược hướng ngắm, lệch tối đa 2,1°** → chứng minh cả tàu quay chứ không phải chỉ nòng; bắn thử 3 hướng, đạn bay đúng hướng ngắm lệch ≤1,2° và xuất phát cách tâm 145–158px (từ mũi, không phải từ tâm); ảnh `d10-luna-4huong.png` ghép 4 hướng để soi mắt. Chạy lại `shoot_defender.py`: **45/45 đạt**. ⚠️ **Chữ trên HUD và thẻ game vẫn gọi là "Trạm"** (`Giáp trạm` / "bảo vệ Trạm Vũ Trụ nằm ở tâm") trong khi hình bây giờ là phi thuyền — chưa đổi vì đó là chữ người dùng đã chốt ở thẻ game; cần chốt lại cách gọi. — `game-defender.html`
- **Viết lại "Space Defender" (ARCADE-02) thành phòng thủ 360° + tách `css/game-shell.css` dùng chung.** Trạm đứng ở tâm, vật thể dồn vào từ mọi hướng — thay hoàn toàn bản cũ (tàu ở đáy, thiên thạch rơi từ trên). Xem mô tả đầy đủ ở mục 2. **Bốn quyết định đáng ghi lại:** (1) **Sân VUÔNG 600×600** thay vì 800×500 — sân chữ nhật trong game 360° làm hướng trái/phải xa tâm gấp 1,6 lần hướng trên/dưới, tức là cùng một cú bắn mà bên khó bên dễ. (2) **Sinh vật thể trên vòng tròn r=440** (lớn hơn nửa đường chéo sân = 424) nên mọi hướng cách Trạm **đúng bằng nhau** và không bao giờ "hiện ra" giữa sân; bù lại vật thể ngoài khung phải có **dấu nhắc ở mép** không thì người chơi không biết xoay pháo về đâu. (3) **Bỏ thưởng `floor(điểm/25)`** của bản cũ: bảng kết quả ghi bao nhiêu viên thì ví cộng đúng bấy nhiêu, không có con số ẩn. (4) **Tách `css/game-shell.css`** — viết `game-defender.css` độc lập là copy ~80 dòng khung (header/overlay/chip/nút) từ `game-dodge.css`, trái quy tắc 2 mục 6; giờ 2 game chung một khung, `game-dodge.css` còn **9 dòng**, `game-defender.css` còn phần thanh giáp + ô quiz. **Kiểm thử:** `shoot_defender.py` **44/44 đạt, 0 lỗi console** — trong đó có những thứ chỉ chạy thật mới biết: để yên 7 giây không bắn thì **giáp tụt 100%→76%** (vật thể đâm vào Trạm thật sự trừ giáp), autopilot đọc pixel nhận ra **cả 4 loại** (xám 469 · đỏ 225 · tím 68 · vàng 5 lần ngắm), bắn trúng thạch vàng thì **quiz mở ra và điểm DỪNG hẳn** (177 = 177 sau 1,5s), trả lời đúng **giáp 76%→96% và +5 tt**, giáp về 0 thì bảng kết quả hiện và **ví cộng đúng 115+14=129**, đổi EN dịch cả dòng thưởng do JS sinh, thiếu tt thì **không trừ tiền**, điện thoại 390×844 sân vẫn **vuông tỉ lệ 1.000** và thanh HUD nằm trên sân. Sửa 3 chỗ sau khi xem ảnh chụp: **nòng pháo cụt** (34→42px, thêm miệng nòng sáng + 2 vạch dẫn hướng) vì trong game 360° hướng nòng là thông tin quan trọng nhất trên sân; **đạn laser nhạt lờ mờ** → thêm lõi trắng đặc + vệt tím dài; **tia radar vẽ bằng nét dày 34px trông như vệt bẩn** → đổi sang hình rẻ quạt mờ dần. **Đã chạy lại toàn bộ test của ARCADE-01 sau khi tách CSS: 27/27 + đường tt vẫn đạt, 0 lỗi console** — không hồi quy. ⚠️ Cùng ghi `localStorage` như dodge (kỷ lục + ví qua `economy.js`), trái quy tắc 2; điểm nối vẫn dồn vào **một hàm `onFinishGame()`**. — `game-defender.html`, `css/game-defender.css`, `css/game-shell.css`, `css/game-dodge.css`, `game-dodge.html`
- **Soi "Né Thiên Thạch" bằng Chromium thật (Playwright) — tìm ra 6 lỗi mà đọc mã không thấy.** Máy chưa có Node nên dùng **Playwright bản Python** (`pip install playwright` + `playwright install chromium`, ~114 MB vào `%LOCALAPPDATA%\ms-playwright`). Cách chạy: `python -m http.server 8123` **trong `AstroQhtml/`** rồi mở `http://127.0.0.1:8123/game-dodge.html`. Vì game cần điều khiển liên tục mới đi được xa, tôi viết **autopilot chạy ngay trong trang**: mỗi khung hình nó `getImageData` một dải hẹp để tìm Byte (lấy **hàng có nhiều pixel sáng nhất** — lấy trung bình thì bị sao nền kéo lệch), tìm khe hẹp của cột gần nhất (dò từ trái sang, hàng nào >½ pixel màu đá thì tính là đá), tìm viên tt theo màu tím rực, rồi `dispatchEvent(KeyboardEvent('keydown',{key:' '}))` để bay lên. **6 lỗi tìm được:** (1) **Chip HUD đè lên sân, che mất chùm thiên thạch ở dải trên mà hitbox vẫn còn** → chết vào vật cản không nhìn thấy; sửa bằng cách đưa HUD thành **thanh riêng phía trên sân** (đúng chữ "Bảng HUD phía trên Canvas" trong yêu cầu) và bọc sân + overlay vào `.play`. (2) **Nút Âm thanh bị overlay che, bấm không được** ở màn brief và bảng kết quả — trẻ muốn tắt tiếng trước khi chơi thì không tắt được; giờ HUD nằm ngoài `.play` nên luôn bấm được, và nút Pause tự ẩn khi bấm vô nghĩa. (3) **`ov-start` và `ov-need` cùng hiện** → 2 lớp nền tối cộng nhau, thẻ chồng thẻ; `showNeed()` giờ ẩn các overlay khác trước. (4) **Dòng "đã cộng n tt vào ví" không đổi ngôn ngữ** vì do JS sinh nên `applyTexts` không với tới — tách thành `paintPaid()` và gọi lại trong `applyLang`. (5) **Toast thưởng chết lặng:** `var paid` bị xoá khi refactor nhưng còn `if(paid>0)`; **không hề báo lỗi** vì `<div id="paid">` khiến `window.paid` trỏ vào phần tử DOM → `NaN>0` = false. Trình duyệt im lặng, chỉ **script kiểm tên biến tự do** bắt được → **đừng đặt tên biến trùng `id` phần tử**. (6) Toast rơi đúng hàng chip HUD (`top:64px`) → hạ xuống 116px. Sửa thêm phần hình: đá quá tròn và dính vào nhau → 7–10 đỉnh + bán kính lệch nhiều + nét viền tối; vệt sáng mép khe mảnh lại (trước trông như cái nắp); vệt lửa to hơn (vàng → cyan). **Kiểm thử:** `shoot_dodge.py` **27/27 đạt, 0 lỗi console** (mặc định tiếng Việt, trừ đúng 5 tt một lần, pause dừng hẳn quãng đường, `onFinishGame` cộng đúng, EN đủ nhãn, thiếu tt thì **không** trừ tiền, điện thoại 390×844 giữ tỉ lệ 8:5 + không tràn ngang + thẻ không bị cắt); `verify_gem.py` **thử tới 8 lượt cho tới khi ăn được viên tt** → xác minh **+10 điểm & +1 tt vào ví tạm & ví chính +1 lúc kết lượt & toast "+1 tt"** (2 lượt 0 viên thì ví **không** đổi — không có cộng khống); `verify_flame2.py` đo pixel 7 khung liên tiếp sau cú bấm: **lửa chỉ ở bên trái Byte (21 px trái / 0 px phải), vươn ra ngoài thân tới x=167**, Byte đi lên 261→228. ⚠️ **Bài học về cách đo:** lần đầu tôi chụp ảnh rồi mới đọc pixel nên **báo sai là "không có lửa"** — `page.screenshot()` mất ~200ms trong khi `flapT` chỉ sống 0.22s; phải đọc pixel **trong trang** ở các khung `requestAnimationFrame` liên tiếp. Ảnh chụp nằm ở scratchpad (`01-brief`, `10-gem-collected`, `06-over`, `09-mobile`, `zoom-flame`). — `game-dodge.html`, `css/game-dodge.css`
- **Viết lại "Né Thiên Thạch" (ARCADE-01) thành side-scroller / flappy.** Bay ngang, Byte ở 1/4 bên trái, `Space`/click/chạm để bay lên — thay hoàn toàn cơ chế cũ (né thiên thạch rơi, di chuyển 4 hướng). Xem mô tả đầy đủ ở mục 2. **Ba quyết định đáng ghi lại:** (1) **Sân dùng hệ toạ độ ảo cố định 800×500** rồi `ctx.setTransform` scale ra số pixel thật của khung — bản cũ `fit()` lấy W/H theo kích thước màn hình nên **điện thoại và desktop có độ khó khác nhau** (khe hẹp so với chiều cao sân không giống nhau); game flappy thì đó là lỗi cân bằng, không phải chi tiết nhỏ. (2) **Đá của cột được `ctx.clip()` đúng theo 2 hình chữ nhật hitbox** → hình vẽ *bằng đúng* vùng chết, không có chuyện "trông như chưa chạm mà đã thua" (bản cũ dùng hitbox tròn xấp xỉ hình đá). (3) **Thiên thạch tím vào ví tạm, chốt một lần ở `onFinishGame(mined)`** thay vì `Economy.addAsteroids(1)` mỗi viên như trước — đúng yêu cầu, và khi ví lên API thì chỉ còn **một** lời gọi mạng mỗi lượt thay vì một lời gọi mỗi viên. **2 lỗi tự phát hiện khi rà lại và đã sửa:** cột mới sinh khi `lastCol.x < VW-colGap` làm cột **hiện ra giữa sân** (đổi thành `< VW`); và `Space`/`Enter` vừa là "bay lên" vừa kích hoạt nút đang được focus → **trừ 5 tt hai lần**; sửa bằng cách chặn `startRound()` khi đang chơi, `blur()` nút sau khi bấm, và bỏ qua phím khi con trỏ bàn phím đang ở `<button>` (để người dùng bàn phím vẫn bấm được nút trên bảng kết quả). **Kiểm thử (không có Node trên máy nên viết script Python):** `check_dodge.py` **19/19 đạt** (cân bằng ngoặc, 30 `$("id")` đều tồn tại, **31 khoá i18n khớp nhau ở cả vi và en**, mọi `data-i18n*`/`t("key")` đều có khoá, 0 asset lỗi, 0 class thiếu CSS); `check_idents.py` **0 tên tự do** (chỉ còn `$` là global của `ui-common.js`); `sim_dodge.py` port nguyên vật lý sang Python để đo độ khó — 1 cú bật + giữ phím lên **140px** trong 0,71s, khe lệch nhau tối đa 150px và có 1,58s giữa 2 cột nên **luôn với tới được**; bot bay 8×180s: **0,04 lần chết/phút** (0,12 nếu bấm chậm 0,4s/cú) → đúng mức "Diff: Dễ". Khe hẹp bóp từ 200→158px rồi **dừng** ở giây 30, tốc độ cuộn 190→300px/s rồi dừng ở giây 50 → khó tăng nhẹ rồi phẳng, chơi endless được lâu. **Đã cài Playwright + Chromium (theo yêu cầu của người dùng) và soi thật trên trình duyệt** — xem mục kế tiếp. ⚠️ Vẫn ghi `localStorage` cho kỷ lục (`astroq-dodge-best-v2`) và ví (qua `economy.js`) — **trái quy tắc 2 mục 6**, nhưng API ví chưa tồn tại ở `../AstroqSV/`; đã dồn toàn bộ điểm nối vào **một hàm `onFinishGame()`** để lúc chuyển sang API chỉ sửa đúng chỗ đó. — `game-dodge.html`, `css/game-dodge.css`
- **Viết lại tagline + mô tả 2 thẻ game ở Sảnh.** ARCADE-01 "Bay ngang & né tránh" (nhấn mạnh bay ngang qua khe hẹp + thu thạch tím), ARCADE-02 "Phòng thủ 360°" (bảo vệ Trạm Vũ Trụ ở tâm, bắn thạch vàng mở Quiz sạc đạn). Viết **chữ thường trong dữ liệu** vì `.gcard .tag` đã có `text-transform:uppercase` — không hardcode chữ hoa. Thêm đủ bản EN theo quy ước 4. ⚠️ Mã `ARCADE-01/02` **sinh động theo thứ tự sau khi sort** (`ready` trước `soon`), nên thêm game `ready` xen vào trước `defender` là số hiệu đổi. — `games.html` Phát hiện khi rà dữ liệu sau lượt test thật của người dùng: `trangtt.tshn@gmail.com` có **2 hồ sơ, 2 uid** nhưng Firebase chỉ còn 1 tài khoản. Log CloudWatch cho thấy `POST /auth/register` lúc 10:55:10 trả **202** dù email đó vừa kích hoạt xong lúc 10:54:10 — đáng lẽ 409. **Không tái hiện được** (thử lại ngay sau khi kích hoạt thì 409 đúng) nên nguyên nhân chưa rõ; không suy đoán. Nhưng hậu quả thì xác minh chắc chắn: **`ImportUsersAsync` khi trùng email ÂM THẦM ghi đè tài khoản cũ** — không ném lỗi, `FailureCount = 0`, uid cũ biến mất khỏi Firebase. Hai thứ cộng lại = đăng ký lại email của người khác là xoá được tài khoản của họ. **Cách sửa không phụ thuộc vào hành vi Firebase:** thêm bản ghi `PK=EMAIL#<email>, SK=ACCOUNT`; luồng kích hoạt **giành chỗ bằng `PutItem` + `ConditionExpression attribute_not_exists(PK)` TRƯỚC khi gọi Firebase** — thua thì `reason=already` và không đụng Firebase; hỏng giữa chừng thì `ReleaseEmailAsync` nhả chỗ (không nhả là khoá vĩnh viễn email đó). `/register` hỏi `db.EmailTakenAsync` trước rồi mới hỏi Firebase. Thêm một lớp nữa: sau khi giành chỗ vẫn kiểm Firebase để bắt tài khoản tạo **trước** khi có cơ chế này, gặp thì báo `already` chứ không import đè. **Vá dữ liệu:** tạo bản giữ chỗ ngược cho tài khoản đang sống, xoá 13 dòng hồ sơ mồ côi. Kiểm thử `test_dup.py`: **13/13** — trong đó có đúng kịch bản đã gây hỏng (ép một bản ghi chờ rồi kích hoạt lần hai → uid **không** bị ghi đè, vẫn đúng 1 hồ sơ, mật khẩu ban đầu vẫn đăng nhập được). Chạy lại `test_flow` 34/34 và `test_origins` 15/15. — `AstroqSV/src/AstroqSV.Api/Data/DynamoContext.cs`, `Endpoints/AuthEndpoints.cs`, `docs/backend-astroqsv.md`
- **Chạy song song local và server.** Một backend duy nhất phục vụ cả hai. Mấu chốt: Lambda đọc header `Origin` của lời gọi `POST /auth/register`, **đối chiếu allowlist**, rồi ghi vào trường `continueUrl` của bản ghi chờ — phải ghi **ngay lúc đăng ký** vì khi bấm link thì request đến từ ứng dụng email, không còn `Origin` để biết người này xuất phát từ đâu. Đăng ký ở `localhost:8000` thì kích hoạt xong về `localhost:8000`, ở `astroq.org` thì về `astroq.org`. ⚠️ **Bắt buộc đối chiếu allowlist** — tin thẳng `Origin` là mở toang *open redirect* (kẻ tấn công đăng ký hộ người khác rồi trỏ link về trang của hắn); còn đối chiếu **lần nữa** ngay trước khi chuyển hướng, phòng khi allowlist bị thu hẹp sau đó. **Allowlist chỉ khai báo MỘT chỗ**: tham số `AllowedOrigins` trong `template.yaml` → vừa `!Ref` cho CORS của API Gateway, vừa `!Join` thành biến `ALLOWED_ORIGINS` cho `Services/Origins.cs` (trước đó danh sách này bị chép làm 2 bản ở `template.yaml` và `Program.cs`). Frontend: `js/api.js` viết lại — `PROD_API`/`LOCAL_API`, đổi chế độ bằng `?api=prod|local|<url>|reset` và nhớ ở `localStorage["astroq-api"]`; mặc định khi ở máy vẫn trỏ lên AWS (xem giao diện là việc thường xuyên, bật backend là việc hiếm). Thêm huy hiệu `.env-badge` góc dưới-trái, **chỉ dựng khi không phải bản thật**. Backend chạy ở máy bằng `dotnet run` cổng **5080** (`Properties/launchSettings.json` mới) — **không cần Docker** vì `AddAWSLambdaHosting` chỉ kích hoạt khi thật sự chạy trong Lambda; nhưng vẫn dùng DynamoDB/SES/Secrets Manager **thật** nên dữ liệu test ở máy và ở server chung một bảng. Kiểm thử: **15/15** cho 5 trường hợp origin (astroq.org · localhost:8000 · 127.0.0.1:5173 · origin giả mạo `astroq.org.evil.co` · không gửi Origin), **34/34** chạy lại toàn bộ luồng đăng ký, và đã kiểm backend local thật (health 200, CORS preflight 204, đăng ký 202, `continueUrl` đúng). — `AstroqSV/`, `js/api.js`, `js/firebase-auth-ui.js`, `css/landing-app.css`, `docs/backend-astroqsv.md`
- **Chốt 5 quy tắc cách làm việc** (ghi ở đầu mục 6): làm cả client lẫn backend · bỏ localStorage, dùng API + DB · tên bảng DB có tiền tố `Astroq` · test API độc lập trước khi tích hợp · tự triển khai & tự kiểm thử vì người dùng không đọc code. ⚠️ **Hệ quả cần làm dần:** `economy.js` (số dư Thiên thạch tím), `astroq-read` (bài đã đọc), `astroq-dodge-best` (kỷ lục game) và tiến độ quiz vẫn đang nằm hoàn toàn trong `localStorage` — theo quy tắc 2 thì phải chuyển sang API. Việc này **đảo hướng tin cậy**: server quyết thưởng, client không được gửi số dư lên. — `CLAUDE.md`
- **Đăng ký 2 giai đoạn: DynamoDB giữ đăng ký chờ, Firebase chỉ giữ tài khoản đã kích hoạt.** Yêu cầu: AWS lưu tài khoản đang đăng ký + gửi email, email không được trùng với **cả** tài khoản đã có **lẫn** đăng ký đang chờ, xác thực xong mới tạo trên Firebase, link kích hoạt sống **10 phút**. **Backend** (`AstroqSV/`): `PasswordHasher.cs` mới — băm **PBKDF2-SHA256 100.000 vòng** ngay lúc đăng ký rồi vứt mật khẩu thô (bản ghi DynamoDB còn nằm trong backup và PITR, mà trẻ em hay dùng lại mật khẩu); token kích hoạt 32 byte ngẫu nhiên nhưng DB **chỉ lưu SHA-256 của nó**, so sánh bằng `FixedTimeEquals`. `DynamoContext`: thêm `PENDING#<email>/SIGNUP` + `ttl = expiresAt + 86400` để DynamoDB tự dọn. `FirebaseService.ImportVerifiedUserAsync` dùng `ImportUsersAsync` + `Hash.Pbkdf2Sha256` để đẩy thẳng hash lên — người dùng vẫn đăng nhập bằng mật khẩu ban đầu (đã kiểm thật bằng `accounts:signInWithPassword`). 3 endpoint: `POST /auth/register` (202) · `POST /auth/resend` · `GET /auth/activate?e=&t=` (302 kèm `?activated=&reason=`). **Frontend**: thêm `js/api.js` (chỗ duy nhất chứa `API_BASE`, có timeout 20s, không bao giờ ném lỗi ra ngoài); `firebase-auth.js` đổi `register()` sang gọi backend và `resendVerification(email)` nhận email vì **lúc này chưa có phiên Firebase nào** (tài khoản còn chưa tồn tại); bỏ `checkVerified()` — việc kích hoạt do server làm khi bấm link. Thêm bộ xử lý `?activated=…&reason=…` tự mở popup đúng pane (hết hạn → form Đăng ký, còn lại → Đăng nhập, điền sẵn email) rồi `history.replaceState` dọn URL. **Kiểm thử: 34/34 đạt** (không lưu mật khẩu thô · chưa tạo Firebase ở giai đoạn 1 · token sai/hết hạn/dùng lại · gửi lại làm token cũ chết · đăng nhập được sau kích hoạt · `emailVerified=true` · tên tiếng Việt không hỏng · PROFILE+WALLET khớp uid). **2 lỗi gặp phải:** (1) `PUBLIC_API_URL: !Sub 'https://${Api}...'` trong environment của Lambda gây **circular dependency** (`ApiFunction`→`Api`→`ApiFunction`) — sửa bằng cách dựng link từ host của chính request; (2) `sam build` báo `PermissionError` khi xoá `.aws-sam` (OneDrive giữ file) — xoá tay rồi build lại. CORS thêm `https://www.astroq.org` + cổng 8000 cho `python -m http.server`. ⚠️ Tài khoản AWS đang gắn `AdministratorAccess`, nên thay bằng bộ quyền hẹp. ⚠️ Chưa kiểm email vào Inbox hay Spam (SPF/DMARC). — `AstroqSV/`, `js/api.js`, `js/firebase-auth.js`, `js/firebase-auth-ui.js`, `landing-app.html`, `docs/backend-astroqsv.md`
- **Quét wiki: mọi bài phải có link về `https://astroq.org/`.** Kết quả quét: **không trang nào thiếu link** — cả 22 trang (20 bài × 2 ngôn ngữ + 2 mục lục) đều có 4 link về trang chủ (logo `.brand`, breadcrumb, nút CTA, footer; riêng 2 mục lục có 5 vì thêm nút TRANG CHỦ/HOME ở topbar). **Vấn đề thật:** tất cả đều là link tương đối `../index.html` (VI) / `../../index.html` (EN), phân giải thành `https://astroq.org/index.html` — **khác chuỗi URL với canonical trang chủ là `https://astroq.org/`**, và không đồng bộ với `index.html` vốn đã dùng `href="/"` cho logo. Đã đổi **90 link** sang **`href="/"`** để trỏ đúng URL chuẩn. **Cẩn thận khi sửa:** trong `wiki/en/` link "Tiếng Việt" ở mục lục là `href="../"` (mục lục wiki VI, KHÔNG phải trang chủ) và ở bài viết là `../<slug-vi>.html` — không được gom chung; đường dẫn asset `../css/`, `../img/` cũng giữ nguyên. Đã kiểm sau khi sửa: 22/22 trang có `href="/"`, 0 sót link tương đối về trang chủ, asset và link chuyển ngữ nguyên vẹn. ⚠️ **Hệ quả:** mở wiki bằng `file://` ở máy sẽ không bấm về trang chủ được (`/` = gốc ổ đĩa) — xem thử bằng `python -m http.server 8000` trong `AstroQhtml/`. ⚠️ **`scratchpad/gen_wiki_data.py` + `gen_wiki_data_en.py` (generator ghi ở mục 2) không còn trên máy**, nên sửa trực tiếp HTML; nếu khôi phục generator để sinh lại wiki thì phải sửa template trong đó, không thì thay đổi này bị ghi đè. Giữ nguyên 4 link tuyệt đối `https://astroq.org/` sẵn có trong mục *Nguồn tham chiếu* của 4 bài hệ sinh thái. — `wiki/*.html`, `wiki/en/*.html`

### 2026-07-26
- Viết `docs/backend-astroqsv.md` — kế hoạch backend **AstroqSV**: AWS Lambda + **.NET 10** + DynamoDB + SES, API Gateway HTTP API, thư mục đặt **ngang hàng** `AstroQhtml/` (không đặt bên trong vì repo đó deploy public). Đã kiểm tài liệu AWS: **`dotnet10` là managed runtime** trên Amazon Linux 2023, hỗ trợ tới 14/11/2028 — `dotnet9` chỉ chạy dạng container, `dotnet8` hết hạn 10/11/2026, nên chọn .NET 10 là đúng. Nội dung: kiến trúc, single-table DynamoDB, 9 endpoint, `Program.cs` + `template.yaml` (SAM) chạy được, lệnh CloudWatch, thiết lập SES, ước lượng chi phí. ⚠️ **Chưa deploy được:** máy chưa có dotnet/aws/sam/docker và không có credential AWS. ⚠️ **Cần chốt trước một quyết định:** danh sách API có `đăng ký`/`đăng nhập` + SES gửi email = **làm lại việc Firebase Auth vừa làm**. Tài liệu khuyến nghị **phương án A** (Firebase lo danh tính, Lambda verify JWT rồi chỉ lo dữ liệu game) thay vì tự quản lý mật khẩu trẻ em; phương án B viết ở phụ lục. **Client KHÔNG cần đổi công nghệ** — HTML + vanilla `fetch` gọi REST API bình thường; việc thật là chuyển từ offline-first sang cần mạng và đảo hướng tin cậy của `economy.js` (server quyết thưởng, client không gửi số dư). — `docs/backend-astroqsv.md`, `docs/firebase-auth.md`
- **Bắt buộc xác minh email trước khi vào app.** Firebase **không có** luồng "xác minh xong mới tạo tài khoản" — `createUserWithEmailAndPassword` tạo ngay lập tức. Cách đạt đúng hiệu quả đó: tài khoản được tạo nhưng **`register()` cố ý KHÔNG gọi `syncProfile()`**, nên `localStorage["astroq-user"]` chưa có `uid` → mọi lối vào app (vốn đều dựa vào `uid`) đều bị chặn. `login()` gọi `reload(user)` rồi kiểm `emailVerified`; chưa xác minh thì trả `{needVerify:true}` và **giữ nguyên phiên** (không `signOut`) để còn gửi lại được email. Chỉ khi `checkVerified()` thấy đã xác minh mới ghi hồ sơ và cho vào. Thêm `resendVerification()`. Giao diện: pane thứ 3 **`#auth-verify`** trong popup (icon phong bì, hiện email đang chờ, 3 nút *Tôi đã xác minh xong* / *Gửi lại email* / *Quay lại đăng nhập*, ghi chú nhắc kiểm tra Spam) + 6 khoá i18n × vi/en + CSS `.auth-verify`. Nút *Quay lại đăng nhập* gọi `logout()` để bỏ phiên chưa xác minh. ⚠️ **Mẫu email xác minh mặc định là tiếng Anh**, gửi từ `noreply@astroq-782f7.firebaseapp.com` — sửa ở Console → Authentication → Templates nếu muốn tiếng Việt. — `js/firebase-auth.js`, `js/firebase-auth-ui.js`, `landing-app.html`, `css/landing-app.css`
- **Bật xác thực thật: điền config Firebase project `astroq-782f7`.** Dùng **`<script>` tag / CDN ES module**, KHÔNG dùng npm (npm cần bundler + build step, trái `CLAUDE.md`). Nâng SDK 12.4.0 → **12.16.0** theo bản console gợi ý. **Cố ý bỏ `measurementId` / không bật Analytics** — thêm ~50 KB SDK và là script theo dõi hành vi trên nền tảng cho trẻ em. Đã kiểm bằng probe REST (`accounts:signInWithPassword` với email không tồn tại, không tạo tài khoản nào): trả `INVALID_LOGIN_CREDENTIALS` → **API key hợp lệ và provider Email/Password đã bật**. **Vá một lỗ hổng phát sinh khi bật auth thật:** nút "Trải nghiệm ngay" ở `landing-app.html` vào thẳng dashboard nếu `localStorage` còn hồ sơ — nghĩa là hồ sơ cũ từ thời demo (không có `uid`) bỏ qua được toàn bộ đăng nhập. Giờ chỉ đi thẳng khi hồ sơ **có `uid`** (tức gắn với tài khoản Firebase thật). Đính chính tài liệu: Authorized domains **không** chặn đăng nhập Email/Password thuần (chỉ áp cho OAuth và link trong email) — trước đó tôi ghi quá tay. — `js/firebase-config.js`, `js/firebase-auth.js`, `landing-app.html`, `docs/firebase-auth.md`
- **Tích hợp Firebase Authentication (provider Email/Password) thay cho đăng nhập giả.** Thêm 3 module ES: `js/firebase-config.js` (chỉ chứa config, chỗ duy nhất cần sửa), `js/firebase-auth.js` (logic + bảng lỗi Firebase → thông báo VI/EN, **import động** SDK nên chưa cấu hình thì không tải byte nào), `js/firebase-auth-ui.js` (gắn vào popup). `landing-app.html`: **xoá 2 handler giả** (chúng bỏ qua mật khẩu hoàn toàn), mở `window.AstroQAuthUI = {close, toast, t, showPane}` cho module dùng lại popup, thêm nút **Quên mật khẩu** (+ khoá i18n `auth_forgot`, CSS `.auth-forgot`). `dashboard.html`: đăng xuất gọi `AstroQAuth.logout()` để xoá cả phiên Firebase. **`js/auth-flow.js`: sửa 2 lỗi tiềm ẩn** — `startJourney()` ghi đè nguyên object làm **mất `uid`** vừa lưu lúc đăng ký (đổi sang `Object.assign({}, existing, …)`), và reset `astroq-asteroids` về 0 mỗi lần đổi nhân vật (giờ chỉ khởi tạo khi khoá chưa tồn tại). **Thiết kế quan trọng: chưa điền config → mọi thứ tự lùi về chế độ demo cũ**, trang không bao giờ vỡ; điền config vào là tự động chuyển sang xác thực thật. ⚠️ Firebase Auth chỉ lo *danh tính* — Purple Meteors và tiến độ vẫn ở localStorage từng máy, muốn đồng bộ đa thiết bị phải thêm Firestore (Bước 7 trong `docs/firebase-auth.md`). Thêm `Disallow: /docs/` vào robots.txt vì file .md không gắn được meta noindex. — `js/firebase-*.js`, `landing-app.html`, `dashboard.html`, `js/auth-flow.js`, `css/landing-app.css`, `robots.txt`, `docs/`
- **Dọn mã chết còn sót sau đợt refactor.** `dashboard.html`: bỏ `GAME_COST`, `openModal` (chỉ `closeModal` được dùng), `pad`, và toàn bộ hạ tầng toast — `var toast`, phần tử `#toast`, rule `.toast` trong `css/dashboard.css` — vì sau khi nút "Chơi Game" chuyển thành điều hướng thẳng sang `games.html` và modal quiz demo bị gỡ thì **không còn lời gọi `toast()` nào**. `games.html`: bỏ `bumpGem` và `var n`. Đã xác minh từng cái: không xuất hiện trong `on*=` của HTML, không gọi động. Kiểm tra sau khi xoá: 10/10 trang **0 khai báo thừa**, HTML cân bằng, mọi `$("id")` vẫn có phần tử tương ứng, 0 asset lỗi. ⚠️ Nếu sau này cần toast ở dashboard thì phải thêm lại cả 3 phần (phần tử + `AstroQ.makeToast` + CSS `.toast` riêng của trang; phần chung `.toast.show`/`.toast-msg`/`.toast-ic` vẫn nằm ở `css/common.css`). — `dashboard.html`, `games.html`, `css/dashboard.css`
- **Bản tiếng Anh của Wiki + mở phân mục cho AI bot.** (1) Sinh `wiki/en/` — 10 bài + mục lục, slug tiếng Anh riêng, nội dung viết mới chứ không dịch máy; generator assert đoạn trả lời 40–60 từ cho cả 2 ngôn ngữ (EN rơi vào 41–50 từ). Mỗi trang có **3 thẻ `hreflang`** (vi/en/x-default) + nút chuyển ngữ ở topbar trỏ đúng bài tương ứng. (2) `sitemap.xml` sinh lại: **23 URL** (trang chủ + 2 mục lục + 20 bài) kèm **66 khai báo `<xhtml:link hreflang>`**. (3) `robots.txt` viết lại — **gộp về một nhóm `User-agent: *` duy nhất** (trước đó tôi thêm nhóm `*` thứ hai; hợp lệ nhưng một số parser chỉ đọc nhóm khớp đầu tiên), thêm `Allow: /wiki/`, `/wiki/en/`, `/llms.txt` cho cả 7 AI bot. (4) **`llms.txt` thêm mục Wiki** liệt kê đủ 20 bài kèm đoạn trả lời — đây mới là thứ AI engine đọc để biết có gì mà trích. (5) Link Wiki trên trang chủ đổi theo ngôn ngữ (`wiki/` ↔ `wiki/en/`). **Sửa lỗi phát hiện khi rà:** `js/index.js` vẫn dùng 3 vòng lặp i18n cũ nên `data-i18n-alt`/`-aria` vừa thêm ở trang chủ không hề được dịch — đã chuyển sang `AstroQ.applyTexts(t)` như 10 trang còn lại. Kiểm tra: 22 trang wiki, 0 liên kết hỏng, hreflang hợp lệ 22/22, ngoặc CSS/JS cân bằng, i18n 11/11 trang đồng bộ. — `wiki/en/`, `wiki/`, `robots.txt`, `sitemap.xml`, `llms.txt`, `js/index.js`, `css/wiki.css`
- **Biên soạn bộ 10 bài AEO Wiki** vào `wiki/` + trang mục lục, `css/wiki.css` (tái sử dụng `css/common.css`, **không thêm dependency**, không JS). Chủ đề: Vật lý lượng tử · AI vs Robotics · Hố đen · ISS · Sao Hỏa · Big Bang · Tuổi học lập trình · Comet & Byte · Purple Meteors · Ứng dụng thiên văn. Mỗi bài đúng cấu trúc Direct Answer (khối `.answer` **40–60 từ**, generator tự assert), có bảng so sánh, Góc Khám Phá astroQ và khối nguồn. **26 URL nguồn NASA/ESA/MIT đều đã curl kiểm tra trả 200 trước khi đưa vào bài** — không bịa link. JSON-LD `Article` + `FAQPage` + `BreadcrumbList` mỗi trang; mục lục dùng `CollectionPage`. Thêm 11 URL vào `sitemap.xml` và **liên kết nội bộ từ trang chủ** (sau FAQ + footer) vì sitemap thôi thì Google đánh giá thấp. Lưu ý nội dung: bài 8 nói rõ **Byte là nhân vật hướng dẫn có kịch bản biên soạn sẵn, không phải mô hình AI trò chuyện tự do**; mọi bài đều ghi astroQ.org không liên kết/tài trợ/chứng thực bởi NASA hay ESA. ⚠️ Wiki hiện **chỉ có tiếng Việt** (site vốn song ngữ) — cần bản EN nếu muốn phủ truy vấn tiếng Anh. — `wiki/`, `css/wiki.css`, `index.html`, `js/index.js`, `css/index.css`, `sitemap.xml`
- **Rà soát & đồng bộ ngôn ngữ toàn site.** Viết script kiểm tra 11 trang theo 5 tiêu chí (khoá lệch vi/en, object `{vi:…}` thiếu `en:`, chuỗi hardcode, thuộc tính không i18n, text thiếu `data-i18n`). Kết quả: **nội dung hiển thị của mọi trang vốn đã song ngữ đầy đủ** (dashboard `STATS`, select `CHARACTERS`, games `GAMES`, game-defender `QUIZ`, explorer `BODY_NAMES`/`STARS`/`REGION_VI` — script báo nhầm vì chưa hiểu cấu trúc `{vi:…, en:…}`). **Vấn đề thật là ~35 thuộc tính** `title`/`aria-label`/`alt`/`placeholder` hardcode tiếng Việt, không đổi theo ngôn ngữ — đây là tooltip và nội dung trình đọc màn hình đọc lên. Cách sửa: thêm **`AstroQ.applyTexts(t)`** vào `ui-common.js` (dịch cả nội dung lẫn thuộc tính qua `data-i18n-title/-aria/-alt/-ph`), gắn thuộc tính + thêm khoá vào cả 2 từ điển ở 11 trang, và **gom 2–3 vòng lặp `querySelectorAll` trùng nhau ở mỗi trang về 1 lời gọi chung**. Ngoài ra: `alt` của ảnh Thiên thạch tím trong toast dùng chung giờ đổi theo ngôn ngữ (`ttImg()` thay hằng `TT_IMG`); 2 chỗ `alt` nằm trong chuỗi JS (quiz, games) nội suy `t("tt_name")` vì chúng render lại sau `applyTexts`. Kiểm tra: 11/11 trang mọi khoá `data-i18n-*` đều có ở cả `vi` và `en`, 2 từ điển cân bằng số khoá, 0 asset lỗi. — `js/ui-common.js`, toàn bộ `*.html`, `js/index.js`, `js/auth-flow.js`
- **Đấu nối form waitlist với Formspree** (`https://formspree.io/f/xkodplgp`). `js/index.js`: thay hàm mô phỏng bằng `fetch` POST `FormData` kèm header `Accept: application/json` (Formspree khi đó trả JSON thay vì redirect, nên không rời trang). Gửi kèm `lang` để biết người đăng ký xem bản VI hay EN. Form có thêm `action`+`method` để **vẫn gửi được nếu JS lỗi**; honeypot đổi tên `company`→**`_gotcha`** (Formspree tự lọc ở server) + hidden `_subject`. **UX:** nút đổi "Đang gửi..." và `disabled` khi gửi · thành công (2xx) → reset ô email, hiện thẻ success có hiệu ứng (icon nảy `donePop`, nét check tự vẽ `doneDraw` — chỉ áp lên `<path>`, KHÔNG lên `<circle>` kẻo vòng tròn đứt nét, vòng sáng lan `doneRing`) + toast · lỗi → thông báo thân thiện **bằng tiếng Việt**, không hiện thẳng lỗi tiếng Anh của Formspree (`"should be an email"`) mà chỉ `console.warn` cho lập trình viên. **Luôn backup vào `localStorage["astroq-waitlist"]`** kèm cờ `sent` (true = đã lên server) — mất mạng vẫn không mất lead. Đã test thật endpoint: 200 `{ok:true}` khi hợp lệ, 422 + mảng `errors` khi email sai. - **Ảnh OG 1200×630**: `img/og-cover.jpg` (84 KB) sinh bằng Pillow từ chính font + màu của trang (script `scratchpad/make_og.py`, dùng bản TTF biến thiên đầy đủ tải từ google/fonts — bản subset trong `fonts/` thiếu glyph tiếng Việt nên render ra ô vuông). Thay `og:image`/`twitter:image` (trước đó mượn `img/m1.png` ảnh dọc), thêm `og:image:type/width/height/alt`. — `index.html`, `js/index.js`, `css/index.css`, `img/og-cover.jpg`
- Thêm 3 tệp tĩnh ở gốc cho SEO/AEO: **`robots.txt`** (mở `Allow: /` cho `*` + 7 AI bot: GPTBot, ChatGPT-User, PerplexityBot, ClaudeBot, Claude-Web, Google-Extended, Applebot-Extended; khai báo Sitemap). **Cố ý KHÔNG `Disallow` `landing-app.html`** dù trang đó `noindex` — chặn ở robots.txt sẽ khiến bot không đọc được thẻ meta noindex. **`llms.txt`** (Markdown theo llmstxt.org: mô tả nền tảng, khoang lái 3D, 2 linh vật Comet/Byte, Purple Meteors, nguồn NASA/ESA, trạng thái pre-launch, + mục English summary) — ghi rõ **lời bài đọc/quiz là do đội ngũ biên soạn lại cho trẻ em, KHÔNG phải bản sao tài liệu NASA/ESA, và không có quan hệ liên kết/tài trợ/chứng thực nào** với NASA hay ESA, vì đây là file AI trích dẫn nguyên văn. **`sitemap.xml`** — chỉ 1 URL `https://astroq.org/`, vì giai đoạn pre-launch chỉ trang chủ là công khai. Đồng thời thêm **`noindex,follow` cho cả 9 trang app** (`dashboard`, `select`, `learn`, `library`, `quiz`, `games`, `game-dodge`, `game-defender`, `explorer`) — trước đó chúng không có meta robots nên mặc định vẫn được index, trong khi robots.txt vừa mở toàn bộ cho crawler. **Chỉ `index.html` được lập chỉ mục.** **Khi app mở cửa cần làm 3 việc:** (1) xoá meta robots ở 9 trang app + `landing-app.html`; (2) bổ sung URL vào `sitemap.xml`; (3) thêm lối vào app từ trang chủ. — `robots.txt`, `llms.txt`, `sitemap.xml`, 9 trang app
- **Go-live landing "Sắp ra mắt" + hoán đổi trang chủ.** (1) Bỏ `noindex` → `index,follow,max-image-preview:large`; (2) xoá nhãn `PREVIEW` (thẻ HTML + khối CSS `.preview-badge`); (3) canonical & `og:url` → `https://astroq.org/`. Vì canonical trỏ về gốc thì trang phải **nằm ở** gốc (canonical trỏ sang URL khác sẽ vô hiệu hoá `index`), nên hoán đổi file: `index.html`→`landing-app.html` (+ `css/index.css`→`css/landing-app.css`, thêm `noindex,follow`), `coming-soon.html`→`index.html` (+ `css/coming-soon.css`→`css/index.css`, `js/coming-soon.js`→`js/index.js`). Sửa 2 liên kết nội bộ để luồng app không rơi vào trang "sắp ra mắt": nút Đăng xuất ở `dashboard.html` và logo ở `select.html` → `landing-app.html`; logo trang chủ → `/`. Đã kiểm: 11 trang 0 asset lỗi, 2 khối JSON-LD parse OK, 45/45 khoá i18n đủ vi+en, 13 id JS đều tồn tại. ⚠️ **Hệ quả:** app giờ không có lối vào công khai từ trang chủ — chỉ vào bằng URL trực tiếp (`landing-app.html`, `dashboard.html`…). Đúng ý đồ giai đoạn pre-launch; khi mở cửa thì thêm link + bỏ `noindex` ở `landing-app.html`. — `index.html`, `landing-app.html`, `css/`, `js/`, `dashboard.html`, `select.html`
- **Tự host font, bỏ Google Fonts: 621 KB → 101 KB (−84%)** và cắt 2 kết nối tới domain ngoài. Lý do: sau khi ảnh nền còn 99 KB thì font trở thành nút thắt lớn nhất trên mạng yếu — đo thật thấy 11 file woff2, riêng các subset mà trang tiếng Việt phải tải (`latin`+`latin-ext`+`vietnamese`) đã là **621 KB**, và thẻ `<link>` tới `fonts.googleapis.com` **chặn hiển thị** lại nằm ở domain khác (tốn thêm DNS+TCP+TLS). Cách làm: (1) bỏ `latin-ext` — đã kiểm bằng script là `latin`+`vietnamese` phủ **đủ mọi** ký tự tiếng Việt (ă/â/đ/ê/ô/ơ/ư + toàn bộ dấu + ₫), riêng nó đã cắt 323 KB; (2) đổi Space Grotesk & Inter sang bản **biến thiên `wght@400..700`** — 1 file lo mọi độ đậm thay cho 3–4 file tĩnh mỗi họ (15 file → 5 file); (3) tải 5 file woff2 về `fonts/`, sinh **`css/fonts.css`** (`@font-face` + `unicode-range` + `font-display:swap`, URL `../fonts/…` vì CSS nằm trong `css/`); (4) thay 3 thẻ Google Fonts ở **cả 11 trang** bằng 1 thẻ `<link href="css/fonts.css">`. Sinh lại font bằng `scratchpad/selfhost_fonts.py`. **Lưu ý khi thêm ngôn ngữ mới** (Nga/Hy Lạp/Nhật…): phải bổ sung subset tương ứng vào `fonts/` + `css/fonts.css`, không còn Google tự lo nữa. Ghi chú: 2 chỗ `font-weight:800` trong `css/select.css` dùng Space Grotesk (tối đa 700) nên vốn đã hiển thị ở 700 — không đổi gì. — `fonts/`, `css/fonts.css`, toàn bộ `*.html`
- **Tối ưu toàn bộ kho ảnh: 72,4 MB → 2,79 MB (−96%)**, không hỏng trang nào (smoke test 11 trang, 0 asset lỗi). Cách làm: đọc CSS lấy **kích thước hiển thị lớn nhất thật sự** của từng ảnh → resize LANCZOS về **~4× kích thước đó** → ép **bảng màu 256 (FASTOCTREE, giữ alpha)**. Ghi đè tại chỗ nên **0 dòng code phải sửa**. Cụ thể: `3d/*` → cạnh dài 480px (roster `.char img` 74px, `.hud-info .preview img` 120px) · `ava/*` → 512px (`.ava-frame` 132px, Castor `zoom:1.6` ≈ 211px) · `img/3qok` 768 · `img/raica1` 960 (`.f-raica1` 240px) · `img/luna1|cho1|q1|qb1|qg1` 512 (`.floaty`/`.ic-*` 124px) · `img/luna2` 320 (canvas `sizeShip` 64px/`sizeByte` 78px) · 19 ảnh chưa ai tham chiếu → 720 (để rộng phòng khi dùng sau). **Ảnh nền dashboard** (`background/Khoangluna.png` 6,9 MB, 2550×1664, PNG là sai định dạng cho ảnh dạng photo, không có alpha) → bỏ `background-image` trong CSS, chuyển sang **`<picture>` trong `dashboard.html`** với 4 biến thể sinh lại **từ ảnh gốc** (không nén chồng): `Khoangluna-1920.avif` **99 KB** · `-1920.webp` 149 KB · `-1280.avif` 57 KB · `-1280.webp` 87 KB. Trình duyệt tự chọn định dạng + độ phân giải qua `sizes="max(100vw,153vh)"` (153vh = bề rộng ảnh sau `object-fit:cover`, tỉ lệ 1920/1253 ≈ 1,53 — nhờ vậy màn dọc/điện thoại vẫn nhận bản đủ nét). Thêm `fetchpriority="high"` vì ảnh nền khai báo trong CSS bị trình duyệt phát hiện muộn. **6,9 MB → 99 KB (−98,6%)**. `.bg-photo` giờ chỉ lo bố cục (`img{object-fit:cover}`), lớp phủ tối `::after` giữ nguyên (đứng sau `<picture>` trong DOM nên vẫn nằm trên ảnh). Đã xoá file PNG gốc. **Mọi bản gốc vẫn nằm trong lịch sử git** — khôi phục: `git show HEAD~1:background/Khoangluna.png > background/Khoangluna.png`. **Đã xoá 8 ảnh trùng** trong `img/` (`b3d, bao3D, chim3D, cho2, cu3d, cua3d, m3d, q3d` — bản y hệt đã có trong `3d/`, không trang nào tham chiếu). ⚠️ Còn **11 ảnh trong `img/` chưa ai dùng** (b2/b3/b4, m2/m3/m4, q3/q4, raica2/3/4) — giữ lại vì có thể là art dự phòng. **Kho ảnh cuối cùng: 2,15 MB / 45 file.** — `img/`, `3d/`, `ava/`, `background/`, `css/dashboard.css`
- **Tối ưu 3 ảnh nặng nhất** (Pillow, resize LANCZOS + `optimize`): `img/tt.png` 1080² → **256²** (1.141 → 63 KB, −94%), `img/m1.png` & `img/b1.png` 1080×1620 → **720×1080** (1.113 → 401 KB và 920 → 310 KB). Ghi đè tại chỗ nên **không phải sửa dòng code nào** (tt.png được tham chiếu ~40 chỗ ở 9 file + vẽ trên canvas 2 game). Độ phân giải mới vẫn dư 4–8× so với kích thước hiển thị lớn nhất (tt ~30px; m1 124px ở `.floaty`; b1 168px ở `.f-b1`). Thêm `img/m1-320.webp` + `img/b1-320.webp` (~28 KB/ảnh) dùng qua `<picture>` chỉ trong `coming-soon.html`. **Bản gốc vẫn nằm trong lịch sử git** — khôi phục bằng `git show HEAD:img/tt.png > img/tt.png`. Kết quả: `coming-soon.html` tải về **179 KB** (trước ~3,2 MB). ⚠️ **Còn lại chưa xử lý: thư mục ảnh tổng cộng ~72 MB** (`background/Khoangluna.png` 7 MB, `3d/*` và `img/*3d*` mỗi ảnh 1,7–3 MB) — nên làm một đợt tối ưu riêng. — `img/`
- Tạo landing page **"Coming Soon"** `coming-soon.html` (+ `css/coming-soon.css`, `js/coming-soon.js`) cho astroQ.org, responsive, đúng 2 rule mới (CSS ở file riêng, dùng lại `common.css`/`ui-common.js`/`icons.js`). Bảng màu thương hiệu: tím neon `#8A2BE2`, cyan `#00FFFF`, vàng meteor `#FFD700`. Bố cục: topbar (logo + trạng thái + đổi ngôn ngữ) · **Hero** (H1 gradient, phụ đề, **đếm ngược** tới `2026-08-01T00:00+07:00`, 2 CTA) · thẻ **linh vật Comet & Byte** (`img/m1.png`/`img/b1.png`, hiệu ứng float) · **form waitlist** (email + honeypot chống bot, validate, trạng thái loading → thẻ thành công, lưu `localStorage["astroq-waitlist"]`, `submitWaitlist()` mô phỏng API ~900ms) · khối **AEO** H2 "astroQ.org là gì?" + 4 trụ kiến thức + **FAQ `<details>`**. `<head>`: meta SEO/OG/Twitter, canonical, và **JSON-LD `EducationalApplication` + `FAQPage`** (5 Q&A khớp 1-1 với FAQ hiển thị — đã kiểm bằng script). Song ngữ VI/EN đủ 45 khoá ở cả 2 từ điển. **Không** dùng Tailwind CDN (trái rule "CSS ra file riêng" + "không thêm dependency"). Chưa gắn backend: email mới chỉ nằm trong localStorage của máy khách. — `coming-soon.html`, `css/coming-soon.css`, `js/coming-soon.js`
- **Tách toàn bộ CSS khỏi HTML + gom phần dùng chung** (quét cả 10 trang). (1) Mọi khối `<style>` inline → `css/<trang>.css`, HTML chỉ còn 2 thẻ `<link>`; phần **giao nhau** của các rule trùng selector ở ≥3 trang gom vào **`css/common.css`** (31 rule: reset, `.back-btn`, `.lang-switch`, `.currency`/`.gem`, `.toast`, `.modal`, `.starfield`/`.nebula`, `.tt-inline`, `.lic`, keyframes, reduced-motion) — chỉ gom khai báo **giống hệt nhau** nên computed style không đổi (đã kiểm bằng script so bộ `(selector, prop, value)` từng trang). (2) Inline `style="..."` **tĩnh** → class: `.brand-text`, `.ctl-row--gap`, `.stat .v.sm`, `.life .ring--hab`, `.loc-lock--open` (explorer), `.gcard .hud-line.foot`/`.online` (games), `.acts .btn-fee` (defender), `.auth-brand strong` (index), `.stat .s-ic img` (library); toast của `quiz.html` bỏ `el.style.cssText` → dùng `<div class="toast">` + CSS. (3) **JS dùng chung**: thêm `js/ui-common.js` (`AstroQ.$/getUser/setUser/clearUser/getLang/setLang/markLangButtons/initLang/makeToast/esc`) và `js/icons.js` (22 icon lucide, hàm `lic`); 10 trang + `js/auth-flow.js` bỏ bản sao `$`, đọc `astroq-lang`, `getUser`, `LIC`/`lic`, `toast`, wiring nút đổi ngôn ngữ & listener `storage`. Tổng: **−2.700 dòng lặp**. Lưu ý thay đổi hành vi nhỏ (có chủ đích): explorer nay **lưu** ngôn ngữ khi bấm `.lang-btn` và nhận `@media (prefers-reduced-motion)`; nội dung toast luôn bọc `<span class="toast-msg">`. — `css/`, `js/ui-common.js`, `js/icons.js`, toàn bộ `*.html`, `js/auth-flow.js`

### 2026-07-25
- Tạo `library.html` (Thư Viện Khám Phá / Space Knowledge Reader) — vanilla. Topbar: Về Trạm Tri Thức + ô tìm kiếm + lọc nguồn (Tất cả/NASA/ESA/AI&Tech) + số dư tt. Layout sidebar (danh mục Thiên văn/AI/Lượng tử/Robot có đếm số bài + thống kê Đã đọc/Đã thu thập) + feed (hero card ảnh HD + lưới card hover glow, lọc theo search/nguồn/danh mục). Reader modal: **ảnh HD NASA thật** (6 URL images-assets.nasa.gov đã kiểm tra 200 https, có fallback gradient+emoji khi lỗi), tóm tắt cho trẻ em, khung thoại mascot (m1/b1) giải thích thuật ngữ, credit + link nguồn, nút đọc ~5s nhận ☄️ (lưu `astroq-read`) + CTA "Làm Quiz bài này (+20)" → `quiz.html`. Icon lucide inline, song ngữ. learn.html: thẻ "Vào Thư Viện Reading" → `library.html` (thay cuộn). Kiểm tra: JS cân bằng ngoặc, id đủ, 6 ảnh https 200. — `library.html`, `learn.html`
- Mở rộng `scripts/fetch_nasa_news.py` lên **4 nguồn NASA**, lưu vào thư mục con riêng dưới `learningdata/astronomy/nasa_articles/`: `apod/`, `images/`, `mars/`, `techtransfer/` (mỗi thư mục có `_index.json`). Nguồn: **APOD**; **Image & Video Library** (tìm theo `--query`, tự lấy ảnh gốc **~4K** qua asset endpoint `~orig`, tắt bằng `--no-hi-res`); **Mars Rover Photos** (`--rover`, thử `latest_photos` rồi đi lùi `earth_date`, mặc định trần 250 ảnh); **TechTransfer** (`--tt-kind` patent/software/spinoff + `--tt-query`, gọi `technology.nasa.gov/api/query/{kind}/{term}`). `--source all` chạy cả 4. Thêm soft-404 (bỏ qua nguồn lỗi mà không crash), retry/429, UTF-8. **Test thật:** APOD ✓, Images ✓ (ra URL `~orig` 4K), TechTransfer ✓ (patent thật). **Lưu ý:** endpoint Mars trên `api.nasa.gov/mars-photos` hiện trả **404 (NASA proxy tạm ngưng)** — code đã xử lý mượt (in cảnh báo, bỏ qua Mars), sẽ chạy lại khi API hồi phục. Nên đặt `NASA_API_KEY` để tránh giới hạn DEMO_KEY. — `scripts/`, `learningdata/astronomy/nasa_articles/`
- Tạo `learn.html` (Trạm Tri Thức / Learning Hub) — vanilla, chèn giữa dashboard và quiz. Header Sci-Fi + số dư tt; 2 thẻ chế độ (📖 Thư Viện Khám Phá → cuộn tới bài viết / ⚡ Thử Thách Quiz → `quiz.html`); lưới 4 bài viết nổi bật (NASA JWST, NASA AI exoplanet, ESA Gaia, MIT/EHT hố đen) thumbnail gradient + badge `[🛰️ NGUỒN]` + thưởng ☄️; click mở trình đọc trong trang (tóm tắt chính xác + link nguồn chính thức + đếm ~6s đọc → nút Nhận thưởng, 1 lần/bài lưu `astroq-read`, cộng qua `Economy.addAsteroids`). Icon lucide inline, song ngữ VI/EN. Nội dung bài = **minh hoạ học tập** (có ghi chú), không phải bài fetch trực tiếp. Dashboard: nút "Vào học & làm Quiz" → `learn.html` (thay vì thẳng `quiz.html`). Kiểm tra: JS cân bằng ngoặc, id đủ. — `learn.html`, `dashboard.html`
- Sửa điều khiển tàu + ESC popup (cả 2 game). **`game-defender.html`**: tàu **di chuyển 4 hướng** A=trái/W=lên/D=phải/S=xuống (hoặc mũi tên), trực tiếp không trễ (`CONFIG.shipSpeed=500`), kẹp trong nửa dưới sân; pháo **ngắm theo con trỏ** mỗi khung (bỏ xoay pháo bằng A/D chậm cũ); thêm **va chạm tàu↔thiên thạch** (đâm = trừ máu + phá thạch). **Cả 2 game**: nhấn **ESC** → popup "Kết thúc lượt?" (pause) với **Chơi tiếp** / **Về Sảnh Game**; ESC lần nữa = chơi tiếp. `game-dodge` tăng `MOVE` 440→520 cho nhạy. i18n `pause_title/pause_body/resume/to_hub` (vi+en), cập nhật hướng dẫn how. — `game-defender.html`, `game-dodge.html`
- Đồng bộ tên game + điều khiển: header 2 game bỏ tag tiếng Anh cứng (`[ ASTEROID_DODGE ]`/`[ SPACE_DEFENDER ]`) → `#gtag` hiển thị `[ tên game ]` theo ngôn ngữ đã chọn (khớp tên ở hub → chỉ 1 ngôn ngữ trên mọi màn). **`game-dodge.html`**: bỏ reticle "+"/điều khiển chuột (`cursor:default`, keyboard-only), thêm **di chuyển 2 chiều** bằng ◀▲▼▶ hoặc A/W/S/D (chuẩn hoá chéo), sửa hướng dẫn how1; gỡ `targetX` (fix ReferenceError trong `fit()`). Cả 2 game: **ESC = thoát ra Sảnh** (`games.html`). Defender giữ ngắm chuột + laser tím. — `game-dodge.html`, `game-defender.html`
- `game-defender.html`: nhân vật điều khiển đổi thành Luna (`img/luna2.png`, thay tàu vector, giữ tỉ lệ qua `sizeShip()` + fallback `b1.png`); đạn đổi thành **tia laser tím** (`#c084fc` + lõi sáng + glow tím), pháo cũng tông tím. `games.html`: render **sắp xếp game `ready` trước, `soon` sau** (sort ổn định); **bỏ nhãn "Sắp ra mắt" ở góc trên phải** (gỡ `.ribbon` + i18n `soon_ribbon`); **thêm 2 thẻ soon** "Mê Cung Thiên Hà" (🌀, card--maze xanh lá) & "Bắt Sao Băng" (🌟, card--catch vàng). — `game-defender.html`, `games.html`
- `game-dodge.html` (Né Thiên Thạch): nhân vật người chơi cố định dùng `img/luna2.png` (bỏ logic lấy theo nhân vật đã chọn/`SPRITES`/`getUser`); giữ `sizeByte()` theo tỉ lệ ảnh + fallback `img/b1.png` khi lỗi. — `game-dodge.html`
- Dựng game thứ 2 `game-defender.html` — "Space Defender" (bắn hạ thiên thạch bảo vệ tàu), canvas 2D vanilla 60fps, module hoá (CONFIG/SFX/draw*). Tàu đáy giữa + pháo laser xoay theo chuột/◀▶, bắn chuột trái/Space; thiên thạch xám 2 máu / tím 1 máu (+10đ +1tt) / vàng mở câu đố Thiên văn (đúng: +5tt +20% máu) / boss có thanh máu; HUD Máu tàu neon + Điểm + tt; nổ hạt, screen-shake + chớp đỏ khi trúng tàu; game over reward = tt bắn + `floor(điểm/25)` qua `onFinishGame`→Economy; trừ 5tt/lượt; reticle "+" + cursor:none. Hub: thẻ "Bắn Thiên Thạch/Asteroid Blaster" đổi thành **"Space Defender"** `status:"ready"` → `game-defender.html`. Kiểm tra: JS 2 file cân bằng ngoặc, id đủ. — `game-defender.html`, `games.html`
- `game-dodge.html` sửa 4 lỗi UX: (1) icon phí lượt chơi trong toast dùng ảnh `img/tt.png` (token `{tt}`, toast render innerHTML) thay emoji ☄️ — đúng "Thiên thạch tím". (2) Nhân vật điều khiển = nhân vật user đã chọn: đọc `astroq-user.character` → map `SPRITES` (id→`3d/*.png`, fallback avatar → `img/b1.png`), giữ tỉ lệ ảnh qua `sizeByte()`. (3)+(4) Con trỏ: ẩn cursor OS trên sân (`cursor:none`) và vẽ **reticle "+" cyan phát sáng bám chuột khi đang chơi** (`drawReticle`, `pointerIn`); overlay để `cursor:default` nên menu vẫn có trỏ thường. — `game-dodge.html`
- Đổi tên game trùng + dựng game thật: thẻ "Phòng Trái Lực/Gravity Room" trong hub (trùng tên card dashboard) đổi thành **"Né Thiên Thạch"/"Asteroid Dodge"**, `status:"ready"`, `file:"game-dodge.html"`. Tạo `game-dodge.html` — mini-game canvas 2D né thiên thạch: Byte theo con trỏ/phím, né thạch rơi (va = thua), thu tt +1 ngay mỗi viên; trừ 5 tt mỗi lượt lúc bắt đầu/chơi lại, thiếu tt → overlay sang Quiz; brief + game-over (điểm/kỷ lục localStorage/sao/tt khai thác), khó tăng dần, WebAudio, song ngữ. `games.html launch()` bỏ trừ phí (game tự lo). Kiểm tra: JS 2 file cân bằng ngoặc, id đủ, hub không còn tên trùng. — `game-dodge.html`, `games.html`
- Nối nút "Chơi Game": tạo `games.html` (Sảnh Trò Chơi/Arcade Bay) cùng phong cách cockpit-HUD; nút "Chơi Game" ở dashboard giờ điều hướng `window.location.href="games.html"` thay vì trừ tt + toast demo. Hub có 4 thẻ game (Phòng Trái Lực/Đường Đua Sao Chổi/Bắn Thiên Thạch/Ghép Chòm Sao) dạng dữ liệu `GAMES`, badge phí + độ khó, song ngữ, tích hợp economy. Chưa có game thật → tất cả gắn "Sắp ra mắt" (không tạo link hỏng, không trừ tt); `launch()` đã sẵn trừ phí + mở `file` khi đặt `status:"ready"`. — `games.html`, `dashboard.html`
- `quiz.html`: refactor sang **bố cục 3 cột** (grid 214px/1fr/208px, sticky sidebar, gộp 1 cột trên màn <1100px). **Cột trái `[ POWER_UPS ]`**: 5 vật phẩm (📡 Rada 50:50 x2 loại 2 đáp án sai · 🔍 Kính Lúp x3 hiện gợi ý · 🛡️ Khiên Tím x1 giữ chuỗi khi sai · ⏳ Ngưng Thời Gian x1 đóng băng 15s · 🚀 X2 Thiên Thạch x2 nhân đôi thưởng câu đúng kế) — glassmorphism, hover nảy, click flash + trừ badge, icon lucide-style inline SVG. **Cột phải `[ MISSION_HUD ]`**: đồng hồ đếm ngược 30s/câu (đỏ khi ≤10s, xanh băng khi freeze, hết giờ = sai), chuỗi đúng (streak), điểm tạm tính (= tt kiếm trong lượt). Center rộng hơn + khung minh hoạ cao hơn. Footer: bong bóng Byte to/rõ hơn, nút KÍCH HOẠT sáng rực khi đã chọn đáp án. State mới: timer/streak/frozen/shieldOn/boostOn/lastGain; sheet có trạng thái 🛡️ "được bảo vệ". Vanilla (giữ nguyên tông màu/token), đã kiểm tra: cân bằng dấu ngoặc JS, id không trùng. — `quiz.html`
- Khởi tạo kho dữ liệu câu hỏi `learningdata/` (mục tiêu 1.500 câu = 5 chủ đề × 300, chia 12 level × 25). Có `README.md` (kế hoạch + bảng reward theo level + tiến độ + lưu ý tính xác thực), `schema.json` (JSON Schema per-file/per-question), `_tools/rebalance_answers.py` (cân bằng vị trí đáp án A/B/C/D, tất định). Đã bàn giao mẫu chuẩn: `ai/level_01.json` + `ai/level_02.json` (50 câu, tiếng Việt, mascot Byte/Comet, đã validate: parse OK, không trùng id/câu, đủ 4 lựa chọn, đáp án đã phân bố đều). **Lưu ý:** `source_reference` chỉ loại nguồn uy tín (không phải trích dẫn truy xuất trực tiếp) — cần giáo viên rà soát trước khi phát hành, nhất là Level 10–12 & Vật lý lượng tử. Còn lại 1.450 câu sinh theo batch khi được yêu cầu. — `learningdata/`
- Tạo `quiz.html` — màn "Interactive Quiz & Diagnostic" toàn màn hình (vanilla, không framework), thay modal quiz demo cũ trên dashboard. Space Cockpit HUD: header [Rút lui viền đỏ] + bình năng lượng (fill cyan/green theo số câu đã trả lời) + tiền tệ ☄️ gem tím pulsing; badge `[ CHỦ ĐỀ · CÂU n/5 ]`; khung minh hoạ 2D Byte né vành đai thiên thạch (SVG + b1.png, animated); 4 đáp án A–D kiểu bảng điều khiển (hover nâng + glow, selected tím, correct→xanh + hạt thiên thạch bay lên bộ đếm, wrong→shake + amber); mascot Byte + bong bóng gợi ý; nút "KÍCH HOẠT" (engage) xác nhận → popup giải thích (đúng +20 ☄️ qua `Economy.addAsteroids`, sai gợi ý nhẹ nhàng) + "Câu tiếp theo"; màn Hoàn thành: 3 sao theo độ chính xác, tổng ☄️ khai thác, %, huy hiệu, actions Làm lại / Về Khoang Lái / Nhiệm vụ tiếp. Có âm thanh cơ khí (WebAudio synth), phím tắt A/B/C/D + Enter, song ngữ VI/EN đồng bộ `astroq-lang`, tôn trọng reduced-motion. State: idx/selected/status/earned/answered/locked; "props" = mảng `QUESTIONS`. Dashboard: nút "Vào học & làm Quiz" và "Làm Quiz ngay" → `quiz.html`; gỡ modal quiz demo + `openQuiz/answer/QUIZ` cũ. — `quiz.html`, `dashboard.html`
- Dashboard: tích hợp bảng Thống kê học tập & Gamification (vanilla, không framework). **Topbar**: đổi ô nhiên liệu thành "Thiên thạch tím" (Purple Meteor) với gem tím phát sáng (`.gem-badge`) + thêm gauge **Flight Streak** (🔥 amber glow). **Center**: thay widget cũ bằng **Stats HUD** 2 cột — trái: XP hành tinh (Cấp+% + tên) & "Khám phá n/8 Hành tinh" (pips); phải: 3 mini stat tile (Thời gian bay / Độ chính xác Quiz / Huy hiệu) với icon lucide-style. **Console dưới cards**: `[ RADAR_SKILL_SCAN ]` (radar SVG đa giác + quét xoay) + `[ LOG_HISTORY ]` (nhật ký hoạt động). Icon = inline SVG lucide (Flame/Sparkles/Clock/Target/Award/Orbit). Dữ liệu động qua object `STATS` (ghi đè bằng `window.AstroQStats`), vẽ qua `renderStats()` (theo LANG). Tăng blur/nền card HUD. Thêm i18n keys vi+en (st_streak, stats_tag, planet_prog, xp_label, galaxy_explore, planets_unit, tile_*, radar_sub, log_sub). Bỏ widget `.holo-widget`/`paintProgress` cũ. — `dashboard.html`
- Dashboard: bỏ chữ "tt" trong các toast liên quan Thiên thạch tím — thay bằng ảnh `img/tt.png` inline (token `{tt}`), chỉ giữ số + ảnh. Thay emoji ✅/❌ (không hợp UI) bằng icon SVG check/cross trong vòng tròn (neon `--good` / đỏ `#ff8a8a`, có glow); `toast()` nhận thêm tham số `type` (ok/bad/info) và render qua innerHTML. — `dashboard.html`
- Dashboard: bỏ hoàn toàn dãy nhân vật (Comet/Byte/3 Quark) + bệ Holo-Desk ở giữa; đưa khối "Chào mừng" lên sát mép khung kính (giảm padding hero). Thay khoảng trống bằng Widget Hologram gọn "Tiến trình khám phá Thiên hà" (quả cầu Trái Đất mini xoay + ProgressBar; mỗi 100 tt = 1 cấp, cập nhật theo số dư qua `paintProgress`). Tăng blur card HUD 16→20px + nền slate đậm hơn (.66→.5) cho nổi trên nền Trái Đất. Thêm khóa i18n `prog_tag/prog_title/prog_level` (vi+en). — `dashboard.html`
- Dashboard: dùng ảnh `background/Khoangluna.png` làm nền phủ kín màn hình (`.bg-photo`, cover, + lớp phủ tối nhẹ cho dễ đọc); bỏ khung kim loại bo 4 góc (`.cockpit` + frame/corner/rim/glare) và cảnh CSS cũ (nebula/starfield/earth) vì ảnh nền đã có sẵn khoang tàu + sao + Trái Đất. — `dashboard.html`
- Avatar khung tròn: bỏ zoom mặc định (100%, không lẹm đầu); zoom riêng theo `zoom` mỗi nhân vật (Castor=1.6 vì đầu nhỏ trong ảnh). Đồng bộ zoom sang avatar header dashboard (`avatarZoom`). — `select.html`, `js/auth-flow.js`, `dashboard.html`
- Thẻ bí ẩn: bỏ bóng đen, giữ dấu "?" phát sáng. — `select.html`
- Thêm 2 thẻ nhân vật bí ẩn (khoá) ở màn chọn: bóng đen + dấu "?" phát sáng, click hiện toast "sắp mở khoá". — `js/auth-flow.js`, `select.html`
- Đổi tên nhân vật ở màn chọn: raica→Castor, báo→Umbra, chim→Ignis, chó→Sirius, chuột→Lyrae, cú→Moros, cua→Karkinos. Đồng bộ cỡ avatar trong khung (zoom nhẹ + căn giữa; ảnh đều 1080²). — `js/auth-flow.js`, `select.html`
- Bỏ nút "Đăng nhập" riêng ở index; chỉ còn 1 nút "Trải nghiệm ngay" → mở popup (đăng nhập nằm trong popup). — `index.html`
- Luồng mới: Đăng ký → `select.html` → `dashboard.html`; đăng nhập → thẳng dashboard, dùng lại avatar. — `index.html`, `select.html`, `js/auth-flow.js`, `dashboard.html`
- Tạo `select.html` (ID card glassmorphism, roster 3D bounce, HUD role/trait/stats neon, dấu APPROVED) + `js/auth-flow.js`. Dùng ảnh `3d/` + `ava/`.
- Dashboard: avatar header = ảnh nhân vật đã chọn + tên phi hành gia.
- Bỏ icon thư ở nút Contact Us; làm sáng raica1 (CSS filter). — `index.html`
- Redesign dashboard thành khoang tàu: nền deep space + sao lấp lánh + Trái Đất, khung vỏ tàu kim loại, Holo-Desk (bệ hologram + đai năng lượng + slime bob), card HUD glassmorphism 4 góc neon. — `dashboard.html`

### 2026-07-24
- Tạo `economy.js` (số dư Thiên thạch tím; pilot mới đổi thành 0 khi đăng ký).
- Tạo `dashboard.html` (3 card, gating game <5 tt → modal nhắc Quiz, quiz demo +10 tt, song ngữ + đổi ngôn ngữ + Đăng xuất).
- index: popup Đăng nhập/Đăng ký (giả lập demo) + giả lập đăng nhập thành công.
- explorer: thêm bảng thông tin **Mặt Trời** (số liệu NASA), **Solar Neighborhood 3D** (sao theo RA/Dec/khoảng cách, click zoom + info), Sao Kim quay ngược chiều, toggle "Giảm cấu hình", popup xác nhận đi tới vùng khác / mở khóa nhiệm vụ; fix toggle quỹ đạo/nhãn ở region 2; đồng bộ ngôn ngữ + màn loading.
- index: nhiều màn hoạt cảnh nhân vật (b1/m1/luna1/cho1/raica1 bay vào; qg1+qb1+q1 → 3qok vòng vàng; q1 văng ra tự xoay); đồng bộ kích thước.
